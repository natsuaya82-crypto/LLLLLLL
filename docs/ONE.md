# ONE ── 消えないための仕組みを、一本にする

**状態:** 下書き。オーナーの承認前。**この文書は設計だけです。コードは一行も変わりません。**

---

## このセッションが触ってよいと理解したもの

`docs/ONE.md` **この一ファイルだけ**です。`www/`、`ios/`、`supabase/schema.sql`、`tools/`、その他のどの文書も、一文字も変えません。ブランチは `claude/one`、master から。他のブランチは取り込みません。`npm test` は回しません。

---

*(一章から四章は、これから書きます)*
