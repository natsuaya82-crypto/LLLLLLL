# みんなへ — 枝の状態と、読んで見つけたもの

サブリーダーから。**どの枝にも触っていません。読んで、試して、消しただけです。**
master `8581f22` 時点。

## 1. 今どこにいるか

| 枝 | 先 | master から後ろ | 最後の commit |
|---|---|---|---|
| `claude/kb6` | +20 | **49** | 08-27 11:27 |
| `claude/ask` | +2 | 0 | 08-27 11:32 |
| `claude/kb4` | +2 | **73** | 08-27 05:51 |
| `claude/badge` | +1 | 0 | 08-27 11:37 |
| `claude/gram` | +1 | **38** | 08-27 11:04 |
| `claude/plan` | +1 | **206** | 08-26 12:18 |
| `claude/yaa-g3pdv6` | +2 | 0 | 報告のみ |

`cowork-migration-review-wfx1ra`(+221) と `detailed-tasks-execution-ak61z2`(+216)
は master から **563 後ろ**。死んだ枝として除いています。

**後ろの数が大きい枝は、`git merge origin/master` を自分の枝に一度。**
`CLAUDE.md` が「報告の前に required」と書いているのはこれです ── 触るのは自分の
枝だけで、リーダーの merge を fast-forward にします。

## 2. 試し merge ── **コードは一つも衝突しません**

master の上に一本ずつ載せてみました（使い捨ての worktree、push なし）:

| 枝 | 衝突 |
|---|---|
| `claude/kb6` | `docs/BACKLOG.md` `docs/CHANGELOG.md` |
| `claude/gram` | `docs/CHANGELOG.md` |
| `claude/kb4` `claude/plan` `claude/ask` `claude/badge` | **なし** |

**`www/keyboard.js` も `www/shell.js` も `www/i18n/*.js` も自動で合流します。**
衝突は docs だけで、どれも「両側を残す」で片が付きます（両方が Unreleased の頭に
足すため）。

**ただしテキストが通ることと、意味が通ることは別です。** `kb6` は
`www/keyboard.js` と `tools/kb-check.mjs` に入っていて、master には既に `kb5` の
同じファイルへの変更が載っています。**合流した木で `npm run kb` を一度**、
リーダーが全部を積む前に。

## 3. `kb6` へ ── `www/shell.js` の一行、正しい使い方です

`viewReset()` に `ltQ=''` を足していますね。**あそこは私の章ですが、直す必要は
ありません。** `viewReset()` は「画面が忘れるもの」の一箇所で、字の検索語は
まさにそれです。私の `appIs()` と `viewLeft()` とは別の場所で、衝突しません。

**一つだけ知っておいてください**: `viewReset()` は `langOpen()` と `wipeAll()`
からしか呼ばれません。**「画面を離れたら忘れる」にはなりません。** それが要る
なら `viewLeft(from, to)`（同じファイル、少し下）が render() から呼ばれる方です。

## 4. `shots/` が二通りに使われています

`.gitignore:31` に:

```
# pictures taken to look at once (tools/shot.mjs)
shots/
```

**なのに master には `shots/` の PNG が4枚、追跡されています**（`gram-ja.png`
`part-order-untouched-ja.png` `part-stage-empty-ja.png` `part-stage-made-ja.png`、
各 68KB 前後）。

私は同じ絵を出すとき、**gitignore に従って `docs/reports/door/` に置きました。**
**二つのセッションが別の答えを出しています。** どちらが正しいかは決める話で、
私が決めません。ただ**どちらかに揃えないと、次の人も迷います:**

- 絵を残すなら `.gitignore` から `shots/` を外す（「一度見るための絵」ではなくなる）
- 残さないなら `docs/reports/` に置く場所を決める

**どちらでもよいので、一つにしてください。**

## 5. 読んで見つけたもの（前の報告から、まだ残っているもの）

`docs/reports/check2-2026-08-27.md` に全19件あります。**まだ持ち主に届いて
いないかもしれないもの:**

**⑧ 「画面の半分まで」が 0.55**（`www/keyboard.js:1083` と
`KeyboardViewController.swift:47`）。JS と Swift は一致していて `kb-check` が
突き合わせています ── **ずれているのは実装同士ではなく、オーナーの言葉と数**です。
0.5 にするかはオーナーのもの。**`kb6` の方、心当たりがあれば。**

**嘘になっているコメント二つ:**

- `www/sheet.js:1110`「There is no Swift behind `sheet` yet」→
  **`LinguaShare.swift:276` に在ります**（`renderPdf` も）
- `www/settings.js:547`「`.plterm .pwas` is NOT in the stylesheet yet」→
  **`index.html:1493` に在ります**

どちらも「動くコードの上に、古い説明が乗っている」形です。持ち主のセッションで
一行直してください。

## 6. `claude/ask` へ

10言語とも +1/-1 で対称でした。`ask.word.ask` から個数を外した件、
**鍵の数は動いていません。** `i18n-check` は通るはずです。

## 7. master のゲート

（結果が出たらここに足します）
