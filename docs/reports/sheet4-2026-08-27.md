# sheet4 — 「ここで書く」を無くし、書き出すたびにファイルとして残す

## Scope

- **Goal:**
  ① 用紙の「ここで書く」（アプリ内 Markup）を無くす。
  ② 書き出しは毎回ファイルとして端末に残す。
- **Branch:** `claude/sheet4`（`origin/master` = `c09eb6a` から）
- **Owns (may change):**
  `www/sheet.js` · `ios/App/App/LinguaShare.swift`（`markup` の口だけ）·
  `ios/App/App/LinguaMarkup.swift` · `ios/App/App.xcodeproj/project.pbxproj`
  （ファイルを外すときだけ）· `www/act-map.js` · `www/i18n/*.js` ·
  `tools/sheet-check.mjs` · `tools/fixture.mjs` · `docs/`
- **Does NOT own:** それ以外すべて。とくに
  `www/index.html`（リーダーのもの。CSS が要るなら測って報告に出す）·
  `www/keyboard.js` `tools/kb-check.mjs`（kb6）·
  `www/grammar.js` `www/wordsheet.js`（文法）
- **Decision it implements:**
  OWNER 2026-08-27（夕方・実機ビルド #96）
  > 「あとはシートの書き出しだけど、ここで書く無くして。毎回ファイルに保存して欲しい。」

  これは同じ日の**午前**の決定を **supersede** します:
  > 「そのままdlした端末上で書くとか？じゃないと無理じゃね」 OWNER 2026-08-27（午前）

  午前の決定で入ったのが `shWrite()`（`www/sheet.js`）と
  `ios/App/App/LinguaMarkup.swift`、`LinguaShare.markup` です。
  夕方の決定が新しいので、そちらが仕様です。**古いほうは superseded として
  記録し、それが書かせた文（`docs/` の「アプリの中で書ける」）は同じコミットで
  消します。**
- **Check to run:** `npm run sheet`、加えて `es5` `dead` `act` `i18n` `assets` `box`。
  `npm test` は回しません（リーダーのもの）。

## 衝突の確認（着手前）

`git log --oneline --all -- <file>` を持ちファイル全部にかけ、`master` より
先に居るブランチ 8 本の差分も見ました。**私のファイルに他のセッションは
居ません。**

- `www/sheet.js` `LinguaShare.swift` `LinguaMarkup.swift` `project.pbxproj`
  `tools/sheet-check.mjs` を触っている先行ブランチ: **なし**
- `www/act-map.js` `www/i18n/*` `tools/fixture.mjs` を触っているのは
  `origin/claude/detailed-tasks-execution-ak61z2` のみ。これは
  `docs/STATE.md` が「456 後ろで、四つとも master が別の道で済ませたので
  取り込まずに落とした」と書いている**死んだブランチ**です。生きている
  セッションではありません。
- `www/index.html` は `detailed-tasks-execution-ak61z2` のみ。私は触りません。

## まだ測っていないこと（着手時点）

`shMake()` が今どうなっているか ── ファイルに落としているのか、共有シートを
出しているのか、画面に持っているだけなのか ── は**この宣言の時点では読んで
いません**。次の手順が「読んで測る」で、結果は下に足します。
