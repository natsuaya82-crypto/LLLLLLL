# `claude/sales` ── 売上とアナリティクス（`docs/FEATURES.md` § 8 の ①②③）

枝: `claude/sales`（親は `origin/claude/leader-handover-2026-08-25-3kz0b8`）
日付: 2026-08-26

**この節（§1）だけ先に push しています。** `docs/FEATURES.md` § 8 が
「**確かめてから作る。推測するな**」と名指しで言っているところなので、
コードより先に出します。§2 以降は書き次第この同じファイルに足します。

---

## 1. Apple の API が実際にどうなっているか

全部 Apple の公式ドキュメントで確かめました。出典は各項の末尾に。
**推測はありません。分からなかったことは「分からない」と書いてあります。**

### 1-1. 売上（①）と ダウンロード数（②）── **同期で取れる。毎回開くたびが成立する**

```
GET https://api.appstoreconnect.apple.com/v1/salesReports
```

- **同期**です。頼んで待つ形ではありません。1回の GET で本文が返ります。
- 返るのは **`application/a-gzip`** ── gzip で固めた **TSV**（タブ区切り）です。
  JSON ではありません。**展開してタブで割る必要があります。**
- 必須の絞り込みは四つ:

  | 名前 | 値 |
  |---|---|
  | `filter[reportType]` | `SALES` / `SUBSCRIPTION` / `SUBSCRIPTION_EVENT` ほか |
  | `filter[reportSubType]` | `SUMMARY` / `DETAILED` ほか |
  | `filter[frequency]` | `DAILY` / `WEEKLY` / `MONTHLY` / `YEARLY` |
  | `filter[vendorNumber]` | **§1-5 を読んでください。四つ目の鍵です** |

  `filter[version]` は任意ですが、**組み合わせは決まっていて自由ではありません。**
  ドキュメントの表をそのまま写すと:

  | reportType | reportSubType | frequency | version |
  |---|---|---|---|
  | `SALES` | `SUMMARY` | DAILY, WEEKLY, MONTHLY, YEARLY | `1_0` |
  | `SUBSCRIPTION` | `SUMMARY` | **DAILY のみ** | `1_3` |
  | `SUBSCRIPTION_EVENT` | `SUMMARY` | **DAILY のみ** | `1_3` |
  | `INSTALLS` | `SUMMARY` | **MONTHLY のみ** | `1_2` |

  外れると 400 です。**この表以外の組み合わせは無い**ので、コードは表を持ちます。

- **遅れは一日です。** Apple の言葉で「next-day data」。
  一日の区切りは **太平洋時間の 0:00–23:59** ── `daily-prompt/index.ts` の
  `pacificDay()` が既に同じ理由で同じことをしています（「Apple が App Store を
  動かしているタイムゾーン」）。**今日の売上は今日は取れません。**
- `filter[reportDate]` は DAILY では任意ですが、**省くと「最新のもの」であって
  「今日」ではありません。** 日付を明示して、無ければ前の日へ下がるほうが、
  何日のものを見ているのか画面が言えます。

出典:
- https://developer.apple.com/documentation/appstoreconnectapi/get-v1-salesreports
- https://developer.apple.com/help/app-store-connect/view-sales-and-trends/download-and-view-reports/

### 1-2. 解約と継続率（③）── **数は取れる。「継続率」という欄はどこにも無い**

`SUBSCRIPTION` / `SUMMARY` / `DAILY` / `1_3` が **その日の断面**です。
契約者数はここにあります（列名そのまま）:

```
Active Standard Price Subscriptions
Active Free Trial Introductory Offer Subscriptions
Active Pay Up Front Introductory Offer Subscriptions
Active Pay as You Go Introductory Offer Subscriptions
… ほか、オファー種別ごとに 17 列
```

解約は `SUBSCRIPTION_EVENT` / `SUMMARY` / `DAILY` / `1_3` の **`Event` 列**です。
取りうる値は 14 個あり、解約に当たるのは `Cancel` と
`Canceled from Billing Retry`、継続に当たるのは `Renew` と `Reactivate` です。

**⚠ 継続率（retention）そのものを返す欄は、どちらのレポートにもありません。**
出すなら**こちらで割り算します**。そして割り算には決めることが二つあります ──
**何を分母にするか**（前日の契約者数か、その月に始まった人か）と
**どの期間か**。`docs/FEATURES.md` § 8 の
「Not decided: what a number is called, what period it covers」がまさにこれです。
**決めていません。§3 で聞きます。**

出典:
- https://developer.apple.com/help/app-store-connect/reference/reporting/subscription-report/
- https://developer.apple.com/help/app-store-connect/reference/reporting/subscription-event-report/

### 1-3. App Store Analytics ── **「頼んで、あとで取りに来る」で当たり。四往復です**

`docs/FEATURES.md` § 8 の警告どおりでした。こちらは salesReports とは別物です:

```
1. POST /v1/analyticsReportRequests            ← 頼む（accessType: ONGOING）
2. GET  /v1/analyticsReportRequests/{id}/reports
3. GET  /v1/analyticsReports/{id}/instances     ← DAILY / WEEKLY / MONTHLY
4. GET  /v1/analyticsReportInstances/{id}/segments
5. その segment の url を落とす
```

ドキュメントの言葉:

> When making a request with this endpoint, the accessType `ONGOING` is most
> common and provides current data. This report request **generates reports
> daily**.

> The download URLs are valid for **5 minutes** after generating.

**「毎回開くたび」との関係を、混ぜずに書きます。**

- **1 は一度きり**で、毎回ではありません。頼んでおけば Apple が毎日作ります。
- **2–5 は同期**なので、**開くたびに叩くこと自体はできます。**
- ただし**中身は日次より新しくなりません。** 開くたびに取りに行っても、
  返るのは Apple が最後に作った日次のものです。

出典:
- https://developer.apple.com/documentation/appstoreconnectapi/post-v1-analyticsreportrequests
- https://developer.apple.com/documentation/appstoreconnectapi/get-v1-analyticsreportinstances-_id_-segments

### 1-4. 認証 ── **ES256 の JWT で合っています。有効期限の上限は 20 分**

ヘッダ `alg=ES256` `kid=<Key ID>` `typ=JWT`、
本文 `iss=<Issuer ID>` `iat` `exp` `aud=appstoreconnect-v1`。

> For most requests, App Store Connect **rejects a token with a lifetime
> greater than 20 minutes**.

`.p8` は PKCS#8 の EC 秘密鍵で、Deno の `crypto.subtle` が
`ECDSA / P-256 / SHA-256` でそのまま署名できます。**外の library は要りません。**

出典: https://developer.apple.com/documentation/appstoreconnectapi/generating-tokens-for-api-requests

### 1-5. ⚠ 鍵は三つではなく **四つ**要ります

`supabase/setup.md` § 10-2 は `ASC_ISSUER_ID` / `ASC_KEY_ID` /
`ASC_PRIVATE_KEY` の三つを挙げています。**`filter[vendorNumber]` が必須で、
これはその三つのどれでもありません。**

そして **API にはベンダー番号を教えてくれる口がありません。** 人が
App Store Connect の **「お支払いと財務レポート」**（Payments and Financial
Reports）の左上から読み取って、手で置くしかありません。

**`supabase/setup.md` § 10-2 に四つ目として `ASC_VENDOR_NUMBER` を足しました。**
これが無いと、鍵を三つ正しく置いても売上は 400 で返ってきません。

出典: https://developer.apple.com/documentation/appstoreconnectapi/get-v1-salesreports
（`filter[vendorNumber]` は required）

---

## 2. で、「画面を開いたときに毎回」は成立するのか

**成立します。ただし「毎回取りに行ける」であって「毎回新しい」ではありません。**
この二つは別のことで、混ぜると嘘になるので分けて書きます。

| | 開くたびに取りに行けるか | 中身の新しさ |
|---|---|---|
| ① 売上 | **はい**（同期の GET 一本） | **前日まで**（太平洋時間） |
| ② ダウンロード数 | **はい**（同じ一本） | **前日まで** |
| ③ 契約者数・解約 | **はい**（同期の GET 二本） | **前日まで** |
| ③ 継続率 | 割り算はこちら | **決まっていません（§3）** |
| Analytics | 2–5 は**はい**、1 は一度だけ | **日次**、最初の一回は Apple が作るまで待ちます |

**だから、オーナーの決定「画面を開いたときに毎回」は、Apple の側では
何も邪魔していません。** 取りに行く回数の話であればそのとおりに作れます。
**引っかかるのは「毎回」ではなく「今日の数字は無い」ほうです** ── 画面には
必ず**いつの日の数か**が要ります。そうでないと、昨日の数を今日の数として
見ることになります。これは説明文ではなく、その数が何の数かという事実です。

**なので ①②③ は salesReports 三本だけで建てます。** Analytics（四往復のほう）は
**建てていません** ── ①②③ に要るものが salesReports で全部揃うからで、
ダウンロード数も `SALES` レポートの `Units` にあります。Analytics が要るのは
それ以外の指標（表示回数、クラッシュ、など）を出すと決めたときで、
**それは決まっていません。**

---

## 3. 決めていないこと ── オーナーに聞くところ

`CLAUDE.md`「Deciding」に当たるものだけを並べます。**こちらでは決めていません。**
今日の画面は「Apple が返した粒度をそのまま出す」で作ってあり、
どれも**答えが出てから直せば一行か二行**です。

### 3-1. 通貨 ── いま「足していません」

`www/store.js` と `ios/App/App/LinguaStore.swift` が同じ文で禁じています:

> Building "$" + a number is how an app ends up showing dollars to somebody
> being charged yen.

Apple は**国ごとに、その国の通貨で**払います。売上レポートは
`Developer Proceeds` と `Currency of Proceeds` の組で返ってくるので、
**一つにまとめるには為替が要り、為替はこのリポジトリにありません。**
Apple もくれません（くれるのは「いくら払ったか」であって「今いくらか」では
ないので）。

**今こうなっています ── 通貨ごとに一行:**

```
売上    JPY 700
売上    USD 3.49
```

**選択肢:**

| | |
|---|---|
| **A. このまま**（通貨ごとに一行） | 嘘が一つもない。国が増えると行が増える |
| B. 円だけ出す | 一番読みやすい。**他の国の売上が画面から消えます** |
| C. 為替を入れて円にまとめる | 一行になる。**為替 API という外部の鍵がもう一つ増えます**。しかも Apple が実際に払う額とは一致しません |

**A で置いてあります。**「一つの数にしない」がいちばん壊れないからで、
B や C にするのはオーナーの判断です。

### 3-2. 継続率 ── いま「率は出していません」

**どちらのレポートにも継続率という列はありません**（§1-2）。出すには
二つ決める必要があります:

1. **何を分母にするか** ── 前日の契約者数か、その月に始まった人か
2. **どの期間か** ── 1日か、1ヶ月か、初回から N ヶ月後か

**今こうなっています ── Apple が数えた数だけ:**

```
契約    21     ← その日に生きている契約
継続     9     ← その日の Renew の件数
解約     3     ← Cancel と Canceled from Billing Retry
```

この三つがあれば率は後からいつでも足せます。**逆に、率だけ出すと元の数が
見えなくなります。**

### 3-3. 数の呼び名

`docs/FEATURES.md` § 8 の「Not decided: what a number is called」です。
今は日本語で 売上／ダウンロード／契約／継続／解約／日 としてあります。
**ダウンロードは特に確認をお願いします** ── Apple は三つを別々に数えていて、
今出しているのは一つ目だけです:

| | いま |
|---|---|
| 初めて入れた人 | **これを「ダウンロード」として出しています** |
| 入れ直した人（`3` `3F`） | 数えてはいるが画面に出していません |
| アップデート（`7` `7F` …） | 同上 |

どれを「ダウンロード数」と呼ぶかで数字が変わります。分類は Apple の表
そのままで、こちらで決めた分け方ではありません
（https://developer.apple.com/help/app-store-connect/reference/reporting/product-type-identifiers/ ）。

### 3-4. 鍵が入っていないとき、画面は「空欄」です

`CLAUDE.md`「アプリ内に説明書くの禁止」に従って、**画面には理由を出していません。**
鍵がまだのときも、Apple にその日のレポートがまだ無いときも、同じ空欄です。
理由は `supabase/setup.md` § 10-5 の表と、Edge Function の Logs で見ます。

**これでよければこのままです。** ここは 2026-08-22 の narrowing
（「必要な説明は書いてね。みてわからないのが一番ダメ」）に当たるかもしれない、
と思ったところです ── ただし narrowing は「アプリが何かを取り上げた画面」に
限られていて、ここは何も取り上げていないので、**書かない側に倒してあります。**

---

## 4. 画面のどこに何を置いたか

⚠ **`docs/FEATURES.md` § 8 は「画面がどう見えるか」を「決まっていない」と
書いています。だからこれは提案です。「こう置いた、これでよいか」を聞いています。**

**新しいルートは作っていません** ── `www/shell.js` の `PAGES` は他のセッションが
持っているファイルなので触っていません。既にある管理画面（`vAdmin`）の
`adminRow` の列に足しただけです。

```
  人        1,204        ← もとからあった四つ
  投稿      8,821
  言語        932
  通報          3   ›
  売上     JPY 700       ← ここから下が今回
  売上     USD 3.49
  ダウンロード     3
  契約          21
  継続           9
  解約           3
  日     2026-08-25
  スタッフ                ← もとからあった
  @lingua
```

- **`adminRow()` をそのまま使っています。** 行の高さも見た目も、
  設定画面の行と同じものです。新しい CSS は一行も足していません
  ── `CLAUDE.md`「NO ROUNDED BOX」と「Rows in one list are one height」
- **「日」は要ります。** Apple のデータは前日ぶんまでなので、日付の無い数字は
  「昨日の数を今日の数として見る」ことになります。これは説明文ではなく、
  その数が何の数かという事実です
- 三つのレポートは Apple の側で別々に用意されるので、**日がずれたときは
  両方出ます**（`2026-08-23 / 2026-08-25`）。一つ選ぶと残り二つについて
  嘘になるので
- **押せる行は増えていません。** 全部ただの行なので、`press` の
  `buttons pressed` は動かないはずです

---

## 5. オーナーがやること

`supabase/setup.md` § 10 に全部書き直しました。ここは要約です。

1. **§10-1** App Store Connect でキーを作る（`Sales and Reports` の役割）。
   `.p8` は**一度しか落とせません**
2. **§10-2** Supabase → Edge Functions → Secrets に**四つ**置く

   ```
   ASC_ISSUER_ID       App Store Connect の統合ページ上部
   ASC_KEY_ID          同じページ、キーの行
   ASC_PRIVATE_KEY     .p8 の中身まるごと（改行込み）
   ASC_VENDOR_NUMBER   ★ 別の場所です（下）
   ```

   ⚠ **四つ目は今回わかったものです。** `filter[vendorNumber]` は必須で、
   **API に自分の番号を教えてくれる口がありません。**
   App Store Connect → **お支払いと財務レポート** → 左上の灰色の細い字を
   人が読み取ります。これが無いと Apple は 400 を返し、画面は空欄のままです

3. **§10-3** 関数を置く

   ```
   npx supabase functions deploy appstore
   ```

4. **§10-4** 管理画面を開く。数字が入っていれば通っています

**鍵はリポジトリに一つも入っていません。** 値はどこにも書いていません ──
`supabase/setup.md` に書いてあるのは「どこに置くか」だけです。

---

## 6. 何が確かめてあって、何が確かめていないか

**`CODE CONFIRMED`**

- `npm run es5` / `npm run dead` / `npm run act` / `npm run i18n` ── 四本とも緑。
  `npm test` は回していません（リーダーの仕事）
- `npm run rls` ── **回していません。回す理由がありません。**
  `supabase/schema.sql` は一行も触っていないからです（§7）
- **Edge Function は実際に動かして確かめました。** Deno はこの箱に無いので、
  `Deno` と `fetch` を差し替えて **Node で本物のハンドラを呼んでいます**:

  ```
  node --experimental-strip-types supabase/functions/appstore/check.mjs
  ```

  18 件、緑。ES256 の署名も gzip の展開も本物です（Node の `crypto.subtle` と
  `DecompressionStream`）。**ゲートには入っていません** ── `tools/gate.mjs` は
  `www/` と `ios/` を歩くもので `supabase/` を知らないので、`npm run rls` と
  同じく手で回すものです

- **バグを三つ戻して、赤くなるのを見てから取りました:**

  | 戻したバグ | 赤くなった行 |
  |---|---|
  | 二つの通貨を足して一つの数にする | `two currencies stay two rows` |
  | 返ってこなかったレポートを 0 にする | `nothing ready is null and not 0` ほか2件 |
  | 管理者チェックが、聞けなかったとき「はい」と答える | `a stranger is refused` |

**`DEVICE CONFIRMED` ── 未。** Linux なので実機は踏めません。

**`KEY CONFIRMED` ── 未。これが一番大きい未確認です。**
鍵はオーナーにしか作れないので、**本当に Apple から数字が返るところは
誰もまだ見ていません。** 確かめてあるのは、Apple の**ドキュメントに書いてある
形**（§1、出典つき）と、**その形で返ってきたときにコードが正しく振る舞うこと**
（§6 の 18 件）の二つで、**Apple が本当にその形で返すかどうかは別の事実です。**

具体的に、鍵を入れて初めて分かるものを挙げておきます:

- `filter[version]` の値（`SALES`/`SUMMARY` は `1_0`。過去のフォーラムには
  `1_1` を要求されたという書き込みがあり、今のドキュメントの表は `1_0` です。
  外れると Apple が 400 でその旨を返します）
- ベンダー番号が正しいか
- `.p8` の貼り付けが Supabase の Secrets を通っても壊れないか
- WKWebView からの CORS（プリフライトは関数側で受けてあります）

---

## 7. 触っていないもの／止めたところ

**territory の外は一つも触っていません。**

```
触った:  supabase/functions/appstore/  （新規：index.ts と check.mjs）
        supabase/setup.md             （§10 を書き換え）
        www/net.js                    （netStore() を1つ追加）
        www/mod.js                    （adminAsk / adminDay / adminMoney と6行）
        www/i18n/*.js                 （10言語に6鍵ずつ）
        docs/FEATURES.md · docs/CHANGELOG.md · docs/reports/
```

**触っていない:** `www/index.html` `www/shell.js` `www/settings.js` `www/sns.js`
`www/post.js` `www/home.js` `www/keyboard.js` `www/share.js` `www/onboard.js`
`www/store.js` `ios/` ── どれも他のセッションのものです。

**`www/act-map.js` も触っていません。** 押せるボタンを一つも足していないので
（全部ただの行）、登録する名前がありませんでした。

**`supabase/schema.sql` も `tools/rls-check.mjs` も触っていません。** § 2-5 は
「表を足すなら」でしたが、**表は要りませんでした** ── Apple の売上レポートは
同期で返るので、控えておくものが何もありません。`docs/FEATURES.md` § 8 の
「Apple が返すものを入れる表（未設計）」は、**設計するものが無かった**という
結論です。RLS の 34 本も増えていません。

### 止めたところ ── 一つだけ、リーダーに見てほしいものがあります

**`tools/fixture.mjs` に、Apple の数字が入った状態の顔がありません。**
このファイルは territory の外なので触っていません。

何が起きるかというと、`i18n-check` と `press` は管理画面を
**`ADMIN_ASC` が null の状態**でしか歩きません。つまり:

- 六つの**見出し**（売上・ダウンロード・契約・継続・解約・日）は歩かれています。
  行は数字が無くても必ず描かれるので
- **数字が入った状態の `adminMoney()`（通貨ごとに一行になるところ）だけが、
  どの検査にも歩かれていません**

穴としては小さい（`t()` を通っていることは見出し側で押さえられている）のですが、
**黙っていると「歩かれている」と読まれるので書いておきます。**
`tools/fixture.mjs` に `ADMIN_ASC` を持った顔を一つ足せば埋まります。
**リーダーの判断でお願いします。**

---

## 8. 見た目 ── 実際に撮って見ました

`tools/shot.mjs` そのままでは撮れませんでした。`tools/fixture.mjs` の
「the admin screen」の顔は **html を返してから状態を戻す**ので、shot.mjs が
そのあと呼ぶ `render()` はドア（パスワード画面）に戻ります ── shot.mjs 自身の
コメントが「an entry that tidied up after itself photographs the screen it
tidied back to」と書いているとおりでした。**shot.mjs と fixture.mjs は
どちらも territory の外なので触っていません。** 状態を置いたまま
`render()` する使い捨てのスクリプトで撮りました（コミットしていません）。

撮ったもの: 数字の入った状態 / 鍵を入れる前 / ダーク。

**見て確かめられたこと:**

- **行の高さは上の四つと同じ**です。`adminRow()` をそのまま使っているので、
  人・投稿・言語・通報 と 売上・ダウンロード・契約・継続・解約・日 が
  一続きの一つのリストに見えます
- **囲いは一つも増えていません。** 全部下線だけの行です
- **ダークも問題ありません。** 色は全部変数越しなので、白のときと同じに出ます

**見て初めて分かった、聞きたいこと（§3-1 の続き）:**

- **通貨ごとに一行にすると、左が「売上」「売上」と同じ名前で二行並びます。**

  ```
  売上        JPY 4900
  売上        USD 13.96
  ```

  読めなくはないのですが、**同じ名前の行が並ぶのは他のどの画面にも無い形**です。
  「売上 JPY」「売上 USD」のように左に通貨を出す手もあります（数字だけが右に
  揃うので、そちらのほうが表らしくはなります）。**どちらもこちらでは決めません。**

- **鍵を入れる前は、六行が名前だけで空欄のまま並びます**（2枚目）。
  §3-4 で聞いたとおり、アプリに理由は書いていません。**六行が空で並ぶのを見て
  「壊れている」と読むなら、そこは考え直すところです。**

### 見つけたが直していないもの（私の変更ではありません）

**通報カードの「@@veth を凍結」── @ が二つ出ています。**

`www/i18n/*.js` の `mod.out` は**十言語すべてが `"@{0} を凍結"` のように
文字列の側に @ を持っている**のに、`www/mod.js` が
`t('mod.out', '@'+r.who)` と @ を足して渡しています。`mod.in` も同じ形です。

`www/mod.js` は私の territory ですが、**今回の作業とは別のバグ**なので
直していません（`CLAUDE.md`「*while I'm in here*」）。**リーダーへ。**
直すなら `mod.js` 側の `'@'+` を落とすほうです ── 文字列十本を直すと、
@ の位置が言語によって違うのに一箇所に決め打ちすることになります。

---

## 9. 「項目ごとで分けて別ページにして」OWNER 2026-08-26

**⚠ リーダーへ。これは私の territory では作れません。実装していません。**
オーナーの指示なので**仕様として記録**し、絵だけ作って止めました。

### 9-1. なぜ全部縦に並んでいたか

理由は一つで、**新しいルートを作れなかったから**です。`www/shell.js` の
`PAGES` は他のセッションが持っているので、既にある `admin` の一枚に足しました。
その結果が「①②③④が全部一列」で、**オーナーの言うとおり読みにくいです。**
`CLAUDE.md` の形の決まり ──「choosing is a screen, changing is the screen you
arrive at」── からしても、一列に詰めたほうが間違いです。

### 9-2. 「読み直す」は何か

一番上にあった `読み直す` は **Refresh（もう一度サーバーに聞く）**です。
`t('mod.again')`、`www/mod.js` の `adminLoad` を呼ぶボタンで、**通報画面から
来た既存のもの**です（今回足したものではありません）。

**これは要らないと思います。**「画面を開いたときに毎回」取りに行くと決まって
いるので、開いた時点で最新です。**消すかどうかはオーナーの判断です。**

### 9-3. 何が要るか ── 三つとも他人のファイルです

絵のとおりに作るには、これだけ要ります:

| ファイル | 要るもの | 持ち主 |
|---|---|---|
| `www/shell.js` | `pageName()` の分岐。**無いと中のページの名前が全部「管理」になります**（絵で確認済み） | 他セッション |
| `tools/act-check.mjs` | `walkArg('admin', vAdmin, [...])` の一行。**無いとゲートが赤になります** ── 中のページにしかないボタン（スタッフの追加など）が「どの画面も名指ししていない登録」として落ちます | ─ |
| `tools/fixture.mjs` | ページごとの顔 | ─ |

`www/route-map.js`（新ルートにする場合）も同じです。
**`walkArg` は手書きの一覧**で、ページに聞いているわけではありません
（`tools/act-check.mjs:211-253`）。だから引数を増やすと必ずそこに一行要ります。

### 9-4. 絵（`shots/mock-admin*.png`、コミットしていません）

`www/mod.js` を**手元だけで**書き換えて撮り、**戻しました。**
枝には入っていません（`git status` clean、es5 / dead / act は緑のまま）。

```
  管理                          売上
  ─────────────────           ─────────────────
  売上                  ›      JPY          4900
  ダウンロード      41  ›      USD         13.96
  契約              21  ›      日     2026-08-25
  アプリの中      1284  ›
  通報               1  ›      契約
  スタッフ           2  ›      ─────────────────
                               契約            21
                               継続             9
                               解約             3
                               戻り             1
                               日     2026-08-25
```

**分け方はこちらでは決めていません。** オーナーの ①②③④ をそのまま一枚ずつに
しただけです。聞きたいのは三つ:

1. **この六つの分け方でよいか。** 「アプリの中」（人・投稿・言語）を一枚に
   まとめてありますが、三枚に割ることもできます
2. **目次の右の数字は要るか。** 今は各ページの代表を一つ出しています。
   **売上だけは出せません**（通貨が複数あるので一つに選べない ── §3-1）
3. **ダウンロードは三つに割ってあります**（はじめて／入れ直し／更新）。
   一列だったときは「はじめて」しか出していませんでした。
   ページが増えたので三つとも置けます

### 9-5. 私が今できること

**`www/mod.js` の中身（目次と各ページの本文）は全部書けます。**
止まっているのは**ページの名前**と**検査の一行**だけです。
リーダーが `www/shell.js` と `tools/act-check.mjs` を誰かに割り当てれば、
そちらと合わせて仕上げられます。**指示があるまで枝には入れません。**
