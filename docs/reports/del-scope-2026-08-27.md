# `claude/del` ── 下書きの削除。scope の宣言

枝: `claude/del`（親 `origin/master` = `a173aa1`）／ 2026-08-27
**これが最初のコミットで、コードは一行も入っていません。**

```
アカウント削除は全部消すって言ってるやん。言語を消すでも言語系は全部消すって
言ってるでしょ？なんでそれに含まれないと思うの？          OWNER 2026-08-27
```

## この枝がやること

1. **DELETE REVIEW を書く**（`docs/DATA_SAFETY.md` の形で）── コードより先。
2. **`SLICES` の件でいったん止めてリーダーに投げる。**`SLICES` に入れると削除と
   バックアップが同時に決まってしまい、**オーナーはバックアップ側について
   何も言っていない**ため。
3. リーダーの返事を待ってから実装。

## 変えてよい file（リーダーが指定）

```
www/settings.js · www/core.js · www/post.js
supabase/schema.sql（触ったら npm run rls）
tools/backup-check.mjs · tools/fixture.mjs · docs/
```

## 変えない file

```
www/index.html      リーダーのもの。CSS が要るなら測って投げる
www/keyboard.js www/share.js tools/kb-check.mjs      claude/kb6
www/assist.js www/words.js                           claude/ask
```

## 回す検査

`npm run backup` · `npm run plan` · 速い五つ。**`npm test` は回しません**
（`docs/SESSIONS.md` ── リーダーのもの）。

消す側の検査は**バグを戻して赤を見てから**。**両方向を見ます** ──
「消えるはずのものが残っている」と「**残るはずのものが消えた**」。
後者のほうが取り返しがつきません。
