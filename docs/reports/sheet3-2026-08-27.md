# sheet3 — 書き出した PDF が落とせない／戻した PDF が読まれない

OWNER 2026-08-27 実機ビルド #93
> 「自分の文字作ろうとpdf書き出してもダウンロードできないよ？アップロードしても認識もされない。」

## scope

branch `claude/sheet3`。触る: `www/sheet.js` · `ios/App/App/LinguaShare.swift` ·
`www/i18n/*.js`（自分の行）· `tools/sheet-check.mjs` · `tools/fixture.mjs`（自分の面）·
`docs/CHANGELOG.md` · `docs/BACKLOG.md` · `docs/reports/`。
触らない: `www/index.html` · `shell.js` `onboard.js` `sns.js` `keyboard.js` `share.js`
`settings.js` `store.js` `post.js` `import.js` `wordsheet.js` `glyph.js` `home.js`
`net.js` `supabase/` · `ios/App/App/LinguaStore.swift` `ios/App/App/MainViewController.swift`
`ios/App/LinguaKeyboard/`。

## 2-1 の原因（確定）

**`<a download>` ではありません。** `www/sheet.js` は既に `backup.js` と同じ道を通って
います — `sharePlug()` → `Capacitor.nativePromise` です。`registerFont` も
`Capacitor.Plugins` も使っていません。

原因は **native 側にその口がない** ことです。

- `www/sheet.js:1089` — `p('LinguaShare', 'sheet', {name:shFileName(), b64:btoa(pdf)})`
- `ios/App/App/LinguaShare.swift:24-35` — `pluginMethods` は
  `write` `registerFont` `keep` `kept` `dropKept` `keepVoice` `voice` `dropVoice`
  `pickPhoto` `audio` `settings` の11個。**`sheet` はありません。**
- `@objc func sheet` もファイル中に存在しません。

Capacitor のブリッジは `pluginMethods` にない名前を **reject** します。なので
`shMake()` は必ず `['catch']` に落ち、`t('wr.nobridge')`
「ファイルの保存先に届いていません。」を出して終わります。**PDF はどこにも書かれません。**

`www/sheet.js:1073` のコメント自身がそう書いてありました ——
「There is no Swift behind `sheet` yet -- LinguaShare.swift has keep/kept/write/
pickPhoto/audio and not this」。書き出しは **一度も動いたことがありません。**

直し方: `LinguaShare.swift` に `sheet` を足す。`keep()` と同じ Documents の下
（`FileManager.documentDirectory`）へ。`assets-check` が Sources ビルドフェーズを
見るので、新しい `.swift` を足すのではなく **既存の `LinguaShare.swift` に生やす**のが
安全です（＝ `project.pbxproj` を触らずに済む）。

## 2-2 の原因（確定）

**自分で出した PDF を自分で読み戻せません。** そしてそれは
「戻す側が壊れている」のではなく、**書き出す側が写真を持っていない** からです。

- `shSheet()`（`www/sheet.js:240` 付近）が PDF に入れる画像は名前の絵だけで、
  `/ColorSpace /DeviceGray /BitsPerComponent 8` の **生ストリーム**です。
  `/DCTDecode` は一つもありません。
- 読む側 `shPdfJpeg()`（`:345`）は `/DCTDecode` を探します → **null**。
- `shPdfWhy()`（`:370`）は `/DCTDecode` なし・`/Subtype /Image` あり → `'packed'`。
- `shTakeFile()`（`:1198`）は `jpg` が null なので `why==='drawn'` でもないため
  `t('wr.pdf.no')`「この PDF の中の絵は取り出せません。」で refuse。

**人には何が見えるか:** 黙ってはいません。`shFail()` が `s.why` に入れ、
`shInHTML()` が `<div class="note">` で出します。ただし出る文は
「この PDF の中の絵は取り出せません」で、**本当の理由（これは紙に印刷して手で書いて
スキャンしたものではない）を言っていません。**

なお本来の道 —— 印刷 → 手で書く → スキャン → その PDF を戻す —— なら
スキャナは `/DCTDecode` を返すので読めます。オーナーが 2-1 で紙を一枚も
手にできていない以上、**戻す側で試せたのは「アプリが出した PDF そのもの」だけ**
だったはずで、それがこの refuse に当たります。

## 検査が守っていること／守っていないこと

`tools/sheet-check.mjs`（451行）:
- **守っている:** 汚い写真からの読み取り、名前の帯、リングの巻き、何も描いていない箱、
  帯が破れた紙、シートでない写真の拒否、`shTakeIn()` が letters を足すこと。
- **守っていない（今回の二つ、両方）:**
  1. `shMake()` を一度も呼びません。native の口があるかを誰も見ていません。
  2. **`shSheet()` が出した PDF のバイト列を一度も使いません。**
     チェックは `build` の中で canvas に**自分でページを描き**、PNG の dataURL を
     `shTakeFile()` に渡しています（`:52-175`, `:177`）。
     つまり「自分の出した紙を自分で読み戻せる」は誰も確かめていません。

`tools/shape-check.mjs`（239行）: 箱の中の形だけ。PDF には触れません。


---

# 直したこと

## 2-1 — 書き出したシートが Documents に落ちる

`ios/App/App/LinguaShare.swift` に `sheet` を足しました。`pluginMethods` に一行、
`sheets()` と `@objc func sheet(_:)`。**新しい `.swift` は作っていません** ので
`project.pbxproj` は触っていません（`npm run assets` 緑、swift 20 files すべて
Sources phase に在り）。

`www/sheet.js` は一行も変えていません。呼び方は最初から正しかったからです
（`Capacitor.nativePromise`。`registerPlugin` も `Capacitor.Plugins` も使って
いません）。

**どう渡すか:** `backup.js` の `keep()` と同じ Documents です。
`Documents/Sheets/<name>.pdf`。`UIFileSharingEnabled` と
`LSSupportsOpeningDocumentsInPlace` は `Info.plist` に既にあるので Files アプリ
から見えます。**二つ目の書き出し方は作っていません。**

**一つだけ `keep()` と違うところ、そしてこれは直しの一部です:**
`shFileName()` は言語 id を持たないので毎回同じ名前になります。素直に書くと、
Files で開いてペンで書いた一枚が次の書き出しで消えます。**上書きせず**
`<name> 2.pdf` `<name> 3.pdf` と続けます。何も消しません。

**決定として上げるもの（実装はしていません）:** 共有シート（`UIActivityView
Controller`）にするか、Documents のままにするか。今は Documents だけで、
これは `backup.js` と同じ道です。押した人が「どこに行ったのか」を知るには
Files を開く必要があります。トーストは `wr.out.ok`「書き出しました」のままで、
どこに行ったかは書いていません（規則：説明文を書かない）。**変えるなら決定です。**

## 2-2 — 戻した PDF

**答え: いいえ、自分の出した PDF は自分で読み戻せません。**

実測（`shSheet(['a','b','c'])`、64,649 バイト）:

```
why: 'packed'   jpeg: false   head: '%PDF-1.4'
→ 「この PDF の中の絵は取り出せません。」で拒否、got: null
```

`shSheet()` が PDF に入れる画像は箱の上の名前だけで、
`/ColorSpace /DeviceGray /BitsPerComponent 8` の**フィルタなし生ストリーム**です。
`/DCTDecode` は一つもありません。

**そして、拒否するのは正しいです。** 白紙のシートには何も書かれておらず、
読めば二十枠すべて空 = `wr.empty.all` になるだけです。本来の道
（印刷 → 手で書く → スキャン → 戻す）ならスキャナが `/DCTDecode` を返すので
読めます。**直すところは「拒否」ではなく「言っている理由」でした。**

**人には何が見えていたか:** 黙ってはいません。`shFail()` → `s.why` →
`shInHTML()` の `<div class="note">`。ただし出ていた文
「この PDF の中の絵は取り出せません。」は**別のファイルについての文**でした。

`shPdfWhy()` の `packed` が「画像があって JPEG ではない」だったためです。
アプリ自身のシートは名前が画像なので `packed` になります。

**直し:** 写真でない画像と、開けない画像を分けるのは**フィルタ**です。スキャナが
作るページは必ずフィルタの後ろにいます（`/DCTDecode`、そうでなければ Flate・
JPX・CCITT）。`shSheet()` が書くのはフィルタなしの 8bit グレーで、スキャナは
それを作りません。`shPdfWhy()` は画像の辞書だけを見てフィルタを問うようになり、
アプリ自身のシートは `drawn` =「この PDF の中に写真がありません。」になります。

`shPdfImageAt()` が足りた関数一つ（`/Subtype /Image` と `/Subtype/Image` の
両方。PDF はどちらも書きます）。**保存されるものは何も変わりません。拒否する
かどうかも変わりません。変わるのは人が読む一文だけです。**

**そのあと入れたもの:** 画面の上でペンで書いた PDF のレンダラ。下 § 3。

# どの検査が守るか

## `tools/sheet-check.mjs` — 紙を本物の PDF から出すようにしました

**これが 2-2 の本体です。** 検査は自分でページを canvas に描いていました
（旧 `build` の 52-175 行）。`shBoxAt`/`shMarks`/`shCellAt` は書き手と同じもの
なので幾何は必ず一致し、**`shSheet()` の出すバイト列には一度も触れていません
でした。** 書き手のとなりに描いたページは書き手の写しで、写しは必ず一致します
（CLAUDE.md 規則10「a check that recomputes the thing under test is a copy of
it」と同じ形）。

今は本物の PDF を組み立て、**その xref を読み、ページを見つけ、内容ストリームを
トークンにして canvas に流します。** `shPageOps()` が出す演算子
（`re f` `re S` `g` `G` `w` `q`/`Q`/`cm` `Do` `BT…Tj…ET`）だけを解釈し、
**知らない演算子が来たら黙って無視せず失敗します** ── 一部が抜けたページで
描かれるのは、この検査が拒むための当の「静かな間違った答え」だからです。

そのうえで既存の二十項目（汚い写真、帯、リングの巻き、空の箱、破れた帯、
シートでない写真、`shTakeIn()`）が全部そのまま本物のバイト列を通ります。

足した主張:

1. **箱の数だけ名前の絵が置かれていること**（バイト列にしか言えない）。名前が
   一つ欠けたページは印刷も写真も完璧に通ります。
2. **どの種類の PDF が来たかをアプリが分かること** ── 自分のシートは `drawn`、
   スキャンは `photo`、フィルタの後ろのページは `packed`、PDF でないものは
   `not-pdf`。
3. **自分のシートをそのまま返すと、本当の理由で断られること。**

## 赤を見たか — 四回、全部見ました

| 戻したバグ | 出た赤 |
|---|---|
| `shPageOps()` の帯を1セルずらす | 帯が読めず紙は拒否、**10項目が赤**。旧チェックでは緑のままだったところ |
| 箱3の名前の絵を置かない | `the file draws a name over every box … 19 names, 20 boxes` |
| `shPdfWhy()` を古い規則（画像があれば `packed`）に戻す | `its own sheet is` **`packed`** と、断り文が「絵は取り出せません」に |
| （上と同じ run） | `handed straight back … "The picture inside this PDF cannot be taken out."` |

## 守っていないと分かっていること

- **`shMake()` を通る検査はまだありません。** native の口があるかは Linux では
  問えません。`sharePlug()` はブラウザで null を返します。
- `tools/shape-check.mjs`（239行）は箱の中の形だけ。PDF には触れません。緑。

# 走らせた検査

`npm run sheet` `npm run shape` `npm run es5` `npm run dead` `npm run act`
`npm run i18n` `npm run assets` ── **全部緑。** `npm test` は回していません
（リーダーのもの）。`origin/master` は `git merge` 済み（Already up to date）。

# リーダーに入れてほしい CSS

**ありません。** `www/index.html` は一文字も触っていません。新しいセレクタも
`style=` も足していません。

# 実機でしか確かめられないもの（名指し）

Linux では Swift を一行も走らせられません。**`DEVICE CONFIRMED` は未です。**

1. **`LinguaShare.swift` がコンパイルすること。** Swift のコンパイラがこの環境に
   ありません。`sheet` は `keep()` と同じ形で書いていますが、目で見ただけです。
2. **`Capacitor.nativePromise('LinguaShare','sheet',…)` が resolve すること。**
   ── 書き出しを押して「書き出しました」が出るか。
3. **Files アプリの「このiPhone内」→ Lingua → Sheets に PDF が現れること。**
4. **二回押したとき二枚目が `<name> 2.pdf` になり、一枚目が消えないこと。**
   ここは `docs/DATA_SAFETY.md` に直接触るので、**実機で必ず見てください。**
5. **その PDF が A4 に正しく印刷されること** ── 四隅のマーク、二十の箱、箱の上の
   名前、下の帯。バイト列は検査が通していますが、紙は誰も見ていません。
6. **印刷 → 手で書く → iOS のメモかスキャナで PDF にする → 戻す** の一周。
   ここがこの章の全部で、Linux にはカメラも紙もありません。
7. **アプリ自身のシートをそのまま戻したとき**「この PDF の中に写真がありません。」
   が出ること（検査は緑ですが、画面で読めるかは別）。

# 触ったファイル

```
ios/App/App/LinguaShare.swift   +47   sheet / sheets / pluginMethods 一行
www/sheet.js                    +38-6 shPdfWhy の分類、shPdfImageAt
tools/sheet-check.mjs           +200  PDF 再生器、guard、主張3つ
docs/CHANGELOG.md               +37
docs/BACKLOG.md                 +23
docs/reports/sheet3-2026-08-27.md
```

`www/index.html` `shell.js` `onboard.js` `sns.js` `keyboard.js` `share.js`
`settings.js` `store.js` `post.js` `import.js` `wordsheet.js` `glyph.js`
`home.js` `net.js` `supabase/` `ios/App/App/LinguaStore.swift`
`ios/App/App/MainViewController.swift` `ios/App/LinguaKeyboard/` ── **一つも
触っていません。** `www/act-map.js` も `www/i18n/*.js` も変更なし（新しい文字列
も新しいボタンも足していません）。

CODE CONFIRMED / **DEVICE 未** / OWNER 未

---

# 3. 画面の上で書いた PDF を電話に描かせる

「pdfkitのレンダラやろう」OWNER 2026-08-27。BACKLOG に上げたものが決定に
なったので、そのまま入れました（BACKLOG の項目は消しました ── 直したら規則を
消す、と同じ扱い）。

## 何が読めるようになるか

写真の入っていない PDF は二種類あって、どちらも同じ問題です:

- **画面の上でペンで書いたもの** ── iPad の Markup、GoodNotes。インクがページに
  「描かれて」いて、ページの写真の画素ではない
- **ページがこのファイルのほどけないフィルタの後ろにいるもの**（Flate、JPX、
  CCITT）。PNG を吐くスキャナがこれです

どちらもレンダラが要り、`www/sheet.js` はレンダラではなく、電話は持っています。

## 形

`ios/App/App/LinguaShare.swift` に `renderPdf`。一ページ目を `SH_LOOK`（2200px）の
JPEG にして base64 で返します。**大きさの数はこれまで通り `www/sheet.js` の
一箇所**で、Swift 側は渡された数を使います。

`www/sheet.js` に `shPdfDraw()`。**`shPdfJpeg()` 以外で PDF が絵になる唯一の
場所**で、`shLook()` から先は一文字も変わりません ── 同じ閾値、同じ四隅のマーク、
同じ帯。二本目の道はページが二通りに描かれて静かに食い違う道です。

**描けるかどうかはレンダラが答えることにしました。** `shPdfWhy()` は「どの種類の
ファイルが来たか」を言うもので、それは別の問いです。誰に試させるかをそれに
決めさせると、本当に知っているものの前に推測を置くことになります。なので
**JPEG が取れなかったものは全部渡します** ── PDF ですらないファイルも。
CoreGraphics がそれを断り、それが答えです。

電話がないとき（ブラウザ、このビルドより前のアプリ）は聞く相手がいないので、
**これまでの文がそのまま出ます。** 退行はありません。

## PDFKit ではなく CoreGraphics、これは好みではありません

`LinguaShare.swift` 自身が記録しています ── `import WidgetKit` をここに書いたら
`PHPickerViewController` がスコープから消えて **#84 が落ちました**（a82a633 から
緑だったピッカーで）。`CGPDFDocument` と `drawPDFPage` は UIKit のもので、
**このファイルの上にある import だけで足ります。** 新しい `.swift` も作って
いないので `project.pbxproj` は無傷です（`npm run assets` 緑、swift 20 files）。

`getDrawingTransform` を使うのは、**ページが回転を持つことがある**からです
（スキャナの PDF はよく持ちます）。九十度回った紙は四隅のマークが違う隅にあり、
**失敗せずに「シートでないもの」として読まれます。**

**一ページ目だけ**です。これは読む側がずっとそうしてきたことで
（`shPdfJpeg()` は一番大きい JPEG を一つ返し、`shPage()` は一ページ読む）、
二十一個目の名前は二枚目のシートであって、このシートの二ページ目ではありません。

## 新しく保存されるものはありません

送った PDF も返ってきた絵もどこにも書かれず、読み終わったら消えます。

## 検査 ── `tools/sheet-check.mjs` § 8

Swift は Linux で走りません。`backup-check` が `keep()` に引くのと同じ線で、
**ブリッジをスタブして、その手前まで全部**を通します:

1. **電話に渡されること** ── `LinguaShare.renderPdf` に 1 回、
   **そのファイル自身**の base64 で、**読む側が見る大きさ**（`SH_LOOK`）で
2. **返ってきた絵がスキャンと同じ読み側を通ること** ── 20 の名前、うち 3 枠に
   インク。§1 が撮った写真をそのまま返させます（注釈の入ったページとは、
   まさにそれ ── 同じ二十枠に、三つだけインクが乗ったもの）
3. **断られたら画面が文になること** ── 永久に変わらない画面ではなく

## 赤を三回見ました

| 戻したバグ | 出た赤 |
|---|---|
| 電話に聞かない（この変更の前の状態） | `0 call to undefined.undefined` と `0 names` |
| 渡す大きさを間違える（2200 → 400） | `a page with no photograph … handed to the phone to draw` が赤 |
| 断りを飲み込む（`catch` を空に） | 画面が永久に変わらず **timeout** で落ちる |

## 走らせた検査

`npm run sheet` `npm run shape` `npm run es5` `npm run dead` `npm run act`
`npm run i18n` `npm run assets` ── **全部緑。** 新しい文字列も新しいボタンも
足していないので `i18n` と `act-map.js` は無変更。CSS もなし。

## 実機でしか確かめられないもの（§ 2 の 1〜7 に足して）

8. **`renderPdf` がコンパイルすること。** `CGPDFDocument` / `getDrawingTransform` /
   `drawPDFPage` を使っています。Swift のコンパイラがこの環境にありません
9. **iPad の Markup でシートに書いて戻す一周** ── これがこの追加の全部です
10. **回転を持つ PDF**（スキャナの縦横が逆のもの）が正しい向きで読まれること。
    `getDrawingTransform` に任せていますが、目で見ていません
11. **大きい PDF がブリッジを通ること。** スキャンした PDF を base64 で
    WKWebView 越しに渡します（`keep` が言語まるごと、`pickPhoto` が写真を
    渡すのと同じ道）。何 MB まで実用かは測っていません
12. **フィルタの後ろのページ**（PNG を吐くスキャナ）が読めること

CODE CONFIRMED / **DEVICE 未** / OWNER 未
