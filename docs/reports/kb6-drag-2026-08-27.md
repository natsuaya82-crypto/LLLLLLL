# `claude/kb6` ②ーー 長押しでキーを動かす。**調べて、落とす先だけ入れました**

2026-08-27。**`CODE CONFIRMED`（下の数は全部このセッションで測ったものです）。**
**`DEVICE CONFIRMED`: 未**（実機は Linux では出せません）。

> 「くっついてるキーがある時は動かすのは無理だけど例えば長押しでキー自体を
> 移動させれるとか？」 **OWNER 2026-08-27**

前半は決定として入っています（後述）。後半は「〜とか？」なので案として調べ、
**もう入っていた**ことと、**そこに二つ穴がある**ことが分かりました。
オーナーは**その両方**に答えました:

> 「満杯だと追加できないから」 **OWNER 2026-08-27**
> 「長押しの時は動くよ？ iPhoneのホーム画面と同じ ウェジットも2*2とかあるけど
> その分みんな動くでしょ？それと同じ」 **OWNER 2026-08-27**

- **満杯の行に落とせてしまう件 ── 直しました**（§②）
- **結合キーが黙ってほどける件 ── 決まりましたが、入れられていません**（§③）。
  この枝に結合キー（`kbVJoin` / `kbVFix`）が無く、`claude/kb5` にしかないためです

この報告は最初「調べただけ、実装していません」として書いたもので、答えが来た分を
書き足してあります。**測った数は一つも書き換えていません。**

---

## 先に一行

**もう入っています。** 長押しでキーを掴んで動かすのは、`www/keyboard.js` に
今日の `master` の時点で実装済みで、行をまたいで動かせます。

だから訊かれているのは「作れるか」ではなく **「今こうなっているが、これでいいか」**
です。以下は全部それについてです。

---

## 何が既にあるか

`master` の `www/keyboard.js`:

| | |
|---|---|
| `kbDown()` | 指が触れた瞬間。**380ms** のタイマーを置く |
| `kbLift()` | 380ms 経ったら発火。キーが浮き、`kbWob=true`（ホーム画面と同じ、全キーが揺れる状態） |
| `kbDragTo()` | 指の下のキーを `elementFromPoint` で探し、`insertBefore` で**行をまたいで**入れ替える |
| `kbUp()` | 離した瞬間。`kbReadRows()` が画面から並びを読み戻し、`saveKb()` |

コメントも既にそう言っています ──
「長押ししたら右上に➖出てきて消える。iPhoneのホーム画面と同じ挙動」
「長押しして持っていきたいのに動かない」（これは直された跡です）。

### 測り方

`tools/kb-check.mjs` と同じ立ち上げ方で本物のアプリを起動し、**本物の
`kbDown` / `kbLift` / `kbDragTo` / `kbUp` を呼びました。** 指が無いので
`document.elementFromPoint` だけ、「指の下にあるのは何か」という**一問のあいだ
だけ**差し替えています。それ以外は全部アプリのコードです。

---

## ① 今の画面の習わしに乗るのか ── **乗っています。既に同居しています**

心配されている「掴んで動かすのは、行の番号・列の文字で選ぶのと別の入り方では
ないか」は、**もう解かれています。**

キーの `[data-do]`（＝タップで選ぶ）を測りました:

| | キーに `data-do` があるか |
|---|---|
| 普通のとき | **ある**（`kbTapKey` ＝ 押して選ぶ） |
| 揺れているとき（長押し後） | **ない** |

`kbKeyHTML` が `(kbWob? '' : DO('kbTapKey', [ri, ki]))` と書いているからです。
**揺れているあいだ、タップは存在しません。** さらに `kbUp()` は動かした場合に
`preventDefault()` を呼ぶので、**運んだ指がそのままキーの画面を開くこともありません。**

選択（`KBH`）と運搬はぶつかっていません。二つの入り方が一つの画面に同居する形は、
既に決着済みです。

---

## ② 動かした先が埋まっていたら ── **割り込んで、行が十を超えていました。直しました**

**②の案とは無関係に、この日の `master` にあったバグです。** 以下は見つけたときの
測定で、直したあとの話は §「決まりました」から。

qwerty パターンの盤（各行ちょうど十キー）で、行1のキーを行0へ運びました。
数字は**半列**（十キー = 20）です:

```
運ぶ前   [20, 20, 20, 20, 20]     全部ちょうど十キー
運んだ後 [22, 18, 20, 20, 20]     行0が **十一キー**
```

`kbDragTo()` は `row.insertBefore()` するだけで、**行の幅を一度も訊きません。**

**訊く関数は在ります。** `kbRoomIn()` ── 手で足すとき（`kbCellAdd`）は
これを通っていて、満杯の行には入りません。運搬だけがそれを通っていません。

規則19 は「**十が電話の数**」と言っています（一番細い iPhone が320なので
十キーで32ずつ、十一なら29）。運搬はその天井の**外**にいます。同じことは
`kb5` 側でも測りました ── 同じ数字が出ます。**`kb5` が入れたものではありません。**

### 決まりました ── **A（拒む）。入れました**

> 「満杯だと追加できないから」 **OWNER 2026-08-27**

出した四択のうち **A** です。**理由がそのまま決定の言葉になっています** ──
手で足すとき（`kbCellAdd`）は `kbRoomIn()` を通って満杯の行に入らないので、
運搬も同じ扱いにする、ということです。

| | 何が起きるか | |
|---|---|---|
| **A 拒む** | 満杯の行には落ちない。指を離すと元の場所 | **これになりました** |
| **B 入れ替える** | 落ちた先のキーと場所を交換する | 採られませんでした |
| **C 押しのける** | あふれた分を次の行へ送る | 触っていない行が動くので不採用 |
| **D 今のまま** | 十一キーの行ができる | 規則19が禁じている数そのもの |

**私は B を推していました。採られたのは A です。** 私が挙げた A の唯一の心配
（「動かない」と読まれる）は残りますが、手で足すときと同じ振る舞いになるので、
この画面の中では一貫します。**演出は何も足していません**（アプリ内に説明は禁止）。

#### 押し返されたことは目で分かるか ── **実測しました。分かります**

リーダーに「分からないなら測って報告」と言われたので、断られた運搬と入った運搬を
並べて測りました。`transform` と、親（行）が変わったかどうか:

| | 運んでいる間 | 指を離すと |
|---|---|---|
| **断られた** | `transform: translate(50px, 90px)` ── **指について来る**。ただし行は変わらない | transform が消えて**元の場所へ戻る** |
| **入った** | `transform` は無し、**親の行が変わっている** ── その場に**収まる** | そのまま |

つまり、入った運搬は指の下で**キーが行にはまって止まり**、断られた運搬は
**はまらずに指について来続け、離すと戻ります。** 二つは見た目が違います
── iPhone のホーム画面でアイコンを詰まった所へ持っていったときと同じ形で、
**何も付け足さずにそうなっています。**

#### 入れたもの

`kbDragTo()` が `kbRoomFor()`（`kbRoomIn()` と同じ規則）を訊きます。
**行をまたぐときだけ** ── 同じ行の中は幅が変わらないので、訊くとキーを二度数えて
満杯の行の並べ替えが凍ります（これも赤で確かめました）。

`kb-check` に六項目。**特に二つ「どの行も十以下」と「キーは元の場所に戻っている」
は分けてあります** ── 断るときにキーを**消す**バグを入れると、
**前者は緑のまま**（10, 9, 10, 10, 10）で、捕まえたのは後者だけでした。

---

## ③ 結合キー（`h` と `up` の対）を動かすと ── **黙って結合が外れます**

`kb5` の縦結合を読み込んだ木で測りました（`master` にはまだ `kbVJoin` が
無いので、そちらでは起きません）。

結合直後の1行目・2行目:

```
行0   lt::1:h2  lt::1  lt::1 ...       ← 上のキーが h=2（二行ぶんの高さ）
行1   gap::1:up lt::1  lt::1 ...       ← 下は「影」の gap、up=1
```

**上半分（背の高い方）を別の行へ運んだ後:**

```
行0   lt::1 lt::1 ...                  ← h2 が **消えている**
行1   gap::1 lt::1 ...                 ← up も **消えている**
```

`h2` も `up` も残っていません。**下半分は一緒に動きません。**

**外しているのは `kbVFix()` です。** `saveKb()` から走り、対が縦に揃って
いなければ `delete k.h` / `delete k.up` します ── そういう設計です（「Merges,
kept honest, in one place」）。運搬は `kbReadRows()` → `saveKb()` で終わるので、
**運んだ瞬間に `kbVFix()` が「この対は壊れている」と判断して、直す代わりに
ほどきます。**

これは `kbVFix()` のバグではありません。**運搬が `kbVFix()` に「対が動いた」と
言う手段を持っていない**だけです。何も throw せず、画面は正しく描かれ、
**結合だけが無かったことになります。**

### 決まりました ── **一緒に動く。ただしこの枝では入れられません**

> 「長押しの時は動くよ？ iPhoneのホーム画面と同じ ウェジットも2*2とかあるけど
> その分みんな動くでしょ？それと同じ」 **OWNER 2026-08-27**

塊のまま動き、周りがその分だけ場所を空ける。落ちる先は**二行ぶん**要り、
どちらかの行に空きが無ければ落ちない ── §②で入れた門を**二行ぶん訊くだけ**。
一番下の行には落ちない。

**`kbVFix()` は一行も変えません。** 上の測定を「運んだ瞬間に `kbVFix()` が
ほどく」と書きましたが、**ほどくのがあの関数の仕事**で、対を壊しているのは
運搬が下半分を置き去りにしていることです。運搬が上下を一緒に動かせば、
`kbVFix()` が見たときには対は揃っていて、ほどく理由がありません。例外を足すのは、
一箇所で持っている規則に、それを知らない側から穴を開ける形になります。

**入れられなかった理由 ── この枝に結合キーが無いからです。**

```
origin/master              kbVFix / kbVJoin / kbTall / kbShadow → 無し
claude/kb6（master から）  同上 → 無し
origin/claude/kb5          有り
```

上の測定は `git worktree` で `claude/kb5` を**読み取り専用**に立てて出したもの
です（merge していません）。**同じ手で検査は書けません** ── 検査はこの枝の
コードに対して走るので、`kbVJoin()` が無ければ対を作ることすらできず、
`kbTall()` を呼ぶコードを書けば `dead-check` が「名前が何でもない」で赤に
なります（規則5）。

**`claude/kb5` が master に入った日に、書けます。**

---

## ④ 巻き戻しに乗るか ── **乗ります**

`kbReadRows()` が `saveKb()` で終わり、`saveKb()` が `kbNoted()` を呼ぶので、
運搬は一手として記録されます。測りました:

```
運ぶ前   [20, 20, 20, 20, 20]
運んだ後 [22, 18, 20, 20, 20]
巻き戻し [20, 20, 20, 20, 20]     ← 幅まで完全に元通り
```

**上の②③をどう決めても、取り消しは効きます。** これは既にある財産です。

---

## ⑤ `press` は何を検査できて、何ができないか ── **正直に**

**`press` はこれを一度も押していません。押せません。**

`press` は `[data-do]` を持つ要素に `click()` を投げる walk です。運搬は
`touchstart` / `touchmove` / `touchend` の listener と `setTimeout` で出来ていて、
**`[data-do]` を一つも持ちません。** だから:

| | |
|---|---|
| `press` が押さえているもの | タップで選ぶ側（`kbTapKey`）が throw しないこと、画面が空にならないこと |
| `press` が押さえていないもの | **長押しも、運搬も、落とす先も、幅も、結合も、一つも** |

**だから `kb-check` が持ちます。** 満杯の行に落とさない決定が来たので、
本物の `kbDown` / `kbLift` / `kbDragTo` / `kbUp` を、この報告の数字を出したのと
同じやり方で呼びます ── `elementFromPoint` だけを一問のあいだ差し替え、
それ以外は全部アプリのコード。六項目あります。**`press` は今も一度も押しません**
ので、運搬について言えることは全部この六項目が言っていることです。

**実機でしか見えないもの:** 380ms が長押しとして自然か、指の下でキーが
ついてくる感じ、`elementFromPoint` が実際の指の接触面で何を返すか。
**この三つは Linux では出せません。**

---

## まとめ

1. **満杯の行に落としたらどうなるか** ── **決まって、入りました。A（拒む）。**
   「満杯だと追加できないから」 OWNER 2026-08-27。
2. **結合キーを掴んだらどうなるか** ── **決まりました。「一緒に動く」。**
   「長押しの時は動くよ？ iPhoneのホーム画面と同じ」 OWNER 2026-08-27。
   **入れられていません** ── この枝に結合キーが無いので（§③）。

**2 は `claude/kb5` が master に入るまで書けません。** 仕様は §③ に全部
書いてあります ── 塊のまま動く、落ちる先は二行ぶん、`kbVFix()` は変えない。
`docs/BACKLOG.md` にも同じことが入っています。

---

# 付録 ── ③の検索。**入りました**

> 「名前と音どっちも調べれる。」 **OWNER 2026-08-27**

この付録はもともと「入れていない理由と、要る差分」でした。差分はそのまま
通り、**要ると書いた三つの file をリーダーが開けてくれたので、入っています。**
古い本文は残していません ── 読まれるので。

## 入ったもの

| file | 何を |
|---|---|
| `www/sound.js` | `ltQ` / `ltQList()` / `ltPickList()` / `ltPaint()` / `ltSetQ()`、`ltViewRow()` に `.search` の箱、`vLtset` の一覧を `#lt-list` へ |
| `www/keyboard.js` | `kbLtGrid` も `ltPickList()` と `#lt-list` へ |
| `www/shell.js` | `viewReset()` に `ltQ='';` の**一行だけ** |
| `www/act-map.js` | `actIn('ltSetQ', ltSetQ);` の**一行だけ** |

`www/index.html` は触っていません ── `.search` は既にあり、**新しい CSS は
一行も要りませんでした**（これは事前の見立てどおりでした）。

## 決まっていなかった二つ

- **何で引くか → 名前と音の両方。** `ltCodes()` を訊きます。あれは既に
  「この文字が答える名前の全部」を持っていて、font もそれで組まれているので、
  `ab` と `snd` を読み直すと四つ目の写しになります。
- **箱を常に出すか → 常に出す**（リーダーの決定）。このアプリの検索はどこも
  常に出ていて、条件つきで現れる箱は一つもありません。

## 検査で言えること ── 実測

**「音でも引ける」を、名前だけの検索と区別できる形で測っています。**
このフィクスチャのアルファベットは a〜z なので、ほとんどの文字は名前と音が
同じ文字列です。**そこで、音がどの文字の名前にも含まれない文字を探します** ──
`g` が読む **`ɡ`**（U+0261、script g）。名前は全部 ASCII なので、
**名前だけを引く検索はこれに何も答えられません。**

```
searching that SOUND finds it (ɡ -> g)
```

名前だけ引くように戻すと、この行が `(ɡ -> )` になって赤になります。

九項目あります: 箱が両画面にあること / 名前で狭まること / 音がどの名前にも
無い文字が居ること / **その音で引くとその文字が出ること** / 何も答えない
検索で一覧が空になること（全部出るのではなく）/ 消すと全部戻ること /
**打っている欄が生き残ること** / 一覧が実際に描き直されること（27 → 1）/
**狭まった一覧はドラッグで並べ替えられないこと**。

赤は四通り見ました ── 名前だけ引く / `ltSetQ` が `render()` を呼ぶ /
検索中もドラッグできる / 一覧が検索を訊かない。

## 数

`buttons pressed` は **11698 のまま動いていません。** 箱は `data-in` であって
`data-do` ではないので、押されるボタンではありません。動いたのは `act` の
**typed 38/38 → 39/39** で、それが `ltSetQ` です。
