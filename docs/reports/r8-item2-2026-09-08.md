# 2 番（`SET.done`）── サーバーだけでは答えられない一つが残ります

枝 `claude/r8-server` / 2026-09-08 / リーダーの指示 2 番について

## 指示

> オンボーディングが済んだか（`SET.done`）── サーバーの `profile` 行の有無が
> 答え。起動時：セッションがあり `profile` 行があれば済み、無ければ歩き
> （扉の前）。`SET.done` を書く所・読む所を消し、`SET_PHONE` から外す。
> **歩き途中の状態は「セッションが無い」で表せる**（扉が最後なので、扉の前に
> セッションは無い）── それで足りることを acct-check／open-check で言う。

**「それで足りるか」を確かめました。足りません。** コードを読んで辿った結果で、
押して測ってはいません（下の「測っていないこと」）。

## `SET.done` は一つの旗で二つの質問に答えています

**(a) このアカウントは歩きを通ったか。** サーバーに答えがあります ── `profile`
行の有無で、`obIn()`（`www/onboard.js:933`）は既にそれを訊いています。
読んでいるのは `langOwned()`（`core.js:1160`）、`makeNeed()`
（`onboard.js:1107`）、`post.js:1886`、`appIs()` の 'app' の枝。
**ここは指示のとおりにできます。**

**(b) セッションの無いこの端末は、どの画面で開くか。** ここに答えはありません。
**セッションが無いのだから、訊く相手がいません。**

## (b) に書かれたオーナー決定が二つあります

**OWNER 2026-08-26**（`www/shell.js` § appIs に全文）
> 「ログアウトしたら普通にログイン画面だけ出せばいいやろ。それ以外は表示させるな。」

**OWNER 2026-09-03**（`www/settings.js:714` に全文）
> 「アカウント削除した後オンボーディングから始まるのはなぜ？」

どちらも「セッションが無く、かつこの端末は歩きを通っている」ときは**扉だけ**を
出せ、という決定です。`SET.done` はいま、その一文のためだけに
`wipeHere()`（`settings.js:728`）で立てられています。

**「セッションが無い」では、この二つと「箱から出したての端末」が同じ状態に
なります。** 指示どおりにすると、ログアウトした人・アカウントを消した人は、
起動のたびに**文字を描く画面**から始まります（`ob.step` はメモリなので、
起動すれば 0）。オーナーが二度「やめろ」と言った画面です。

## 端末にもサーバーにも答えが無いことの証明（三行）

- ログアウト後：`lingua.sess` は消えます（`netOut()` → `netSave()`）。訊けません。
- **アカウント削除後：サーバーにその行はもうありません**（`account_delete`）。
  `lsWipeAcct()` がこの端末のその人の鍵も全部持っていきます。それでもオーナーは
  「扉を出せ」と言っています。**消えたアカウントについて、サーバーに残る答えは
  定義上ありません。**
- 「扉を先頭にする」は却下済みです ──「オンボーディング→最後にログイン」
  「そんなの俺頼んでねえぞ」。

## 三つの選択肢（オーナー／リーダーのもの）

**(A) 二つに割る。** (a) はサーバー（`profile` 行）へ。(b) だけを
`SET_PHONE` に残す ── ただし名前を変えて、何の質問かが読んで分かるように
（例 `SET.walked`「この端末は歩きを済ませた」）。テーマや表示言語と同じ
「この端末の設え」の側で、規則 22 が許している欄です。**リーダーの推し。**
`SET.done` を読む五か所のうち四か所はサーバーの答えになり、残るのは
`appIs()` の一行だけになります。

**(B) 指示どおり全部消す。** ログアウトとアカウント削除のあと、起動すると
文字を描く画面から始まります。二つのオーナー決定を上書きすることになるので、
**オーナーが言い直さない限りできません。**

**(C) 何もしない。** `SET.done` はそのまま。(a) の四か所がサーバーの答えに
ならないままです。

## 測っていないこと

**コードを読んだだけで、実機でも headless でも押していません。**
「ログアウトして起動しなおすと文字を描く画面が出る」は、`ob.step` がメモリで
あること（`onboard.js:69`）と `appIs()` の三行から辿った結論で、**そう見た
わけではありません。**指示どおりに直してから `open-check` で見れば、一回で
分かります。

## この枝がしたこと

**コードは一行も変えていません。** 2 番は保留、3 番へ進みました。
