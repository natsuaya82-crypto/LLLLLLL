# kb4 ── シートは触って作る

枝 `claude/kb4`（`master` c2fe552 から）。2コミット。

> 「下のキーを動かして入れるのやめない？ a1とかタップしたらキーを追加とか、
> a1a2触ってキーをくっつけるみたいにしたほうがいいんじゃね」
> 「タップしたらそのキーが選ばれて上のゴミ箱ボタンとかくっつけるボタンとか
> 押してその作業がされるようにしようよ」 **OWNER 2026-08-27**

## 何が変わったか

- 空きマスを押す → そこにキーが1つ入る
- キーを押す → 選ばれる。もう一度で外れる
- 選ばれているキーの隣を押す → 二つが一つになる（**左のキーが残る**。文字も
  フリックも押したときの動きも。取るのは右の**幅だけ**）
- 盤の上のボタンが、選ばれているものに効く ── 行・列・キーで**一つの習慣**

幅のタイル（下に置いてあった1・2・3）と、それを指で運ぶ仕掛けは消えました。
**キーの長押し移動は残っています** ── `kbDragMount()` が繋いでいた二つのうち、
外したのはタイルの側だけです。`www/glyph.js` からの呼びも触っていません。

**縦（a1+a2）は入れていません。** `docs/reports/kb4-vjoin-spec-2026-08-27.md` に
仕様だけ書いて、許可待ちです。

## 触った file

| file | 何を |
|---|---|
| `www/keyboard.js` | 本体（−408/+…、正味で縮んでいます） |
| `tools/kb-check.mjs` | タイルの章を落として触る章を17項目 |
| `tools/fixture.mjs` | 「キーが選ばれている」面を1つ |
| `www/act-map.js` | −`kbSetNew` −`kbPick`、+`kbCellAdd` `kbTapKey` `kbJoinSel` `kbOpenSel` |
| `www/i18n/*.js` (10) | 自分の鍵だけ |
| `docs/CHANGELOG.md` `docs/keyboard.md` | 記録 |
| `docs/reports/` | この2枚 |

**担当外を1行:** `www/shell.js` の `viewReset()` から `kbNew1=0;` を落としました。
`kbNew1` という変数を消したことの直接の結果で、残すと `dead-check` が
「宣言の無い名前に代入している」で赤になります。`KBH` の説明も
「行か列」→「行、列、キー」に直しました。**それ以外は触っていません。**

## データ

**動きません。** 保存されているキーボードは一つも変わりません。移行なし、削除
なし、新しく保存するものもなし。プランにも触れていません。

## 検査

`es5` `dead` `act` `kb` `conv` `i18n` `box` `sides` ── **緑**。

**赤を三通り見てから信じました:** 空きマスをただの `span` に戻す／くっつけても
幅を足さない／くっつけたら右の文字が残る。三つめは最初**空振り**で、板が
パターンから作られるとどちらのキーも文字が空だったからです ── `v='aa'`/`'bb'` を
入れて主張が本当に何かを言うようにしてから、赤を見ました。

`press` の数は**全部測って帰属をつけました**（`CHANGELOG` に表）。
11468 → 11633（+165）、名前 236 → 238、画面 707 → 710。推測はありません。

---

# リーダーへ ── 私の担当外で、直す手が要るもの四件

## 1. `press` がこの枝で赤い。直す場所は `www/index.html`

タイルが消えたので、CSS 四つが着る者を失いました:

```
www/index.html:1888  .kbnew{...}
www/index.html:1889  .kbnewr{...}
www/index.html:1894  .kbnewl{...}
www/index.html:1901  .kbnewt{...}
www/index.html:1904  .kbnewt.on{...}
```

上の1884-1887行と1890-1900行のコメントも、この三つのタイルの話なので一緒に。

**同じコミットで三つ一緒でないと壊れます:**

- `www/index.html` 1884〜1904行を削除
- `tools/box-baseline.txt` の37・38行（`.kbnewt | border` と `| border-radius`）
  ── ルール18「何にも当たらなくなった行は消す」
- `tools/press.mjs` 150行のコメントと164行の `' kbnewt '` ── 44pt の免除。
  免除される者が居なくなります

**`www/index.html` は `claude/sheet3` `claude/cowork-migration-review-wfx1ra`
`claude/detailed-tasks-execution-ak61z2` `claude/code-review-refactor-seaaei`
の四本が触っています。** 私は触っていません。誰が入れるかはリーダーのもの。

なお `www/keyboard.js:2179` の `openForm('kbnew', ...)` は**別物**です ──
新しいキーボードを作るフォームの id で、`.kbnew` という class とは関係ありません。
消さないでください。

## 2. 編集画面の道具バーが 320pt の端末で横に溢れる

272px の枠に 322px 入っていて、**ゴミ箱が 26px 画面の外**に出ます。
`sides-check` は 402pt で測るので見えません。`.kbtool` は `index.html` に
あるので私のものではありません。iPhone SE 1 の実機で最初に出ます。

## 3. 列を切った後、文字キーが幅 0.5 で残ることがある

390pt の端末で **14pt**。`press` はキーボードのキーを高さだけで測る（QWERTY が
十列だから、という免除）ので、これを見つけられません。
`docs/BACKLOG.md` に入れてあります。**決めるのはオーナー** ── 0.5 を許さない
（切るときに巻き込む）か、そのまま許すか。

## 4. 高さの上限、Swift の一行

`docs/reports/kb3-height2-2026-08-27.md` に書いた
`cap` から `view.safeAreaInsets.bottom` を引く件。`ios/` は `claude/plan2` の
ものなので入れていません。
