# claude/docs — 消した文と、それがどこに在ったか

**2026-09-01。**枝 `claude/docs`（`origin/master` から）。
触ったのは **`docs/**/*.md` と `CLAUDE.md` だけ**です。`www/` `tools/`
`supabase/` `ios/` は一行も触っていません。**`docs/CHANGELOG.md` も一行も
触っていません。**

`npm run i18n` — **緑**。鍵は触っていないので当然ですが、確かめました
（10言語・61画面・338レンダー・970鍵、すべて通過）。**遅い二十は回していません**
（`docs/SESSIONS.md`: セッションはゲートを回さない）。

---

## 1. いちばん大事な一つ

**リーダーが読んで、オーナーに間違った説明をしたのは、この二文です。**

| 在った所 | 文 |
|---|---|
| `docs/STATE.md` § 3、項目3 | 「**The language actually living on the server — not started**, and it is the rest of item 1. There is nowhere to put a slice yet: `language` holds a name, a licence and a date, and **the eleven slices are still `localStorage` only.** This is the next thing.」 |
| `CLAUDE.md` § Layout、`docs/STATE.md` の行 | 「what only looks built (**the timeline is on the phone**; the `post`/`follow`/`quote` tables are written and unused)」 |

**どちらも逆です。**同じ `docs/STATE.md` の十行上に「`language` と `slice` は
書かれて読まれている」と書いてあり、`netLangSync()` は `www/boot.js` から
起動時に走っています。**一つのファイルの中で、前の段落と後の段落が逆のことを
言っていました。**後ろのほうが番号付きの一覧で、読まれるのはそちらです。

両方消しました。

---

## 2. 消した文の一覧

「在った所 → 消した文 → いまそこに在る文」。順は枝のコミット順です。

### `CLAUDE.md`

| 消した文 | なぜ古いか |
|---|---|
| § Online「drafts are a flat key on the phone and a voice is a file in Documents, and **both of those are old code, not a decision**」 | どちらも直っています。`netDraftUp()` が `draft` 行を書き、`netUpVoice()` が `post-media` に上げて `body.vu` に道を書く |
| § Layout `docs/STATE.md` の行「the timeline is on the phone; the `post`/`follow`/`quote` tables are written and unused」 | **上の §1。**`post` も `follow` も使われている（`quote` だけがまだ） |
| § Layout `www/rec.js` の行「A file in Documents today, and that is the code being old ... **Not moved yet** — `docs/BACKLOG.md`」 | 移っています。`docs/BACKLOG.md` 自身が 2026-08-28 に「録音のところは直すところではなかった」と書いている |
| 冒頭「the timeline **is** on the server now — `post`, `react`, `follow`, `profile` and the notices RPC」 | 半分だけ本当。言語もサーバーです。表も古い（`draft` `language` `slice` が抜けている）ので、その場で数える一行に置き換えました |
| 規則6「**Eleven slices** — words, lines, name, script, letters, notes, phases, talk, sounds, keyboard, world — **live under** `lingua.<id>.<slice>`」 | 二つ古い。数は**十二**（`lang` と `gram2`）で、`lingua.*` は住む場所ではなく写しです。数は `SLICES` で数える、という書き方に変えました |

**足した文**（消しただけでは仕様が書かれないので）:
端末のものは三つだけであること／言語そのものもサーバーであること／
プランはアカウントのものであること（OWNER 2026-09-01）／キーボードは言語の一部
なのでサーバーであること。

### `docs/ARCHITECTURE.md`

| 消した文 | なぜ古いか |
|---|---|
| 「真実の在り処」の表: **the timeline → `lingua.posts`** | 本体はサーバーの `post` 行 |
| 同じ表: **the person's profile → `lingua.me`** | 本体はサーバーの `profile` 行 |
| 「**That was true and is not.** This paragraph said 「Nothing a person makes is on a server today. The timeline is `localStorage`. ...」」 | **古い文を引用して残す形。**`CLAUDE.md` が名指しで禁じています ──「歴史とかいいから消せよ」。**読まれるから** |
| 「**This paragraph said the wrong one was the copy, and it was written yesterday.** It said 「`localStorage` stays the truth …」」 | 同上 |
| 「Where data flows」の図 ── 端末 → Documents のバックアップ **しか無かった** | サーバーへの道が図に一本も無く、バックアップが唯一の行き先に見えた |

表には「what was written and not sent → `draft` 行」を足し、
`lingua.langs` は「端末が持っている写しの索引」であると書き足しました。

### `docs/STATE.md`

| 消した文 | なぜ古いか |
|---|---|
| § 3 項目3「The language actually living on the server — **not started** ... the eleven slices are still `localStorage` only」 | **上の §1** |
| § 3 項目1「**Anonymous sign-in — done.** The first launch signs in with no address and no password (`netAnon` in `net.js`, called from `boot.js`)」 | 匿名アカウントは無い ──「匿名アカウントはねえよ」。`netAnon()` は `www/net.js:268` のコメントに跡が残るだけ |
| § 3 項目2「`has_account()` in `schema.sql` is 'there is an account', anonymous and frozen included, and **guards the three `language` write policies**」 | `supabase/schema.sql` は `drop function if exists has_account()` を書いている |
| § 3「an anonymous account is made at first launch」（項目一覧の前の段落） | 同上 |
| § 3 項目5「The rest of moderation — the tombstone in a thread, ..., the notices, the frozen state on Home. **Not started**」 | 入っています（`postTomb()` `vNotif` と凍結）。残っているのはプロフィールの `⋯` だけ |
| § 3「`localStorage` is still where a post is kept ... but it is no longer the only place one exists」 | 「まだ端末が置き場」と読める。写しだと言い切る形に直しました |
| § 3 の `rest/v1` の数（2026-08-26 のもの） | 今日の数に。**`draft` 4 が増えています** ── 下書きはサーバー |

**項目4（プラン）だけは消さずに書き直しました。**決定（OWNER 2026-09-01、
プランはアカウントのもの）と、いまのコード（`profile` に列が無く、
`www/net.js` はプランを一度も送らず、値は `SET.plan`）の差として書いてあります。

### `docs/FEATURES.md`

| 消した文 | なぜ古いか |
|---|---|
| 表の行名「Timeline, **on this phone**」 | 端末の上のタイムラインではない |
| 表の Data 欄 ── 投稿 `lingua.posts` ／ プロフィール `lingua.me` | 本体はサーバー。`lingua.*` は写し |
| 表の Data 欄 ── 声「the bytes are a file in `Documents/Voices/`, **never in `localStorage`**」 | バケットに上がります |
| § 8「**~~The language on the phone is not touched.~~** That sentence is stale, and so was the note that replaced it」 | 取り消し線で古い文を残す形 |
| § 8「**This said 「A language is made on this phone with or without an account」**。It changed, on 2026-08-26」 | 同上 |
| § 8「**The copy on the phone is deliberately not touched — § 8 says so** and it has not been re-decided」 | § 8 は逆のことを言っている。`wipeAll()` が端末もバックアップも消す |
| 雲の行「This row said `no` / `yes, deferred` / 「deferred until Supabase $25 is worth paying」」 | 同上 |
| 「**Whatever `docs/STATE.md` § 3 item 3 still says**, this half is written」 | STATE を直したので、身を守る一節が要らなくなった |
| オンボーディングの節、「あとで」ボタンの上の一文の引用 | 消した規則を逐語で残していた |

### `docs/DATA_MODEL.md`

| 消した文 | なぜ古いか |
|---|---|
| `post.vo` の行「**On the disk is not where it belongs** ... **Not moved yet**; `docs/BACKLOG.md`」 | 移っています |
| 「`lingua.posts` (`POSTS`) **is** the timeline, and `lingua.drafts` (`DRAFTS`) **is** what was written and not sent」 | 次の段落で「どちらも写し」と直していたが、**先に読まれるのは前の文**。その場で言い切る形に |
| 写真の節「`pic` **is stored** as a data URL in `lingua.posts`」で始まっていた | バイトは `netUpPics()` で `post-media` へ。上限の理由（`localStorage` の割り当て）は変わらないので残しました |

「What money is allowed to touch」に、プランはアカウントのものだと足しました。

### `docs/DATA_SAFETY.md`

| 消した文 | なぜ古いか |
|---|---|
| **冒頭**「Everything a person makes lives in `localStorage` and in one file in `Documents`. **There is no server copy.**」 | **この文書でいちばん強く逆のことを言っている一文。**本体・写し・バックアップの三段に直しました |
| 声「**the voices are already backed up by being where they are**」 | `post-media` に上がる。端末のファイルは電波が無いときのため |
| 保存カウンタ「Nothing reads it yet. **It is here before the cloud is**, because the day the cloud arrives ...」 | クラウドは来ていて、これを読んでいません。`slice` 行は自分の `no` を持ち、`syMerge()` は両方足すので、どちらが新しいかを決める所がそもそも無い |

**「四つの失われ方」は消していません。**電波があれば `netLangSync()` が
1〜3 を戻す、と足したうえで、**それはファイルを軽く扱う理由にならない**と
書いてあります。

### `docs/PAID_FEATURES.md`

| 消した文 | なぜ古いか |
|---|---|
| 表「languages on **this phone**」 | 言語はサーバー。上限は端末ごとではなくアカウントごと |

頭の規則に「プランはアカウントのもの」を一段落足しました。

### `docs/FEATURE_RULES.md`

| 消した文 | なぜ古いか |
|---|---|
| 2026-08-26 の項の Implementation status「**三つとも未実装。**削除は `netDropMe()` がサーバだけを消す。端末を消すのは `wipeAll()` という**別のボタン**で、押されていない」 | `wipeAll()`（`www/settings.js:504`）が一つのボタンで両方やっています |
| 同じ項「`docs/FEATURES.md` § 8 は ... 「The language on the phone is not touched」と決めて、**そう書いてある**」 | もうそう書いていません（同じ枝で直した） |

**決定ログの先頭に 2026-09-01 を足しました。**オーナーの言葉のまま置き、
決まったこと六つ・Reason（何が起きたか）・Implementation status（4 だけ未実装）・
「コードと合っていない所」を書いてあります。

### `docs/keyboard.md`

冒頭「キーボードは言語ごとです。`lingua.<言語id>.kb` に入ります」だけだったのを、
**言語の一部なので言語と一緒にサーバー**（`slice` の `kb`）、端末の鍵は写し、
に直しました。

### `docs/apple.md`

| 消した文 | なぜ古いか |
|---|---|
| 「1. **`www/store.js`（未着手）**」 | 在ります。`setPlan()` が `storeBuy()` の唯一の呼び出し元 |
| 「4. 「購入を復元」ボタン ... **押すところがまだ無いだけです**」 | `storeRestore()` をプラン画面が呼んでいます |
| 「**足りないのは繋ぎです。**いまの「プラン」画面のボタンは `SET.plan` を書き換えるだけ」 | 繋ぎは在り、足りないのはサーバー側。行き先は `profile` の列 |

**3 節（レシート検証は端末のフラグを信じてはいけない）は元から正しいので
触っていません。**2026-09-01 の決定と同じことを言っています。

### `docs/BACKLOG.md`

| 直した所 | なぜ |
|---|---|
| 「語順と三つの位置が、言語ではなく端末に付いている」 | **済**。`STG.order` / `STG.gpos` が言語のもので、`migrateGramLang()` が `master` に在り `www/phases.js:131` から走る |
| 配信元を変える罠「`backup.js` が Documents に書いたファイル ... 渡る道はあるが」 | 本体はサーバーなので、サインインし直せば `netLangSync()` が戻す。`lingua.sess` も消えるので入り直しは要る、と書き足した |

---

## 3. 直しではないが、見つけたので直したもの

**`docs/BACKLOG.md` に取り込みの跡がそのままコミットされていました。**

```
docs/BACKLOG.md:2435   <<<<<<< HEAD
docs/BACKLOG.md:2493   =======
docs/BACKLOG.md:2541   >>>>>>> origin/claude/tail
```

両側は**別々の項目で、一行も重なっていません** ── `claude/acct2` の
「一度もサーバーに上がっていない言語が、どこからも戻せなくなる」と、
`claude/tail` の「`dead-check` は返した object のプロパティを見ていません」。
**印の三行だけ外して、両方残しました。**どちらの文も消えていません。

---

## 3-a. 画面の基準、十 ── 規則だけ直しました

**OWNER 2026-09-01。**リーダー経由。原文は `docs/CHANGELOG.md` に
`claude/fo2` が入れてあります（`3538668`）。**あちらは書き換えないので**、
`docs/FEATURE_RULES.md` § Owner decision log に写しを作りました ── CHANGELOG
自身が「この十は FEATURE_RULES にも要ります」と書いています。

**シートの禁止を狭めました。**`CLAUDE.md` § Shape、三つ目の禁止:

| | |
|---|---|
| 前 | 「a sheet that slides up **over where you were** instead of a page you went to」 |
| 後 | 「a sheet that slides up **in place of a screen you would otherwise have gone to**」＋狭めた分 |

**許したのは、iOS 自身が出す、消すか変えるかを訊く二〜三行だけ。**
**HTML で似せて描いたシートは閉じたまま**です ── 基準1が「システム標準を
最優先。独自実装は標準では実現できない場合のみ」と言っている以上、似せたものは
**禁止されている側が許された側の名前を着ているだけ**だからです。
`UIAlertController` が要るので `www/` だけでは持てません。

**文言は `claude/fo2` の `cbc3965` から取りました。**あの枝がオーナーの原文
（「長押しで消えるってわかんないだろ普通に。…」と、狭めるかを直接訊いた
「1はイエス」）を持っていて、先に正しく書いていました。`CLAUDE.md` が
こちらの持ち物なのでこちらで入れています ── 向こうは `5de1ccf` で
「この枝の持ち物ではなくなりました」と戻してあります。**二重には入っていません。**

### 十のうち、コードと合っていない二つ ── **直していません**

**① 基準8「タップ領域は44pt以上」と、キーボードのキーの例外。**
`CLAUDE.md` 規則3 は 44pt を要求したうえで、キーだけは高さで測ると書いて
います ── 一番細い iPhone は 320 なので横に十個で一つ 32pt、44 に広げると
**キーボードというものが作れません。**基準8はそこに触れていない。
**例外を消すとキーボードが消えます。**オーナーに訊く一行です。

**② 基準7「フォントは1〜2種類」と、`:root` の顔の数。**数えました
（`www/index.html`）:

```
  --face-ui     -apple-system … 'Noto Sans JP'   画面の地
  --face-caps   'Cinzel'                         見出し
  --face-ital   'Cormorant Garamond'             斜体の飾り
  --face-mono   ui-monospace
  --face-script 'LinguaScript'   ← その人が描いた文字
  --face-type   'LinguaType'     ← 同上
```

後ろの二つは**その人の文字**なので UI の書体には数えられません。
数えられるのは**四つ**で、基準7は二つまで。`--face-caps` と `--face-ital` を
どうするかは**全画面の見た目が変わる**ので `CLAUDE.md`「Deciding」のとおり
オーナーのものです。`tools/face-check.mjs` が両方向を押さえているので、
減らすなら検査と同じコミットで。

### 合っていたもの（数えました）

```
基準2  下タブ3〜5個     TABS は五つ（www/shell.js:652）      ○
基準9  削除→Undo        規則19 が既にその形（訊かず、戻る一歩）  ○
基準4  説明文は書かない  § Explaining が既に同じ                ○
基準10 実機で必ず確認    DEVICE CONFIRMED が既に同じ            ○
```

**画面は一つも触っていません。**この枝は `www/` を持っていません。

---

## 3-b. リーダーの指示のうち、一つだけ実行していません ── **声と下書き**

**指示:** 「音声と下書きをサーバーへ移すコードの作業は後回しになった。だから
『いまはまだ端末にある』という事実を `docs/STATE.md` に一行だけ残すこと。
消すと『もう移した』と読まれます」。

**その一行を書くと、嘘になります。**声も下書きも、**もう移っています。**
`master` のコードを数えました ── 推測ではありません:

```
www/net.js:1474  netPush()  →  :1497 netUpPics()  →  :1503 netUpVoice()
www/net.js:1513  netUpVoice()   voRead() で端末のファイルを読み、
                                netUp('<uid>/<pid>/vo.m4a') で post-media へ、
                                道を row.body.vu に載せる
www/post.js:862  netPush(p, …)   postCatchUp() から。sid の無い過去の投稿も上げる
www/post.js:1290 netPush(mine, …) 投稿した瞬間

www/net.js:1544  netDraftUp()    draft 行を書く
www/post.js:380  netDraftUp(d)    下書きを閉じたとき
www/post.js:440  netDrafts(…)     読み戻し
www/post.js:463  netDraftUp(…)    サーバーに無いものを上げ直す
```

**repo の中に、同じことを言っている記録が三つあります:**

| どこ | 何と書いてあるか |
|---|---|
| `docs/BACKLOG.md:2331` | 「下書きは端末にあった ── 2026-08-27、**済 2026-08-28 `claude/draft`**」。さらに「**投稿された録音は、この項目が書かれた時点で既にサーバーに在る**」と、録音について自分の間違いを訂正している |
| `docs/FEATURES.md:258` | 「**Already online** … 写真と声は Storage に」 |
| `www/net.js:1527` | 「A draft is the timeline's, so it lives on the server … The phone keeps the copy that works with no signal -- www/post.js's DRAFTS -- and **that copy is never the place a draft lives**」 |

**だから書きませんでした。**「いまはまだ端末にある」と `docs/STATE.md` に
書くことは、**この枝がまさに消しに来た種類の文を、いちばん信用されている
ファイルに一つ足す**ことになります。オーナーが 「そのせいで勝手に君が勘違い
してるわけでしょ？」 と言ったのは、その種類の文についてです。

**オーナーの原文は「音声とかをサーバーに載せる重いのは後でって話」。**
これが声と下書きのことなら、**その作業はもう終わっています。**別の重いものを
指しているのだと思いますが、**どれなのかは私には分かりません。**候補として
いま端末に残っている重いものを数えると:

```
Documents/Voices/*.m4a     上げたあとも消していない（電波が無いときのための写し）
lingua.posts の pic         写真の data URL。上げたあとも写しとして残る
下書きの body               写真と録音を base64 で持つ。バケットには置かない
                            ── post-media は公開バケットなので、公開していない
                               下書きの写真をそこに置けないため（www/net.js:1529 に
                               理由が書いてあります）。**これは後回しではなく設計です**
```

**訊きたいこと:「音声とかの重いの」はこのどれですか。**それとも、まだ誰も
見ていない別のものですか。**答えが来るまで `docs/STATE.md` は触っていません**
── いま書いてあるのは、数えて確かめた今日のコードです。

**もし「声と下書きはまだ端末」という報告がオーナーへ行っているなら、それも
古い記載から来ています。**`CLAUDE.md` の Layout の `www/rec.js` の行が
「Not moved yet」と言い、`docs/DATA_MODEL.md` の `vo` の行が同じことを
言っていました ── **この枝で今日消した二文が、まさにそれです**（§2）。

---

## 4. 迷ったもの ── **消していません。決めるのはリーダーかオーナーです**

**「古いかもしれない」で消すと正しい文が消える**ので、以下は手を触れずに
一覧にしました。

### 4-1. `docs/GRAMMAR-V2-SPEC.md` § 18「保存形式」

> **将来 Supabase 等へ移行する可能性も考えて**、UI から直接 localStorage の
> 構造に依存しない。

移行はもう起きているので、文としては古いです。**しかしこの文書は頭に
「これはオーナーが書いた仕様です。この文書を変えてよいのはオーナーだけ」と
書いてあります。**だから触っていません。**オーナーに訊く一行だと思います。**

### 4-2. 設定の「端末のデータを消す」という行

`docs/FEATURE_RULES.md` の決定ログに**三つ**入っています（2026-08-27 と
2026-08-28）:

- 「消す行は三本 ── ログアウト／**端末のデータを消す**／アカウント削除」
- 「端末のデータを消すと Documents のバックアップも消える」
- 「アカウント削除は全部。端末のデータは言語だけ消えて SNS は消えない」

`docs/STATE.md` § 0-a の表にも「**三本あるべきで、二本しか無い**」と在ります。

**2026-09-01 の「そもそも端末に保存するもんはないぞほとんど」と、
正面から噛み合っていません。**端末が写ししか持っていないなら、
「端末のデータを消す」は**写しを捨てて次の `netLangSync()` で戻ってくる**
ボタンです。それが要るのか、要るとして何をするのかは、**書かれた決定が二つ
食い違っている場合**にあたります。`CLAUDE.md`:

> If a decision conflicts with a rule already written down — **stop**, report
> both sides with the code and data affected, and do not resolve it yourself.

**止めました。**

### 4-3. プランが端末に在ること ── **コード側なので、この枝のものではありません**

決定は「プランはアカウントのもの」。コードはそうなっていません:

```
www/core.js       planKeep() / setOnDisk()   実機では Keychain、ブラウザでは設定ファイル
www/settings.js   setPlan()                  SET.plan を書く
supabase/schema.sql                          profile にプランの列が無い
www/net.js                                   プランを一度も送らない
tools/plan-check.mjs                         「ブラウザでは設定ファイル、実機では Keychain」を主張している
```

そして `docs/PAID_FEATURES.md` には、その検査が何を主張しているかを書いた行が
在ります（「that the plan is in the settings file in a browser and NOT in it on
a phone, where the Keychain has it」）。**これは検査の中身を書いた文なので、
検査より先に消すと文書のほうが嘘になります。**検査を直す枝と同じコミットで。

`docs/STATE.md` § 3 の項目4 と、決定ログの 2026-09-01 の
「コードと合っていない所」に、差として書いてあります。

### 4-4. `docs/STATE.md` 20a「The plan was never written down on a phone」

2026-08-22 の実機の回で見つかったバグの記録で、**いまのコードについては
正しい**（Keychain に入れる話）。ただし 4-3 の決定が入ると、この段落ごと
要らなくなります。**その日に消すもの**として置いておきます。

### 4-5. 日付の付いた記録 ── **一つも触っていません**

`docs/reports/*.md`（29本）、`docs/HANDOVER-2026-08-2*.md`（3本）、
`docs/scope/*.md`（4本）、`docs/SCOPE-yaa.md`。

**全部 grep しました。サーバーと端末について嘘を言っている文は一本も
ありませんでした**（`docs/reports/other-2026-08-28.md:189` が
「端末にしか無かったフォローはサーバーにも在ります」と、正しい側で書いている
だけ）。**だから消すものがありません。**

もし今後、日付の付いた報告も「今のことしか書かない」側に入れるなら、それは
`docs/CHANGELOG.md` の例外をどこまで広げるかという決めごとで、**私の判断では
ありません。**いまは `CLAUDE.md` の書き方どおり、CHANGELOG だけを例外として
扱いました。

### 4-6. `docs/FEATURE_RULES.md` の 2026-08-22「匿名アカウント」の項

**SUPERSEDED の帯が付いていて、`CLAUDE.md` が指示している形そのものです**
（「Mark the old decision superseded, fix the rules it wrote, and carry on」）。
だから残しました。**ただし中の Implementation status は「the phone's half is
done. The first launch signs in anonymously」のままです。**帯の中なので嘘には
なりませんが、気になるなら消す一行です。

### 4-7. `docs/BACKLOG.md`「移行が値だけを配った」の項

> **その枝はまだ `master` に入っていない**（`master..origin/claude/grammar2` = 7）。
> 入った日にこれが起きる。

**入っています** ── `stTouched()` も `STG.set` も `master` の `www/phases.js` /
`www/grammar.js` に在ります。つまり「入る前に決めるのが一番安い」という
一文はもう効きません。**ただし中身（案1 と案2 のどちらを取るか）は本当に
open な決めごとで、文法の枝の持ち物です。**日付と枝の状態だけが古いので、
**文法を持っている人が直すのが正しい**と思って触っていません。

---

## 5. 触っていないもの（宣言どおり）

```
www/**            コードは別の担当。一行も触っていない
tools/**          同上。検査の数や名前が docs と食い違っていても、直したのは docs 側だけ
supabase/**       同上
ios/**  package.json  capacitor.config.json
docs/CHANGELOG.md その日に何が本当だったかの記録。一行も触っていない
docs/GRAMMAR-V2-SPEC.md   オーナーだけが変えてよい文書（4-1）
docs/reports/**  docs/HANDOVER-*.md  docs/scope/**  docs/SCOPE-yaa.md   （4-5）
```

## 6. 検査

```
npm run i18n   緑
```

970鍵・10言語・61画面・338レンダー、すべて通過。鍵を触っていないので当然です。
**遅い二十は回していません** ── `docs/SESSIONS.md` のとおり、セッションは
ゲートを回しません。

## 7. コミット

```
fbaacb8  scope
55f5ea9  CLAUDE.md
52863d0  ARCHITECTURE
1d0a87f  STATE §3          ← 勘違いの元
043236d  FEATURES
658ca5f  DATA_MODEL
3065e2e  PAID_FEATURES
3686ba9  FEATURE_RULES     ← 2026-09-01 を決定ログに記録
07e9514  DATA_SAFETY       ← 「There is no server copy」
5e042f1  DATA_SAFETY（声）
a67966b  keyboard.md
94dd81a  apple.md
93e2006  BACKLOG           ← <<<<<<< HEAD を解いた
3c6fe58  DATA_MODEL（写しだと先に言う）
```
