# 監査 1 ── 決定の 1〜30 件目と、コードの照合

- 日付: 2026-09-03
- 範囲: `docs/FEATURE_RULES.md` § Owner decision log の **1〜30 件目**
  （テンプレートの `### Decision` はコードフェンスの中なので数に入れていません。
  実体の 1 件目は「シンプルに作る」です）
- 基準にした木: `master` の `36e65562`
- **コードは一行も変えていません。**このファイルだけを足しています。

## 途中で master が二つ進みました

読み始めた時点は `8dd3a80f` でした。書いている間に二つ入っています。

- `b7e2d82d` iOS 自身のダイアログが三箇所に残っていた ── 止めるものを入れる
- `36e65562` 規則の言葉を直す ── 修正ではなく書き換え

**8 番の指摘は、その一つ目で三箇所のうち三箇所が直っています。**残っているのは
Swift 側の一箇所だけです。下の表と詳細は `36e65562` を読み直して書いたもので、
行番号もそこのものです。

## 表

| # | 決定（短く） | 判定 | 場所 | 何が違うか |
|---|---|---|---|---|
| 1 | シンプルに作る。直すのではなく書き換える | 一致（押さえるもの無し） | ─ | 規則。機械で読める形は無い |
| 2 | 印の無い言語を拾うのは扉だけ | 一致 | core.js:872 / net.js:963 | net.js の四つ目はまだ拾う。本人申告どおり |
| 3 | 端末のものは無い。全部アカウントのもの | **不一致** | core.js:93 / CLAUDE.md:741 | 「lsWipeNS() は消えました」が事実と違う。残っている |
| 4 | 課金はメアドのアカウントに紐づく | 一致（記載が古い） | core.js:1119 / LinguaPlan.swift | 「未実装」と書いてあるが入っている |
| 5 | DL は Plus から。Plus1・Pro3 | **不一致（記載）** | home.js:1249 | 注記が、置き換えられた 2026-08-19 の決定を引いている |
| 6 | 言語の記事は分岐しない | 一致 | home.js:1578 | 残る差二つは本人申告どおり |
| 7 | 長押しのしきい値は 10px | **不一致（記載）** | shell.js:824 / sns.js:390 | コードは 10px。sns.js の注記が前の姿を説明している |
| 8 | iOS 標準のダイアログもシートも使わない | **不一致** | me.js:301 → LinguaShare.swift:723 | UIAlertController が生きていて、me.js が呼んでいる |
| 9 | 機能追加はリーダーが確認する | 一致（運用） | ─ | コード無し |
| 10 | 今週リリース。機能は足さない | 一致（運用） | ─ | コード無し |
| 11 | DL した言語 ── 編集・バックアップ・上限 | 一致（記載が古い） | core.js:360 / backup.js:157 | 「未実装」だが 1・2・4 は入っている |
| 12 | 画面の基準が十 | 一致 | shell.js:736 / index.html:94 | 未達二つは本人申告どおり |
| 13 | アクションシートだけ許す（narrowing） | **不一致（置き換え済み）** | FEATURE_RULES.md:697 / me.js:301 | 8 番が取り消したのに印が無く、コードは古い側に従っている |
| 14 | 課金とアカウントとキーボードはアカウント | 一致（記載が古い） | schema.sql:434 | 「プランは無い」だが `plan` テーブルで在る |
| 15 | 未読は最後に通知画面を開いた時刻より新しいもの | 一致 | sns.js:1510 | `SET_ACCT` にも入っている |
| 16 | 端末に住むものは三つだけ | **不一致（置き換え済み）** | FEATURE_RULES.md:788 | 3 番が区分ごと消したのに印が無い |
| 17 | プロフィール画像の削除は入れる | 一致 | me.js:338 | 中身は在る。形は 8 番の話 |
| 18 | 自己紹介は出す | 一致 | me.js:423, :759 | 自分にも他人にも出ている |
| 19 | 全部の升、触ったら選択 | 一致 | keyboard.js:1407, 1426 | 押すと選択、帯のボタンが入れる |
| 20 | シートは升。中央寄せは両端に半分ずつ | 一致（記載が古い） | keyboard.js:2069 | 「未着手」だが `kbLead()` が丸めずに入っている |
| 21 | 上がっていない言語を守る条件は入れない | 一致 | settings.js:447 | 条件は一つも無い |
| 22 | 端末のデータを消すと Documents も消える | 一致 | settings.js:484 | `bkDropKept()` |
| 23 | サブリーダー居ないときはリーダーが取り込む | 一致（運用） | ─ | コード無し |
| 24 | 取り込むのはサブリーダー | 一致（運用） | ─ | コード無し |
| 25 | 消す行は三本 | 一致（記載が古い）／**不明** | settings.js:275, 309, 311 | 三本在る。ただし 2 本目が端末全体で 3 番とぶつかる |
| 26 | 規約ページの三つ | **不明** | 別リポジトリ | このセッションから読めない |
| 27 | ゲートはサブリーダーが回す | 一致（運用） | ─ | コード無し |
| 28 | 語釈は二段。赤い字は無い | 一致 | post.js:2953 | 両方から消えている |
| 29 | 残り字数は減っていく輪、常に出す | 一致 | post.js:1324 | 門は無い |
| 30 | 話題は X と同じ。青パッチが上がりやすい | 一致（未着手） | post.js:1073 | 本人申告どおり、掛ける先が無い |

---

# 不一致の詳細

「どう書き換えるか」は、条件を足す案ではなく **そのコードを消して書き直す案**
で書いています。ただし 8 番と 13 番は形がオーナーのものなので、書き換えの案では
なく「訊く一行」で止めています。

## 8. iOS 標準のダイアログもシートも使わない

**ファイル:行** `www/me.js:301`（呼ぶ側）→ `ios/App/App/LinguaShare.swift:723`（出す側）

**決定の一文**（`docs/FEATURE_RULES.md:482`、2026-09-01）

> **`confirm()` `alert()` `prompt()` と `UIAlertController` を使わない。**

原文は「標準は使わねえって言ってんだろこれも禁止や」「iPhoneのやつ使ってるsnsないしな」。

**コードが実際にやっていること**

プロフィール画像を触ると `mePicAsk()`（me.js:296）が `LinguaShare` の `ask` を
呼び、`LinguaShare.swift:723` が `UIAlertController` を組み立てて出します。
「写真を選ぶ／削除する／キャンセル」の三行で、下から上がってきます。
**決定が名指しで禁じた型が、そのまま生きています。**

**残っているのはここだけです。**`b7e2d82d` が `www/` 側の三箇所を落としました。

```
www/home.js  editName()  prompt()  → openForm() に
www/core.js  dlStop()    alert()   → toast() に
www/core.js  langStop()  alert()   → toast() に
```

同じコミットで `es5-check` が `www/*.js` 全部をなめて `confirm(` `alert(`
`prompt(` で落ちるようになりました。**その検査は `www/` しか見ないので、
`ios/` の `UIAlertController` は止まりません。**

**どう書き換えるか ── ここは案を書きません。**

決定の末尾がこうなっています。

> **リーダーへ: 勝手に進めない。**この決定は、形が決まる前に三度作って三度
> 外した後に出ている（遷移する画面 → 下から出るシート → 標準のダイアログ）。
> **訊いてから作る。**

四度目を私が決めるところではないので、材料だけ置きます。

- 受け皿は既に在ります。`popAsk()`（shell.js:1387）が画面の中に重なる自前の
  もので、`wipeAll()` も `wipeLangs()` も今それです。
- ただし `popAsk()` は はい／いいえ の二択です。`mePicAsk()` は
  「変える／外す」の二択で、形が違います。
- 消える側の中身は既に在ります（`meDropPic()` me.js:338）。作るものは形だけです。

**オーナーへの一行はこれだと思います。**「プロフィール画像を触ったときの
二択を、`popAsk()` と同じ自前のもので出してよいか。ボタンが二つ並ぶ形になる」。

そして **止めるものを片側にしか入れていないことは、書いておくべきです。**
`es5-check` が `www/` を見るようになった今、`ios/` の一箇所だけが検査の外に
います。`assets-check` が既に `ios/App/` を読んでいるので、置き場所はそこです。

## 13. 「iOS 自身のアクションシートだけ許す」── 8 番に取り消されている

**ファイル:行** `docs/FEATURE_RULES.md:697`（決定そのもの）、`www/me.js:301`（従っているコード）

**決定の一文**（2026-09-01）

> **許すのは、指の下にある一つのものを消すか変えるかを訊く、iOS 自身が出す
> 二〜三行のもの**、しかも **iOS 自身のものだけ**です。

**何が起きているか**

同じ日に 8 番が出ていて、その Affected docs にこう書かれています。

> その日にアクションシートを許した narrowing を**取り消す**

ログは新しい順なので、8 番（:467）が 13 番（:697）より後です。**それでも 13 番に
superseded の印がありません。**本文を読むと今も許可として読めます。そして
`mePicAsk()` はその許可のとおりに作られています。

**古いほうの決定に、コードが従っている状態です。**リーダーの依頼文が名指しした
形そのものです。

**どう書き換えるか**

三つ、同じコミットで。

1. 13 番の見出しに `── **superseded 2026-09-01（8番）**` を足す。
2. 本文の許可の段を**消す**。「これは歴史です」と前置きして残さない
   ── 1 番の三つ目がそれを禁じています。残るのは「画面の代わりに使うシートは
   禁止」の一文だけで、それは 8 番と CLAUDE.md § Shape が既に持っています。
3. Affected features の「いまは**画面**（`openForm`）で、変える／外すが縦に
   並んでいます」も事実と違います。いまはアクションシートです。この行も消す。

コードの書き換えは 8 番と同じ一つの仕事なので、そちらに寄せます。

## 16. 「端末に住むものは三つだけ」── 3 番に取り消されている

**ファイル:行** `docs/FEATURE_RULES.md:788`

**決定の一文**（2026-09-01、決まったこと 3）

> **端末のものとして残るのは三つだけ** ── 言語のバックアップのファイル
> （`Documents/Languages/<name>.json`）、書き出した表、設定。

**何が起きているか**

3 番（2026-09-03、:311）がこの区分ごと消しています。

> **「端末のもの」という区分は無くなりました。**

`CLAUDE.md` は同じコミットで直っていて、§ Online の「This replaced a list」の段が
この三つを名指しで取り消しています。**16 番には印がありません。**

**これは、オーナーの言語が消えた原因として 3 番の Reason に名指しされている
当の文です。**

> **規則に「端末のものは三つ」と書いてあったことが、その姿を正しく見せて
> いました。**規則を消さないと同じ形が出続けます。

`CLAUDE.md` からは消えました。**決定ログの中でだけ、そのまま生き残っています。**

**どう書き換えるか**

- 見出しに `── **superseded 2026-09-03（3番）**` を足す。
- 決まったこと 3 の段落を**消す**。1 と 2（サーバーが本体、端末は写しだけ）は
  3 番と同じことを言っているので、そのまま残ります。
- Affected data の「何も消えません」はそのままで正しい。

**コードは 3 番のほうに従っています**（`setFor()` core.js:1188、
`lsWipeAcct()` core.js:68）。直すのは文だけです。

## 3. 端末のものは無い ── 自分の Implementation status が事実と違う

**ファイル:行** `www/core.js:93`、`CLAUDE.md:741`

**決定の一文**（`docs/FEATURE_RULES.md:347`）

> (2) アカウント削除は `lsWipeAcct()` と `bkDropFor()` で**そのアカウントの
> ぶんだけ**（`lsWipeNS()` と `bkDropAll()` は消えました）

**コードが実際にやっていること**

- `bkDropAll()` は本当に消えています。`www/backup.js` に在るのは `bkDropFor()`
  と `bkDropKept()` だけです。
- **`lsWipeNS()` は `www/core.js:93` に在ります。**`www/` の中からは誰も
  呼んでいません。呼ぶのは `tools/backup-check.mjs` だけです。

`dead-check` が拾わないのは、名前がコメントの中に六回書かれているからだと
思います（core.js:109・116、me.js:94・123、settings.js:424・426）。
**読むだけの仕事なので検査は回していません。ここは推測です。**

そして `CLAUDE.md` の規則 6 が古いままです。

**`CLAUDE.md:741`**

> 「アカウント削除で残るものねえって言ってんだろ何回言わせんだよ全部消えんだよ。」
> So `lsWipeNS()` counts `localStorage` instead and removes everything under
> `lingua.` — no list, and a key added tomorrow is gone the day it is added.

アカウント削除が今やっているのは `lsWipeAcct(uid)` です。そのアカウントのぶん
だけ消します。**この段は 3 番が置き換えた側の説明のままで、規則 6 は毎セッション
読まれる場所です。**

`www/settings.js:619` のコメントもこう書いています。

> bkDropAll() empties the directory ... **It is still there** for a language
> being deleted on its own; nothing here calls it.

**その関数はもう在りません。**

**どう書き換えるか**

1. **`lsWipeNS()` を消す**（core.js:93-102）。条件を足すのではなく、関数ごと。
   `tools/backup-check.mjs:550` がそれを呼んでいるので、同じコミットで
   `lsWipeAcct()` に書き換えるか、その主張ごと落とす必要があります
   ── **どちらかは、その検査が何を証明しているかを読んでから決めることで、
   私は読んでいません。**
2. コメント六箇所を消す。`lsWipeNS()` を説明している段落は、消えた関数の
   説明なので、条件を足す話ではなく段落ごと落とします。
3. `CLAUDE.md:735-742` の段を書き直す。いまの二文（一覧を歩いていたのが悪かった
   → だから名前空間を数える）は、置き換えられた形の説明です。今の形は
   「アカウントの印が付いた鍵だけを数える」で、`lsWipeAcct()` がそれです。
4. `www/settings.js:619` の「It is still there」を消す。

## 5. DL は Plus から ── コードは合っている。注記が古い決定を引いている

**ファイル:行** `www/home.js:1249-1256`

**決定の一文**（`docs/FEATURE_RULES.md:390`、2026-09-02）

> ダウンロードできるのは **Plus から**。持てる数は **Plus 1、Pro 3**。
> ...
> `CAN.dl` は 2026-08-19 の「キーボードと文字の DL は無料、辞書は Plus」を
> **置き換える**

**コードが実際にやっていること**

動きは決定どおりです。`wldGet()`（home.js:1278）が押した先で両方訊きます。

```
home.js:1291   if(upStop(can('dl'))) return;        門
home.js:1292   if(!LANGS[id] && dlStop()) return;   数
core.js:1317   dl: 'plus'
core.js:957    var PLUS_DL=1, PRO_DL=3;
```

**違うのは、その一つ上の行を描く関数の注記です。**

**`home.js:1249`**

> **NO PLAN IS ASKED**, and that is read off docs/FEATURES.md § 4 rather than
> left out: 「Downloading a keyboard or an alphabet is free; downloading a
> dictionary is Plus.」
> ...
> `CAN.dl` and its rung go in with the first `can('dl')`, which is the day
> the dictionary can be taken at all

引いているのは 2026-08-19 の決定で、**5 番が明文で「置き換える」と言っている側
です。**そして「`CAN.dl` はこれから入る」と書いていますが、`CAN.dl` は
core.js:1317 に既に在り、二行下で呼ばれています。

行を描く所が plan を訊かないこと自体は正しい形です（押した先が訊くので、
同じ問いが二箇所になりません ── 1 番のとおり）。**嘘なのは理由の説明のほうです。**

**どう書き換えるか**

`home.js:1249-1256` の注記を消して、短く書き直します。八行が二行になります。

```
/* 段はここで訊きません。押した先の wldGet() が upStop(can('dl')) と dlStop()
   を訊きます ── 一つの問いは一つの場所で。 */
```

## 7. 長押しのしきい値は 10px ── コードは入っている。注記が前の姿

**ファイル:行** `www/sns.js:389-391`

**決定の一文**（`docs/FEATURE_RULES.md:454`、2026-09-01）

> 親指が **10px を超えて動いたら**、長押しをやめる。それ以下は動いていない
> ものとして扱う。

**コードが実際にやっていること**

`www/shell.js:824` に `HOLD_SLOP=10` が在り、:855 が二乗で比べています。
決定どおりです。決定の Implementation status は
「`claude/words2` `3ba0503`。取り込み待ち」ですが、**master に入っています。**

**違うのは `www/sns.js:389-391` の注記です。**

> It does NOT fight the profile tab's hold in www/shell.js. That one arms on
> an element carrying `data-hold` and **disarms on any touchmove**, which is
> exactly right: a pull is not a hold.

「any touchmove で外れる」は、**この決定が直した前の姿です。**そしてその姿は
「実機で長押しが一度も成立していなかった」原因として決定の Reason に書かれて
います。注記はそれを「exactly right」と言っています。

**どう書き換えるか**

その一文を書き直します。

```
   It does NOT fight the profile tab's hold in www/shell.js. That one arms on
   an element carrying `data-hold` and disarms once the thumb has moved more
   than HOLD_SLOP (10px), which is exactly right: a pull is not a hold.
```

決定の Implementation status も「実装済み（master）」に直す。

## 8 と同じ家族 ── 置き換えられた形を説明している注記が二つ

不一致として別立てにはしませんが、同じ「古い文が残っている」形なので置きます。

**`www/shell.js:1414-1435`**

> **THIS IS iOS's OWN DIALOG AND NOT A SHEET.** ...
> So it is **confirm()**, which is what capStop() has always used for the word
> ceiling ... 「システム標準（iOS/Android）を最優先」

その下にあるのは `popAsk()` です。`confirm()` は `www/` から消えています。
この段落は、8 番が禁じた側を「これでよい」と説明しています。

**`www/settings.js:640`**

> It is **the app's own sheet** rather than the browser's alert() because
> nothing in Lingua has ever used one

`openCapLapse()` の中身は `openForm()` です。シートではなく、遷移する画面です
── `popAsk()` の注記が「三度作って三度外した」一つ目として名指ししている形です。

どちらも、段落を消して今の形を一行で書き直すのが書き換えです。

---

# 不明

推測で埋めていません。何が分からなかったかを書きます。

## 25. 「端末のデータを消す」がどこまで消すか

**ファイル:行** `www/settings.js:447`（`wipeLangsGo()`）、`www/backup.js:385`（`bkDropKept()`）

三本（ログアウト・端末のデータを消す・アカウント削除）は全部入っています。
26 番の「未着手（2 を戻す）」は古いだけです。

**分からないのは範囲です。**

```
settings.js:449   for(id in LANGS) ... ids.push(id)     全言語。誰のものでも
settings.js:484   bkDropKept()                          バックアップのファイルを全部
```

二つの決定が違う方向を向いています。

- **22 番・23 番・26 番（どれも 2026-08-28）**は「端末のデータ」について
  字義どおり全部と言っています。原文は「4です。最初からずっと言ってるけど」
  「全部消えるって」。**その読みでは、いまのコードは合っています。**
- **3 番（2026-09-03）**は「『端末のもの』という区分は無くなりました」と言い、
  その Reason は **別のアカウントを消したらオーナーの言語が消えた件**です。
  `wipeAll()` はそれで `lsWipeAcct()` に直りました。**この行は端末全体のまま
  です。**

つまり **2026-09-03 に直したのと同じ形が、直っていないほうの行に残っています。**
B でサインインしてこの行を押すと、A の上がっていない言語と A のバックアップ
ファイルが消えます。

**私が決めるところではないので不明にしました。**二つの書かれた決定が食い違って
いて、新しいほうがこの行について言い直してはいません。`CLAUDE.md` § Deciding の
「止めて報告する」側だと思います。

**オーナーへの一行はこれだと思います。**「『端末のデータを消す』は、いま
サインインしているアカウントの言語だけを消すのか、この端末にある言語を
全部消すのか」。

書き換えの案は、答えが決まってからです。前者なら `wipeLangsGo()` の頭の
`for(id in LANGS)` を `lsWipeAcct()` と同じ問い（`LANGS[id].uid === me`）に
書き換え、`bkDropKept()` を `bkDropFor(ids)` に置き換えます
── どちらも既に在る形なので、新しい仕組みは足りません。

## 26. 規約ページの三つ

**決定**（2026-08-28）は、準拠法はそのまま・通報は24時間・13歳以上。
対象は `tokinets.com/lingua/terms.html` と `/privacy.html`、
リポジトリは `natsuaya82-crypto/tokine2` の `lingua/` です。

**このセッションの GitHub の範囲は `natsuaya82-crypto/lllllll` だけなので、
読めませんでした。**確かめるには、そのリポジトリをセッションに足す必要があります。

決定の Implementation status は「実装中（`claude/legal`。1 は既に入っている、
2 と 3 を入れる）」ですが、`claude/legal` というブランチはこのリポジトリの
一覧に在りません。**これが別リポジトリのブランチなのか、消えたのか、
名前が違うのかも分かりません。**

---

# 押さえるものが無いもの

一致しているが、明日壊れても何も言わないものです。

| # | 決定 | 押さえるもの |
|---|---|---|
| 1 | シンプルに作る | 無し。決定自身が「規則。止めるものはありません」と書いている |
| 8 | ダイアログ禁止 | **片側だけ。**`es5-check` が `www/*.js` を見る（`b7e2d82d`）。`ios/` は誰も見ていない |
| 11-1 | DL した言語は編集できない | `langLocked()` は在るが、それを回す検査は無い。`import-check` が名前に触れているだけ |
| 18 | 自己紹介を出す | 無し |
| 28 | 語釈は二段 | 無し |
| 29 | 輪が常に出る | 無し |

18・28・29 は「これを消すな」という形の決定なので、消えたときに鳴るものが
どこにも無い、という意味です。

---

# 範囲の外で気づいたこと（1〜30 件目ではありません）

私の担当は 1〜30 件目ですが、`lsWipeNS()` を探しているときに一つ目に入りました。
**担当の人に回してください。私は判定していません。**

`docs/FEATURE_RULES.md:1772`（見出しは :1726 の `### Decision`）が、
`wipeAll()` の説明としてこう書いています。

> `wipeHere()` で `lingua.` の鍵ぜんぶ（`lsWipeNS()`、手の一覧は無い）と
> バックアップのファイル。

`wipeHere()`（`www/settings.js:522`）が今呼ぶのは `lsWipeAcct(wipeUid)` で、
消えるのはそのアカウントのぶんだけです。**3 番と同じ古い文が、もう一箇所に
在ります。**

---

# やったことと、やっていないこと

- 決定 30 件を一件ずつ読み、指しているファイルと関数を開いて読みました。
- **検査は一本も回していません。**読むだけの仕事なので。3 番の
  「`dead-check` が拾わない理由」は、そのためコードを読んだ推測です。
- **コードは一行も変えていません。**このファイルだけを足しています。

---

# あとがき（2026-09-03、リーダーの指示で文書だけ直したあと）

**直したもの ── `docs/FEATURE_RULES.md` の二件だけ。**16 番は見出しに
`── 決まったこと3は **superseded 2026-09-03**` を付け、決まったこと3の段落を
消して 4・5 を 3・4 に繰り上げました（1・2・4・5 は生きているので、項ぜんぶを
superseded と印すと嘘になります。印は決まったこと3に絞りました）。
7 番は Implementation status を「実装済み（master）」に。

**直さなかったもの。**3・8・13・25 はリーダーが直したので触っていません。
5 と 7 の `sns.js` の注記はリーダーが「済み」と言いましたが、
**`www/home.js:1249` の「NO PLAN IS ASKED」も `www/sns.js:390` の
「disarms on any touchmove」も、まだそのまま在ります。**私の担当は文書だけなので
コードには触れていません ── リーダーに戻します。

**もう一つ、直していない小さいもの。**7 番の Affected features が
「`www/sns.js` の同じ形も同じ値」と書いていますが、**長押しは `www/shell.js`
にしか在りません**（`data-hold`・`holdStart`・`HOLD_SLOP` は全部そこ）。
`sns.js` に在るのはそれを説明する注記だけです。指示された二件の外なので
足していません。
