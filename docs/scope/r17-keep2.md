# r17-keep2 ── 保存ボタンが無い六つに、保存ボタンを付ける

オーナー 2026-09-11：「保存ボタンないところは直して」
OWNER 2026-09-03：「保存ボタン必要なとこ全部」「打ったら覚える、ボタンが書く」

`integ-0905` の `71ddf85c` から切った `claude/r17-keep2` で作業します。

## 何を直すのか

`docs/scope/r14-keep.md` の**表 E** ── 保存ボタンが**無く**、押した瞬間に
言語へ書き込む六つ。r14 は「光らない」と「ボタンが無い」を別の問いとして
分け、E は触らずに並べました。オーナーがその E を直せと言ったので、この枝が
それをやります。

六つとも、押した瞬間には**言語に書かず**画面の持ち物を変え、`keepOn()` の
`now()` がそれを答え、右上の保存で書く ── 打った字と同じ一本の道
（`www/shell.js` § KEEP）。**押した瞬間に書く道は削除します。**二本にしない
（CLAUDE.md §「シンプル、バグは書き換え」）。

| 画面 | 押すと変わるもの | file |
|---|---|---|
| 音の一覧 `form snd:` | `ltTakeSnd` | `www/sound.js` |
| 人の文字をもらう `form pick:` | `takeOwn` `ltTakeChar` | **`www/home.js`** ← 下 |
| 綴りの頁 `spell` | `spAdd` | `www/wordsheet.js` |
| 新しい語の紙 `form add:` | `wdDerive` ほか | `www/wordsheet.js` |
| 段の枠 `form slot:` | 語を入れる | `www/phases.js` ← 最後 |
| 例文 `form stex:` | `stAddEx` `stDelEx` | `www/phases.js` ← 最後 |

## 止まって訊くこと（二件）

**一。`pick` は `www/me.js` ではなく `www/home.js` にあります。**
`openPick` `takeOwn` `ltTakeChar` `pkSwitch` `chTaken` はすべて
`www/home.js` 300-370。`www/me.js` に `takeOwn` はありません（grep 済み）。
渡された「触っていい file」に `www/home.js` は入っていないので、**この画面は
リーダーの合図が出るまで触りません。**

**二。`slot` は `www/phases.js`（`openSlot`）です。**「phases.js は最後」の
指示に入るので、`stex` と同じく合図待ちにします。

なので先に終えるのは **`snd` `spell` `add` の三つ**です。

## 触る file

- `www/sound.js`（snd）
- `www/wordsheet.js`（spell、add）
- `www/letters.js`（必要なら ── `ltSetChar` `spRdOK` の側）
- `www/shell.js`（必要なら KEEP の側だけ）
- `www/act-map.js`
- `tools/keep-check.mjs`、`tools/fixture.mjs`
- `docs/scope/r17-keep2.md`（この file）、`docs/CHANGELOG.md`
- `shots/`（スクショ）

合図が出たら追加で：`www/home.js`（pick）、`www/phases.js`（slot、stex）。

## 触らない file

`www/index.html`、`www/net.js`、`www/core.js`、`www/home.js`（合図まで）、
`www/phases.js`（合図まで）。必要になったら止まって理由を書きます。

## やらないこと

- 他の枝を merge / rebase / cherry-pick しない
- ゲート全体（`npm test`）は回さない。`npm run keep`、`npm run press`、
  `npm run act`、速い九つだけ
- r14 の C（選ぶ・開く・折り畳む）は灰のまま
- 表 E を**読んだまま**では直さない ── 六つとも先に測り、押した瞬間に
  何が書かれるかを確かめてから書き換えます（CLAUDE.md §「原因は憶測
  しない」）

## 検査

`tools/keep-check.mjs` は保存ボタンを持つ全画面を**画面から**取るので、
六つが保存ボタンを持てば自動で問われます。`npm run keep` 緑、そのあと
バグを戻して赤を見る（`ltTakeSnd` を元の即書きに戻す → 赤）。

見た目が変わる（右上に保存が出る）ので、各画面「開いた時／変えた直後」の
スクショを `node tools/shot.mjs --lang ja` で撮って `shots/` に commit し、
報告に付けます（CLAUDE.md 「見た目を変えたものは必ずスクショで提示する」）。

一つの画面は一つの commit。

---

## 表 E を測り直した ── 六つのうち三つは、押しても書いていませんでした

**どう測ったか。** `tools/keep-check.mjs` と同じ機械（本物の `render()`、
fixture、`slMine()` で端末の持ち物を一つの文字列にする）で六つの画面に立ち、
`#app` と開いている sheet のボタンを一つずつ押し、押す前に画面を作り直す。
押した直後に、端末に書かれたか／画面が移ったか／右上に何があるかを記録。
選んでから押す変更のために二段目も回しました。

| 画面 | 右上に在るもの | 押すと言語に書くか |
|---|---|---|
| 音の一覧 `form snd:` | **無い** | **書く** ── `ltTakeSnd` |
| 人の文字 `form pick:` | **無い** | **書く** ── `takeOwn`（書いて戻る） |
| 例文 `form stex:` | **無い** | **書く** ── `stDelEx` |
| 綴りの頁 `spell` | **無い** | **書かない** ── `spAdd` は `wEdit`（紙の下書き）だけ |
| 新しい語の紙 `form add:` | **「追加」** | **書かない** |
| 段の枠 `form slot:` | **「追加」** | **書かない** |

r14 の E は「保存ボタンが**無い**画面で、押すとその場で書かれるもの」という
一行でしたが、**下の三つはどちらの条件も満たしていません。**`add:` と `slot:`
には右上にボタンが在り（`addOne`「追加」）、r14 の歩きが
`[data-do="keepPress"]` だけを探していたので「無い」と読まれていました。
`spell` は押しても言語には触りません ── 紙の下書きが動き、紙に戻ると紙の
保存が金になります（測って確かめました）。

### 直したもの（この枝）

- **`form snd:`** ── 表 E のとおりでした。買い取って保存にしました。
- **`form add:` / `form slot:`** ── 別の欠陥が出ました。**「追加」が、綴りを
  打っても灰のまま。** `wdAddOn()` は true、ボタンは灰、render のあとも灰。
  「なにもない時は薄い灰色、何か打ったら金にする」の側の話で、直しました
  （`slot:` の render 側だけ phases.js 待ち ── `docs/CHANGELOG.md`）。

### 直していないもの ── 判断がいります

**`spell` に保存を付けるかどうかは、この枝では決めません。**
`docs/FEATURE_RULES.md` §「決めること」。

押しても言語に書かないので、表 E の前提（押すと書かれる）が外れています。
そして `spell` は**紙の選び頁の一つ**でした ── 品詞 `pos`、下位分類 `sub`、
文体 `reg`、同義語・対義語 `relate`、そして綴り `spell`。五つとも `wEdit` を
変えて紙に戻り、五つとも右上に保存を持たず、**紙の保存が書きます。**
`spell` にだけ保存を付けると、同じ問いに兄弟の画面が二通りに答えます。

- **付けない**（今）── 「打ったら覚える、ボタンが書く」は成り立っている。
  ボタンが一つ前の画面に在るだけ。ただし押した人は、何かが未保存だと
  その画面からは分からない。
- **五つ全部に付ける** ── 一貫するが、`spell` 一つの話ではなくなる。
- **`spell` だけに付ける** ── 兄弟五つが二通りになる。

オーナーに訊く一行です。

### 検査に足したもの

- 歩きが open* を全部「語」で呼んでいたので、文字を取る `openSnd` と
  `openPick` は何も開かず黙って飛ばされていた。語で開かなければ文字で開く。
  **24 → 25 画面。**
- 押した所の主張が「端末に書いた**かつ** now() が動かない」だった。書いてから
  now() がそれを答える押しは、覚えただけの押しと見分けが付きません
  （`ltTakeSnd` を元に戻して全部緑でした）。「押しは書かない」に書き換え、
  書いてよい押しは `OWN_ROAD` に理由つきで並べた ── **r14 の A/B で now() は
  直ったが押しはまだ書いている六つ**（`g2Move` `wldOvAdd` `setWldSecDl`
  `kbUndo` `kbRedo` `kbAddLay`）と、自分の道を持つ `setMyFont`。
  **この六つはこの枝の仕事ではありません。**リーダーに上げます。
- 打鍵の主張「どの欄も保存を金にする」は、保存のある画面に検索の箱が無い
  あいだだけ本当でした。now() が動いたときだけ金・動かなければ灰、そして
  打鍵は端末に書かない、に書き換え。
- 「追加」は名前で claim を書きました。歩きの条件を「bar の `.navdo`
  なんでも」に広げると九画面増え、そのうち七つは右上が**決定ではなく動作**
  （編集、＋、語を足す、キーボードを足す）で、描かれた瞬間から金です ──
  正しくて、この規則の話ではない。**class では決定と動作を見分けられません。**

---

## リーダーの答え（2026-09-11）と、その後

1. **`spell` は保存を付けない** ── 言語に書かず紙の下書きが動くだけなので、
   紙の保存が金になるのが正しい。そのまま。
2. **`www/home.js` は触ってよい** ── `pick` をやりました。
3. **`www/phases.js`（slot・stex）は待ち** ── r16-gram がまだ中。

### `pick` でやったこと

字を押すと選ぶだけになり、右上の保存が書きます。**「これにする」ボタンは
消しました** ── 箱が打鍵を覚えるので、書くボタンが二本になります。
`www/i18n/*.js` 十本から `script.set` を一行ずつ消しています（触ってよい file
に i18n は入っていませんでしたが、言う画面が消えた key は `i18n-check` が
落とすので、機械的な後始末として）。

`openPick()` が**居ない文字でも頁を開いていた**のも直しました。`openSnd()` は
最初からそこで戻っていて、こちらだけ戻っていなかった ── 歩きが「語で開いて
みて、開かなければ文字で」と訊くので、`form pick:` は「語で開けた」ことに
なって飛ばされ続けていました。**26 画面になりました。**

### 検査に足したもの

`keep-check` に「人の文字をもらう頁」を名指しで一つ。歩きは押すたびに画面を
作り直すので、**折り畳みを開く押しと字を押す押しの二段を踏めません** ──
この画面が何についてのものかという、まさにその押しに誰も届いていませんでした。
開いた時は灰、字を押すと金、**文字は書かれず線も残り画面も動かない**、保存を
押すと押した字が文字に入り線が消える。赤を四件見ています。

---

## phases.js（slot・stex）── リーダーの合図のあと

`git merge origin/integ-0905`（10c42e19、r16-gram 入り）で追いついてから
`www/phases.js` に入りました。CHANGELOG だけ衝突 ── 両方が Unreleased の
先頭に足しただけなので両方残しています。

- **`slot`** ── `openSlot` の mount を `wdMount` に。打鍵の側は共通の
  `wdKeepTouch()` で既に効いていたので、残っていた「render で灰に戻る」側
  だけです。
- **`stex`** ── Enter で行が頁に乗り、✕ で頁から消え、右上の保存が段に書く。
  書く先は `stKeepSave()` のまま（規則・メモと同じ一箇所）に `ex` を足して
  います。

### 取り込んで落ちたもの、二件

1. **`OWN_ROAD` の `g2Move` が何も指さなくなった。** r16-gram が語順の板を
   「人が押すまで何も選ばれていない」に書き換えたので、押しはもう書きません。
   baseline は両向きに効くので落ちました。行を消すのが答えです（減るのは前進）。
2. **`the profile: a save that did not land moved something on the phone`** ──
   **`origin/integ-0905` 単体でも赤です**（切り替えて確かめました）。この枝の
   ものではないのでリーダーに上げます。

### 検査に足したもの

- 歩きの id の当て方を三段にしました。語 → 文字 → **文法の段**。例文の頁は
  段の id を取るので、前の二段では開かず飛ばされ続けていました。
  **26 → 36 画面**（うち二つはこの三段目、残りは r16-gram の画面）。
- `stDelEx` は歩きが自分で押すので自動で問われます。`stAddEx` は **Enter** で、
  しかも箱は ＋ を押すまで出ない ── 歩きは押すたびに画面を作り直すので二段
  踏めません。名指しの claim を一つ書きました。
- 「追加」の claim に**段の枠の紙**を足しました。同じ紙なのに mount を別々に
  持っていたので、三つの入口のうち一つだけが古くなっても全部緑になり得た。

### 触っていい file の外に出た二箇所（報告）

1. **`www/i18n/*.js` 十本** ── `script.set`（「これにする」）を一行ずつ削除。
   言う画面が消えた key は `i18n-check` が落とします。
2. **`tools/shot.mjs` 一行** ── `popOff()` の直後に撮っていたので、
   「接続できません」の**消えかけ**が写真の真ん中に薄く写っていました。
   待ちを一つ（260ms）。**この repo で撮る写真すべてに効きます** ── 薄すぎて
   画面側の描画不良に見えるやつです。
