# 扉 ── 一枚のものとして、普通のアプリの水準まで

`claude/door2`。**持っているのは `www/onboard.js` と `tools/open-check.mjs`
と、このファイル**です。`www/net.js` は**読むだけ**で、そこに要る直しは
§ オーナーに訊くこと の下の「リーダーへ ── `www/net.js` に要る直し」に
出します。

### Scope

- **Goal:** 扉の面をぜんぶ読んで、人がそこで取り得る道を数え、**一つずつ
  「普通のアプリならどうか」と突き合わせる。**一行の直しではありません。
  発端は入る面が「アカウントが無い」と「パスワードが違う」を同じ一文で
  返していること ──「アカウントがないならそうやって出るわけねえだろ」
  OWNER 2026-09-03
- **Owns (may change):** `www/onboard.js` / `tools/open-check.mjs` /
  `docs/scope/claude-door2.md`
- **Does NOT own:** それ以外すべて。とくに **`www/net.js`**（`netWhy()`
  `netSignIn()` `netMailTaken()` `netVerify()` `netRecoverCode()`
  `netIdToken()` は読むだけ）、`www/i18n/*.js`、`www/index.html`、
  `supabase/schema.sql`
- **Decision it implements:** 2026-09-01「画面の十の基準」の一と二 ──
  **システム標準を最優先／普通の SNS がみんな持っている形**。
  そして 2026-09-03「アカウントのあるアドレスで新規作成はいらんやろ」の
  **入る面**（作る面と再設定の面は済み、入る面だけが誰にも見られていない）
- **Check to run:** `npm run open`（`tools/open-check.mjs`）

---

## 1. 扉の面は七つ、道は五十一本

`obDoorHTML()`（`www/onboard.js:1447`）が `OBM.mode` で選ぶ面が**七つ**あります。
そのうち六つは `obFormHTML` `obAskHTML` `obResetHTML` `obNewPwHTML`
`obWhoHTML` の五つの関数が描いています。

```
  in      入る          メール＋パスワード / Apple / Google / 忘れた / 帯「作る」
  up      作る          メールだけ / Apple / Google / 帯「入る」/ 規約の一行
  code    六桁（作る）   確認 / もう一度送る / 戻る→up
  forgot  再設定の宛先   送る / 戻る→in
  reset   六桁（再設定） 確認 / もう一度送る / 戻る→forgot
  newpw   新しい合言葉   決める / 戻る→（署名があればアプリへ）
  who     名前と @      次へ / 戻る無し
```

そこに Apple・Google の道と、`obIn()` が七つ目の面へ送る道が乗ります。

**数えた道は五十一本。**下に全部並べます。行の頭の記号:

```
  ○  今すでに普通のアプリと同じ
  ×  違う。差が実際に人の目に出る
  ?  文言か形の決めごとが要る。§3 に送った
```

---

## 2. 一本ずつ ── 今どうなっているか / 普通のアプリならどうか / 差

### A. 入る面（`in`）── `obMailIn()` `www/onboard.js:1043`

`obMailIn()` は `netSignIn()` をそのまま呼びます。**`netMailTaken()` を
呼びません。**作る面（1070行）と再設定の面（1131行）は呼んでいます。

| | 道 | 今 | 普通のアプリ | 差 |
|---|---|---|---|---|
|×| **A1 アカウントの無いアドレス** | Supabase が 400 `Invalid login credentials` → `net.badlogin`「メールアドレスかパスワードが間違っています」 | **アカウントが無いことをそう言う。**そのアドレスに登録の道を出すか、少なくとも「パスワードが違う」とは別の文 | **これが本題。**「アカウントがないならそうやって出るわけねえだろ」 |
|○| A2 パスワードが違う | 同じ 400 → `net.badlogin` | パスワードが違うと言う | 文としては合っている。ただし A1 と**同じ文**なので、どちらか分からない |
|×| **A3 パスワードを一度も決めていないアカウント** | 同じ 400 → `net.badlogin` | 「このアドレスは Apple / Google で作られています」など、**合言葉が無い**ことを言う | **必ず起きる道です。**Apple / Google で作った人にはパスワードが無い。さらに `newpw` の面は chevron で飛ばせる（G4）ので、メールで作った人にも無いことがある |
|×| **A4 確認の済んでいないアカウント** | 400 `email_not_confirmed` → `netWhy()` のどの枝にも当たらず、**サーバーの英語がそのまま画面に出る**（`Email not confirmed`） | 確認が済んでいないと、その人の言語で言う | 十言語の app に英語の一文が出る |
|×| **A5 パスワード欄が空のまま押す** | `obMailAsk()` は @ しか見ない。**空のままサーバーへ送る** → 400 → `net.badlogin` | 画面で止めて「パスワードを入れてください」 | 一往復ぶん待たされて、しかも間違った文が返る |
|×| **A6 アドレスの形が違う（`a@` など）** | `indexOf('@')<0` だけなので通る → 400 `Unable to validate email address: invalid format` → **英語の生文** | 画面で止める | A4 と同じ穴 |
|○| A7 @ が無い | `net.needmail`「アドレスを入れてください」画面で止まる、送らない | 同じ | 無し |
|×| **A8 200 が返って署名でなかった** | `bad(d, 0, 'token ≠')` → `netWhy()` は status 0 を全部 `net.offline`「接続できません」にする → **繋がっているのに「接続できません (token ≠)」** | 起きない／起きても「接続できません」とは言わない | `netWhy()` は `www/net.js` のもの。§4 に出しました |
|○| A9 通信が落ちた | `net.offline` ＋ `(token 0)` | 同じ | 無し |
|○| A10 叩きすぎ（429） | `net.toomany`「回数が多すぎます。少し待ってください」 | 同じ | 無し |
|○| A11 二度押し（メールの側） | `if(OBM.busy) return` ＋ ボタン `disabled` | 同じ | 無し |
|×| **A12 送っている間に「作る」「忘れた」を押す** | 帯の「アカウントを作る」も「パスワードを忘れた」も **`disabled` にならない。**押せる。あとから `obIn()` が返ってきて `OBM.mode='who'` を書くので、**作る面に立っていた人の画面が突然「名前と @」になる。**`obNo()` なら作る面に入る面のエラーが出る | 送っている間はその面ぜんぶが固まる | 押している間の状態が主ボタンにしか無い |
|○| A13 サインアウト直後、chevron が無い | `obCanBack()` が `in` の面だけ false | 「ログアウトした時はログイン画面から動かさない」 | 無し |
|○| A14 設定から開いたときは戻れる | `obPending()` が在れば戻る | 同じ | 無し |

### B. 作る面（`up`）── `obMailUp()` `www/onboard.js:1061`

| | 道 | 今 | 普通のアプリ | 差 |
|---|---|---|---|---|
|○| B1 空いているアドレス | `email_taken` に訊く → 六桁を送る → `code` の面 | 同じ | 無し |
|○| **B2 既にアカウントのあるアドレス** | `ob.mail.taken`「このアドレスは既に登録されています」赤 | 同じ | **無し。2026-09-03 に決着済み** |
|○| B3 `email_taken` が答えない | そのまま送りに行く（`obMailUpGo`） | 同じ（外の穴で自分の扉を閉めない） | 無し |
|○| B4 @ が無い | `net.needmail` | 同じ | 無し |
|×| **B5 形が違うアドレス** | 通る → OTP が 400 → **英語の生文** | 画面で止める | A6 と同じ穴 |
|○| B6 429 | `net.toomany` | 同じ | 無し |
|○| B7 二度押し | busy で止まる | 同じ | 無し |
|×| **B8 送っている間に帯や Apple を押す** | A12 と同じ | 同じ | A12 と同じ |
|○| B9 規約とプライバシーの一行 | 在る（`ob.docs` ＋ `docRows()`） | App Review 1.2 | 無し |
|○| B10 パスワードを訊かない | 訊かない（「メアドだけ、アカウント作成で」） | 同じ | 無し |

### C. Apple / Google ── `obSocial()` `www/onboard.js:779`

| | 道 | 今 | 普通のアプリ | 差 |
|---|---|---|---|---|
|○| C1 プラグインが無い（web） | `net.nonative` トースト | 同じ | 無し |
|○| C2 Google の client id が無い | `net.nonative` トースト | 同じ | 無し |
|○| C3 シートを閉じた | 何も言わない（`obShrug`） | 同じ ── 閉じたのは失敗ではない | 無し |
|○| C4 トークンが返らなかった | 何も言わない | 同じ | 無し |
|○| C5 nonce の両側 | `claude/door` が直した。`open-check` §5 が持つ | 同じ | 無し |
|×| **C6 二度押し** | **`obSocial()` に `if(OBM.busy) return` が無く、Apple と Google のボタンは `disabled` にもならない。**二回 `p.login()` が走り、シートが二枚立つ | 一枚目が終わるまで押せない | 基準3。**入る面のメールの側には在る門が、この二つには無い** |
|?| **C7 同じアドレスを別の道で既に作っている** | Supabase が identity を繋げれば通る。繋げないときは 422 → **英語の生文** | その人の言語で、何が起きたか言う | §3 の問い4 |
|×| C8 `initialize()` が落ちた | `obShrug()` ── **黙って spinner を止める。**「設定に失敗した」と「シートを閉じた」が同じ見え方 | 設定の失敗は言う | `obReady()` の `['catch'](obShrug)`。`obNative()` の上のコメント自身が「設定の失敗は閉じたのと別のこと」と書いているのに、受けは同じ |

### D. 六桁 ── 作るほう（`code`）── `obMailCode()` `www/onboard.js:1093`

| | 道 | 今 | 普通のアプリ | 差 |
|---|---|---|---|---|
|○| D1 正しい六桁 | `newpw` の面へ（`fresh=true`） | 同じ | 無し |
|×| **D2 違う六桁** | 400 → `netWhy()` が落ちて **英語の生文**（`Token has expired or is invalid`） | 「コードが違います」 | 十言語の app に英語 |
|×| **D3 期限切れ** | D2 と**同じ英語の生文**。区別が付かない | 「コードの期限が切れました」と別の文 | Supabase は `error_code: otp_expired` を付けてくるので**分けられます**（`netWhy()` は `error_code` を見ていない） |
|×| **D4 空のまま押す** | **止めずにサーバーへ送る。**再設定の側（`obResetGo()`、1148行）は `if(!OBM.code)` で `net.needcode` を出して止めるのに、**こちらだけ止めない** | 画面で止める | **同じ六桁の面が二つあって、片方にしか門が無い** |
|○| D5 もう一度送る | `netMailOtp` → `ob.mail.sent`「送りました」 | 同じ | 無し |
|?| **D6 60秒以内にもう一度** | ボタンは押せたまま。押すと Supabase の 429 → `net.toomany`「回数が多すぎます。少し待ってください」 ── **何秒待つのかは出ない** | ボタンが数字を持って止まる（「45秒後にもう一度」）。数え終わると押せる | §3 の問い5。**秒数と、ボタンの形が変わる**ので決めない |
|○| D7 戻る → `up` | `obDoorBack()` | 同じ | 無し |
|○| D8 二度押し | busy | 同じ | 無し |

### E. パスワードを忘れた（`forgot`）── `obMailForgot()` `www/onboard.js:1124`

| | 道 | 今 | 普通のアプリ | 差 |
|---|---|---|---|---|
|○| E1 アカウントのあるアドレス | `reset` の面へ | 同じ | 無し |
|○| **E2 アカウントの無いアドレス** | `ob.mail.none`「このアドレスのアカウントはありません」赤、送らない | 同じ | **無し。2026-09-03 に決着済み** |
|○| E3 `email_taken` が答えない | そのまま送る | 同じ | 無し |
|○| E4 @ が無い | `net.needmail` | 同じ | 無し |
|×| B5 と同じ形の違うアドレス | 通る → 英語の生文 | 画面で止める | A6 と同じ穴 |
|○| E5 戻る → `in` | `obDoorBack()` | 同じ | 無し |

### F. 六桁 ── 再設定のほう（`reset`）── `obResetGo()` `www/onboard.js:1148`

| | 道 | 今 | 普通のアプリ | 差 |
|---|---|---|---|---|
|○| F1 正しい六桁 | `newpw` の面へ（`fresh=false`） | 同じ | 無し |
|○| **F2 空のまま押す** | `net.needcode` で止まる | 同じ | 無し。**D4 が真似すべきはこれ** |
|×| F3 違う／期限切れ | D2・D3 と同じ英語の生文、区別なし | 別の二文 | D2・D3 と同じ |
|○| F4 もう一度送る | `netRecover` → `ob.mail.sent` | 同じ | 無し |
|?| F5 60秒以内にもう一度 | D6 と同じ | D6 と同じ | D6 と同じ |
|○| F6 戻る → `forgot` | 同じ | 同じ | 無し |

### G. 新しいパスワード（`newpw`）── `obNewPwGo()` `www/onboard.js:1162`

| | 道 | 今 | 普通のアプリ | 差 |
|---|---|---|---|---|
|○| G1 決める | `netSetPass` → `obIn()` | 同じ | 無し |
|○| G2 空 | `net.needpass`「新しいパスワードを入れてください」 | 同じ | 無し |
|○| G3 短すぎる | 422 → `net.weak`「パスワードが短すぎます」 | 同じ（長さはサーバーが決める） | 無し |
|○| G4' `netSetPass` が落ちた → chevron でアプリへ | 「サインインしたらアプリに移動してください」の実装 | 同じ | 無し |
|?| **G4 chevron で決めずに飛ばせる** | 六桁を使った時点で署名は立っているので、chevron を押せばアプリに入れる。**パスワードの無いアカウントができる** → A3 に直結 | ここで飛ばせること自体は普通（Apple / Google の人も持っていない）。**その人が次にメールで入ろうとしたとき何と出るか**が問題 | §3 の問い2 と一つの問い |
|?| G5 見せる／隠すの目 | 無い | ほぼ全部の app に在る | §3 の問い6。**画面の形が変わる**ので決めない |

### H. 名前と @（`who`）── `obWhoGo()` `www/onboard.js:1337`

| | 道 | 今 | 普通のアプリ | 差 |
|---|---|---|---|---|
|○| H1 両方入れる | プロフィールを作る → @lingua をフォロー → 戻り先かアプリ | 同じ | 無し |
|○| H2 名前が空 | `net.needname` | 同じ | 無し |
|○| H3 @ が 2〜24 でない | `net.badhandle`。打った空白などは落として見せ返す | 同じ | 無し |
|○| H4 @ が取られている | `net.handle.taken`（先に訊き、409 でも同じ文） | 同じ | 無し |
|○| H5 戻るが無い | アカウントは既に在るので、後ろは「別人として入る」になる | 同じ | 無し |
|×| **H6 `obIn()` のプロフィール取得が落ちたとき** | `obIn()` は**取りに行く前に** `OBM.mode='who'` を書く（860行）。落ちるとその面にエラーだけ出て**置き去り**。**プロフィールを既に持っている人が名前と @ を訊かれ**、押すと重複の挿入で 409 →「その @ は使われています」── **自分の @ について、そう出ます** | 取れなかったのだから、名前を訊く面には行かない | **通信が一瞬切れただけで、既にある人が登録画面に立つ** |

---

## 3. オーナーに訊くこと

**文言はオーナーのものなので、下は案です。**新しい鍵が要るものは十言語ぶん
並べました。**「訊かない」と決まった鍵は作りません。**

### 問い1 ── 入る面で「アカウントがありません」と出してよいか

作る面（「このアカウントは登録されています」赤文字、2026-09-03）と
再設定の面（`ob.mail.none`）は、**アカウントが在る／無いを外に言う**と
決まっています。**入る面も同じでよいですか。**

- **はい** なら、`obMailIn()` が `netSignIn()` の前に `netMailTaken()` を
  訊きます（作る面・再設定の面と同じ形）。**新しい鍵は要りません** ──
  既に在る `ob.mail.none`「このアドレスのアカウントはありません」を使えます
- **いいえ**（入る面だけは伏せる）なら、今のまま `net.badlogin` です。
  そのときは A3（パスワードの無いアカウント）と A4（未確認）も伏せることに
  なるので、**そこも一緒に決めてください**

> 補足しておきます。作る面と再設定の面で既に言っているので、**入る面だけ
> 伏せても、同じアドレスを作る面に打てば分かります。**伏せることで守れる
> ものは、いま既に守れていません。

### 問い2 ── パスワードの無いアカウントに、何と出すか

Apple / Google で作った人にはパスワードがありません。六桁のあと
`newpw` の面を chevron で飛ばした人にもありません。その人が**メールと
パスワードで入ろうとしたとき**、今は「メールアドレスかパスワードが
間違っています」と出ます。

案（新しい鍵 `net.nopass`）:

```
  en  This address has no password.
  ja  このアドレスにパスワードはありません。
  es  Esta dirección no tiene contraseña.
  pt  Este endereço não tem senha.
  fr  Cette adresse n'a pas de mot de passe.
  de  Diese Adresse hat kein Passwort.
  it  Questo indirizzo non ha una password.
  ru  У этого адреса нет пароля.
  zh  此邮箱没有密码。
  ko  이 주소에는 비밀번호가 없습니다.
```

**「Apple で続けてください」とは書きません**（CLAUDE.md § Explaining ──
何が起きたかは言ってよく、どうすればいいか教えるのは禁止）。この一文が
出た人は、同じ画面の下にある Apple と Google のボタンを見ます。

**この文を出すには問い1 が「はい」である必要があります**（アカウントが在る
ことが分かって初めて、パスワードが無いと言える）。

### 問い3 ── 六桁が違うときと、期限が切れたとき

いま**両方とも英語の生文**が出ます（`Token has expired or is invalid`）。
Supabase は `error_code` で二つを分けているので、**分けて出せます。**

案（新しい鍵 `net.badcode` / `net.oldcode`）:

```
  en  That code is not right.            /  That code has expired.
  ja  コードが違います。                  /  コードの期限が切れました。
  es  El código no es correcto.          /  El código ha caducado.
  pt  Esse código não está certo.        /  Esse código expirou.
  fr  Ce code n'est pas le bon.          /  Ce code a expiré.
  de  Dieser Code stimmt nicht.          /  Dieser Code ist abgelaufen.
  it  Questo codice non è corretto.      /  Questo codice è scaduto.
  ru  Неверный код.                      /  Срок действия кода истёк.
  zh  验证码不正确。                       /  验证码已过期。
  ko  코드가 올바르지 않습니다.             /  코드의 유효 기간이 지났습니다.
```

**一つにまとめる**（両方「コードが違います」）でもよければ、そう言って
ください。鍵が一つ減ります。

### 問い4 ── 確認の済んでいないアドレスと、形の違うアドレス

どちらも今は**英語の生文**が画面に出ます。

```
  net.notconfirmed
  en  This address is not confirmed.      ja  このアドレスは確認されていません。
  es  Esta dirección no está confirmada.  pt  Este endereço não foi confirmado.
  fr  Cette adresse n'est pas confirmée.  de  Diese Adresse ist nicht bestätigt.
  it  Questo indirizzo non è confermato.  ru  Адрес не подтверждён.
  zh  此邮箱尚未验证。                      ko  이 주소는 확인되지 않았습니다.

  net.badmail
  en  That is not an address.             ja  アドレスの形が違います。
  es  Eso no es una dirección.            pt  Isso não é um endereço.
  fr  Ce n'est pas une adresse.           de  Das ist keine Adresse.
  it  Non è un indirizzo.                 ru  Это не адрес.
  zh  这不是一个邮箱地址。                   ko  주소 형식이 올바르지 않습니다.

  net.needpass.in（入る面で、パスワード欄が空のまま押したとき。
                  既に在る net.needpass は「新しいパスワードを」なので使えません）
  en  Type your password.                 ja  パスワードを入れてください。
  es  Escribe tu contraseña.              pt  Digite sua senha.
  fr  Entrez votre mot de passe.          de  Gib dein Passwort ein.
  it  Scrivi la tua password.             ru  Введите пароль.
  zh  请输入密码。                          ko  비밀번호를 입력하세요.
```

### 問い5 ── 六桁をもう一度送るとき、何秒待たせるか【画面の形が変わります】

いまボタンはいつでも押せます。60秒以内に押すと Supabase が断って
「回数が多すぎます。少し待ってください」と出ます ── **何秒なのかは
出ません。**普通の app は**ボタンが数字を持って止まり**、数え終わると
押せるようになります（基準2）。

- 秒数はサーバーの設定と揃える必要があります（Supabase の既定は60秒）
- **ボタンの見た目が変わる**ので、`ui変更は俺が頼んだの以外は勝手な判断で
  やるなよ？` に当たります。**やりません。**やってよいか訊いてください
- やる場合の文案（`ob.mail.again.wait`、`{0}` に秒）:
  `en Send it again in {0}s` / `ja あと{0}秒`

### 問い6 ── パスワードを見せる目【画面の形が変わります】

ほぼ全部の app にあります（基準2）。**いまありません。**画面に一つ増えるので
訊きます。**入れてよいですか。**

### 問い7 ── 設定の失敗を黙っているのを直してよいか

Apple / Google のボタンを押して、プラグインの `initialize()` が落ちると
**何も出ません**（`obReady()` の `['catch'](obShrug)`）。人からは
「押したのに何も起きない」に見えます。`obNative()` の上のコメント自身が
「設定の失敗と、シートを閉じたのは別のこと」と書いているのに、受けが同じです。

既に在る `net.failed`「うまくいきませんでした」を出すだけなので**新しい鍵は
要りません**が、**今まで黙っていた所が喋る**ので訊きます。

---

## 4. リーダーへ ── `www/net.js` に要る直し（読むだけなので触っていません）

**`netWhy()`（`www/net.js:319`）が、扉の差の半分の出どころです。**
上の × のうち、A4・A8・B5・C7・D2・D3・F3 は全部ここに落ちます。

1. **`netWhy()` は Supabase の `error_code` を見ていません。**
   `d.msg` の英語を正規表現で拾っているだけなので、`email_not_confirmed`
   `otp_expired` `validation_failed` `email_exists` はどれも枝に当たらず、
   **最後の `return m || t('net.failed')` で英語がそのまま画面に出ます。**
   十言語の app に英語の一文が出る道が、扉だけで六本あります。

2. **`status === 0` が三つを一つの文にしています。**
   `netWhy()` の頭 `if(!status) return t('net.offline')` は、
   `netTook()` が断った 200（`'token ≠'`）にも「接続できません」と言います。
   **繋がっているときに「接続できません」と出ます。**net.js のコメント自身が
   「三つの原因と三つの出口」と書いていて、**文は一つしかありません。**

3. 問い1 が「はい」になったら、`netMailTaken()` の呼びが扉で三箇所に
   なります。net.js 側は何も要りません（既に在る関数をもう一箇所から呼ぶだけ）。

**どれも `claude/find` か、net.js を持っている枝の仕事です。**順番を決めて
ください。

---

## 5. この枝が、決めごと無しで直すもの

上の × のうち、**文言も画面の形も決めごとも要らないもの**だけを直します。
残りは §3 の答えを待ちます。

```
  C6  Apple / Google の二度押し             門が無い。入る面のメールには在る
  D4  作るほうの六桁が空のまま送られる       再設定のほうには門が在る
  A12 送っている間に帯とソーシャルが押せる   主ボタンだけが disabled
  H6  取得が落ちると「名前と @」に置き去り   取れていないのに名前を訊く
  C8  設定の失敗を黙っている                → §3 問い7 の答え待ち
```

**全部、`tools/open-check.mjs` に主張を足して、バグを入れて赤を見てから
直します。**

---

## 6. なぜこうなったか ── 一行で

**扉は一枚のものとして作られたことが一度もありません。**面ごとに、
オーナーが名指しした日に、その面だけが直されてきました:

```
  2026-09-02  「再送信ボタンと戻るボタンがない」    → code と reset の面に付いた
  2026-09-02  「メアドだけ、アカウント作成で」      → up の面からパスワードが消えた
  2026-09-03  「アカウントのあるアドレスで新規作成」 → up の面が先に訊くようになった
  2026-09-03  「アカウントの無いアドレスに六桁を」   → forgot の面が先に訊くようになった
  ────────────  入る面は、名指しされていません        → 誰も見ていません
```

`netMailTaken()` は 2026-09-03 に作られ、**その日のうちに二つの面が使い、
三つ目の面は誰にも思い出されませんでした。**同じ形の穴が、上の × の
ほとんどです ── 六桁の面が二つあって片方にしか空の門が無い（D4）のも、
`disabled` が主ボタンにしか無い（A12）のも、同じ理由です。

**面ごとに直す限り、名指しされていない面は必ず残ります。**
