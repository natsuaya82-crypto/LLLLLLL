# r107-store ── 1.0.3 の App Store「新機能」の文、10 言語

ブランチ `claude/r107-store`（`integ-0905` から）。

## 持ち物（これ以外は触らない）
- `store/*.json`（10 ファイル）の `whatsNew`、必要なら `promotionalText` だけ
- この文書

## しないこと
- アプリのコード・ほかの文書・ほかのキーは触らない
- ゲートは回さない（json が読めることだけ `node -e` で確かめる）
- ビルドはしない
- 通知が届くとは書かない（iPhone の通知はまだ直っていない、docs/STATE.md）

## 報告

**変えた物**: `store/*.json` 10 ファイルの `whatsNew` だけ（1.0.2 の文を丸ごと 1.0.3 の物に）。`promotionalText` は今のままで
1.0.3 にも合っているので触っていない。ほかのキー・アプリのコード・ほかの文書は触っていない。
保存されるデータ・移行・削除・プランへの影響は無い（App Store Connect に貼る文だけ）。

**中身の出どころ**: `docs/STATE.md` の 1.0.3 の節と `docs/FEATURE_RULES.md` 2026-09-25・26 の決定。
手書きのキーボードは `ios/App/LinguaKeyboard/hand.js` の頭の説明（書いた字に一番近い自作文字が入る）。
**通知は書いていない**（iPhone の通知はまだ Apple に断られている）。フォントの書き出しは Plus だが、「新機能」には置き場所だけ書いた。

**確かめたこと**: `node -e` で 10 ファイルとも `JSON.parse` が通る。書き戻しは `JSON.stringify(o,null,2)+"\n"` で、
元のファイルと同じ形になることを変える前に 10 ファイルで確かめた（各ファイルの差分は whatsNew の 1 行だけ）。
ゲートは回していない。訳は人が読んで直す余地あり（ネイティブの確認はしていない）。

### 字数（whatsNew、上限 4000）

| ファイル | 字数 |
|---|---|
| `de-DE.json` | 1875 |
| `en-US.json` | 1584 |
| `es-ES.json` | 1792 |
| `fr-FR.json` | 1934 |
| `it.json` | 1812 |
| `ja.json` | 733 |
| `ko.json` | 862 |
| `pt-BR.json` | 1749 |
| `ru.json` | 1621 |
| `zh-Hans.json` | 531 |

### ja の全文

```
新しく
- 単語の自動生成：言語の音と音節の形から、合う単語の候補を出します。選んだ物が辞書に入ります。
- 語源の系統図：単語がどの語から来たのかを、木の形でたどれます。
- 組み合わせの書き方：ハングルのように、いくつかの字を一マスに重ねて書けます。字は描いた位置のまま重なります。
- 文字を SVG で書き出せます。全部まとめても、一文字だけでも。
- 引用リポスト：投稿に自分の一言を付けてリポストできます。
- いいね・リポストした人の一覧：ハートやリポストの印を長押しすると出ます。
- フォロー中の人のリポストに「〇〇がリポスト」と出ます。プロフィールにもその人のリポストが並びます。
- スレッドで開いた投稿の本文と意味を、長押しで選んでコピーできます。
- 手書きのキーボード：キーボードの上に指で字を書くと、自作文字の中から一番近い字が入ります。
- キーボードを作る画面で、いくつかのキーを選んで一度に字を入れられます。選んだ順に①②と付き、確定で入ります。
- 作る画面の「?」に使い方が入りました。

変わったところ
- キーボードはどのプランでも作れるようになりました。
- 字を選ぶ画面を一つにまとめ、選べる字の種類が増えました。
- フォントと SVG の書き出しは、文字の画面の共有マークから。
- 意味を出す・出さないはスイッチに。意訳は意味の欄の中に字として入ります。
- 投稿画面がキーボードで揺れなくなりました。
- 押した所に輪が出ます。
- リポストやフォローを外す時は確認します。
- スレッドで開いた投稿に詳しい時刻が出ます。
- 購入した後の表示が正しく出るようになりました。

ほか、多くの不具合の修正と改善。
```
