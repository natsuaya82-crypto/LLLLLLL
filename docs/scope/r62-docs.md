# r62-docs ── 書いてある事が今と違う（2026-09-23）

土台は `integ-0905`。持ち場は r46-audit § C と付録 1、そしてそれを覆う検査一本。

## 覆う一文と、それを持つ検査

**記録でない文書が呼び出しの形で書いた関数の名前は、コードにある。**
`tools/docs-check.mjs` の三つ目の向き（FAST・`npm run docs`・pre-commit に既に居る）。
数えるのは面 ── 生きた文書 27 本の呼び出し全部（1800）。消えた関数を語る文は
~~`name()`~~ と打ち消し、打ち消したのにコードに在れば赤。

赤は四通り見た: 打ち消しを外す／在る関数を打ち消す／`netLangsDown` を改名（25 件）／
baseline に在る関数の行。直す前の docs で 224 件赤。

## r46 の読みと違った所

- 付録 1 の 76 のうち `hasBytes`・`jsIn`・`measureRows` は tools/ の検査の中に在る。
  消えているのは 73。
- r46 のスクリプトは大文字を含む名前だけを見ていた。面を数え直すと、付録の外に
  `has_account()`・`keep()`・`best()`・`approx()`・`slice_back()` など、
  README の 21 名を含めて 100 名を超えた。

## 持ち物外で、baseline に積んだもの（66 行）

README.md（21 名 ── 別のアプリの説明のように古い）、docs/STATE.md、docs/TESTING.md、
docs/FEATURE_RULES.md の決定ログの中（本文には無い）。`tools/docs-baseline.txt`。

## コードのコメントで、持ち物外のもの

- `www/backup.js` 頭のコメント（r46 A4 の「script タグ三つ前」）── www は持っていない。
- `www/core.js:331` のコメントが消えた `netLangBack1()` を名指している。
  この検査は docs だけを見る。

## BACKLOG から消した見出しと、その理由（リーダーの確認に）

項目を消したのは、その物が無いか片付いたことを commit か決定ログで指せた時だけ。
指せなかった一つは戻した。

| 見出し | どうしたか | 理由 |
|---|---|---|
| `askLink()` の `extra` に値を入れる呼び手が無い | 消した | 関数ごと消えた ── `b800697c` 2026-09-01「ai ボタンを章ごと外す」 |
| Is `numSetVal()` reachable at all? | **戻した**（`2e405b49`、今の名前で） | 関数は `d38258b5` で消えたが、「数字の値は人が変えられるか」はオーナーの決定を指せない |
| 取り込みを取り消しても、単語から生まれた音は言語に残る | 消した | 取り消しのボタンと `impUndo()` が `08058d36` 2026-09-06 で消えた（取り込みが四画面になった） |
| 下書きについて、決まっていない二つ ──「バックアップに入れるか」 | 消した | バックアップのファイルそのものが無い ── 決定ログ 2026-09-04「バックアップのファイルも無くす」 |
| 同 ──「アカウント削除で下書きも消すか」 | 消した | 項目自身が「決まった 2026-08-28」と書いていた ── 決定ログ 2026-08-28「アカウント削除は全部」 |
| 端末からファイルが出ていく道が二本ある | 消した | CSV の書き出しは `d586023d` 2026-08-27 で消えた（「csvの書き出しはなくていいよ」）。カードは `22c49926` 2026-09-02 で用紙と同じ一本（`LinguaShare.sheet()` → `shareFile()`）に乗った |
| 辞書の ⋯ が、何も入っていない紙を開く | 消した | 直った ── `85c66948` 2026-08-27 で ⋯ ごと外れた |
| `www/reading.js` に、誰も呼ばなくなったものが四つ | 消した | 直った ── `9baeecb8` 2026-08-28 で五つとも消えた |
| ログアウト→ログインでプランが pro になるか | 消していない。今の形に書き直した | 09-08 の測定は `SET.plan` と改名の移行の話で、どちらも `f2d231ed` 2026-09-11 で消えた |

## docs/STATE.md の baseline 行（リーダーの物。直すのはリーダー）

| 行 | 名前 | 今その役をしているもの |
|---|---|---|
| 124 | `netMediaURL()` | 文が消えたことを言っている ── 打ち消し（`~~`netMediaURL()`~~`）。今は `netMedia()` |
| 291 | `netTakeGone()` | `netLangsGone()`（一本になった） |
| 537 | `postTr()` | 文が消えたことを言っている ── 打ち消し。今は `translate.toNatural()` |
| 726 | `bkRestore()` | `netLangsDown()`（無いものを埋めて止まる） |
| 726・746 | `langMigrate()` | 無い。平キーの道は消えた。古いディスクの鍵を読むのは `slMine()` |
| 765 | `lsWipeNS()` | `lsWipeAcct(uid)` |
| 765・997 | `netMember()` | `netSignedIn()` |
| 999 | `netAnonTok()` | 無い（匿名アカウントは無い） ── 打ち消し |
| 1003 | `netAnon()` | 同上 |
| 1004・1072 | `has_account()` | 無い。schema.sql から消えた ── 打ち消し |
| 1411 | `bkTake()` / `bkRestore()` | `netLangsDown()`。「戻す画面」はファイルと一緒に無い |
| 1793 | `planKeep()` | 無い。Keychain ごと `www/` から消えた（09-11）。段は `PLAN`、書くのは `planGot()` |
