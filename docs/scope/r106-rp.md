# r106-rp ── 「〇〇がリポスト」を名前の下に／字を描く画面の「?」を字の名前の横に

ブランチ `claude/r106-rp`（`integ-0905` から）。オーナーの言葉（2026-09-26）:
「vethの名前の下に〇〇がリポストって入れよう」「？はkの横にしよう」。決定ログに一項。

## 持ち物（これ以外は触らない）
- A: `supabase/schema.sql`（`feed_fo()`・`posts_by()` がリポストした人の表示名とハンドルを行に載せる）、
  `tools/rls-check.mjs`（その列の一行）、`www/net.js`（`netRow()` のみ）、`www/post.js`（`postRow()` の名前の行の下のみ）、
  `www/i18n/*.js`（鍵一つか二つ）、`tools/tl-check.mjs`（印の検査）、`tools/fixture.mjs`（リポストの行を描く顔が要れば）
- B: `www/glyph.js`（字を描く画面の `navTop` と、帯の下の「?」を消す所のみ）
- `docs/FEATURE_RULES.md`（決定ログ一項）、`docs/CHANGELOG.md`、この文書、`shots/r106-*`

## しないこと
- ゲートは回さない（fast と、作った検査・rls・関係する一つだけ）
- ビルドはしない
- 新しい道（ルート）・新しい「?」の置き方は作らない ── `go('profile', …)` と `helpQ()`→`navTop` の今ある物だけ
