# バグで消えたときに、運営側で戻すための案

「あとバグとかで人のデータが消えた時に運営側で復旧できる要素が欲しい」
「バグが多すぎて、セーフティーネットを作らないと炎上するだろ」
「復旧は自分で消した時じゃなくてバグで消えた時の話な」 OWNER 2026-09-03

**この文書は案です。まだ何も決まっていませんし、コードは一行も変えていません。**
残す期間、誰が戻せるか、いくらかかるか ── どれもオーナーが決めます。ここには
「期間によって何が変わるか」だけを書きます。

**線引き。**戻すのは**バグで消えたとき**だけです。本人が消したものは戻しません。
アカウント削除は本当に全部消えます。この文書の案は、どれもその線を越えません。

---

## 一、今、何がどこにあるか

数えたものです。数え方も添えます。

### サーバー（Supabase）── 表 15 個とバケツ 1 個

`supabase/schema.sql` の `create table if not exists` を数えて 15。

| 表 | 何が入っているか |
|---|---|
| `profile` | 人。@（handle）、表示名、顔（`av`）、自己紹介、@を最後に変えた日 |
| `language` | 言語そのものの行。名前、ライセンス、公開日、持ち主のアカウント |
| `slice` | **言語の中身。**`(language, kind)` で一行。`body` は端末の localStorage が持っている文字列そのもの |
| `publication` | 公開の記録。**追記だけ**（update も delete も方針が無い）。ただし中身ではなく sha-256 の指紋だけ |
| `prompt` | その日のお題 |
| `post` | 投稿 |
| `draft` | 下書き |
| `saved_search` | ★を付けた検索語 |
| `recent_search` | 打った検索語（5 件） |
| `plan` | 段（free / plus / pro） |
| `quote` | 他人の言語から引いた単語の出典 |
| `react` | いいねとブースト |
| `follow` | フォロー |
| `block` | ブロック |
| `report` | 通報 |

バケツは `post-media` の一つだけで、**公開**です。投稿の写真（`body.pu`）と
声（`body.vu`）のバイトがここに入ります。

### 言語は 12 のスライスでできている

`www/core.js` の `SLICES` を数えて 12 ── `words` `lines` `lang` `script`
`letters` `notes` `phases` `talk` `snd` `kb` `wld` `gram2`。
`slice` 表はこの 12 を一行ずつ持ちます。

### 端末（localStorage）── 書き込み口 29 か所

`tools/store-check.mjs` の `ROADS` を数えて 29。内訳:

- **20 か所はサーバーへの道がある**（`netSlicePut` `netPush` `netDraftUp`
  `netAvSync` `netNotices`）。つまりサーバーに同じものがある。
- **9 か所は端末のものです。**`lingua.set`（設定）、`lingua.langs`（この端末に
  どの言語があるかの索引）、`lingua.cur`（今どれを開いているか）、
  `lingua.sess`（トークン）、`lingua.<id>.bkn`（バックアップファイルの通し番号）、
  そして預けの三つ（`setParkKey` `meParkKey` `postParkKey`）と
  `phases.js` からの設定書き込み。

### 設定（`lingua.set`）の中身 ── 名前の付いた欄 24 個

同じファイルの `FIELDS` を数えて 24。**そのうちサーバーへの道があるのは 4 つだけ**
（`plan` `planPend` `saved` `recent`）。残り 20 はこの端末のものです。
その中に **`wsys`（書記体系）** があり、`tools/store-check.mjs` 自身が
「これは言語のものなのに設定に入っている、穴だ」と書いています。**サーバーに
無いので、消えたら戻す先がありません。**

### ファイル（iOS の Documents）── 三つのフォルダ

`ios/App/App/LinguaShare.swift` を読んで確認。

| フォルダ | 何 | 何世代 |
|---|---|---|
| `Documents/Languages/` | 言語まるごと（`bkPack()` が書いた JSON） | **3 世代**（`<名前> <id>.json` と `.1.json` `.2.json`） |
| `Documents/Voices/` | 録った声 | 上書きしない（同名は拒否） |
| `Documents/Sheets/` | 書き出した手書き用紙の PDF | 上書きしない |

**この 3 世代は、その端末にしかありません。**iOS の端末バックアップには入りますが、
運営側からは見えません。

---

## 二、今すでにあるもの

**新しく作る前に、あるものを数えました。八つあります。**

1. **`keep()` は最後の無事なファイルを壊さない**（`LinguaShare.swift:140`）。
   `.tmp` に書く → 読み直して JSON として通るか見る → そこで初めて世代を回す。
   書き損じは一世代で済み、人の何か月分は失われません。
2. **`bkTake()` は「無い」ものだけ埋める**（`www/backup.js`）。有るスライスには
   触りません。復元が勝つことでデータが死ぬ、という形を塞いでいます。
3. **`bkSound()` は「空」と「壊れている」を分ける**（`BK_SHAPE` と照合）。
   `[[[not json` は「有る」ので昔は素通りしていました。今は壊れているものは
   ファイルに書き出しません。
4. **`syMerge()` は両方足す**（`www/sync.js`）。どちらが新しいかを決めません。
   代償は重複で、重複は画面に出るので直せます。
5. **`netKeeps()` は縮む書き込みを断る**（`www/net.js`）。マージの結果が
   今あるものを含んでいなければ localStorage に書かず、`NET_SHRANK` に控えます。
6. **`netLangsDown()` / `draftsPull()` も「無いものを埋めて止まる」**。
7. **`bkDropFor(ids)` は名指しでしか消せない**。2026-09-03 に
   「フォルダを空にする」道が二つとも削除されました。
8. **`tools/del-check.mjs` が消す口を全部数えている。**削除に見える名前の act は
   **33 個**、そのうち**ストレージから何かを取るものは 21 個**、
   取らないものは 12 個。新しい口は理由を書くまで赤になります。

### 足りないのはどこか

**サーバーには、前の版がどこにも残っていません。**これが一番大きい穴です。

- `slice` は `(language, kind)` のプライマリキーで **upsert** されます
  （`netSlicePut()`、`Prefer: resolution=merge-duplicates`）。**`body` は
  上書きされ、前の中身は消えます。**言語一本につき、サーバーの版は常に一つです。
- `post` と `draft` も update で上書き、delete で消えます。行の履歴はありません。
- `publication` だけが追記専用ですが、**入っているのは sha-256 の指紋だけ**で、
  中身ではありません。「これは自分が先に作った」を示す証拠にはなりますが、
  戻すのには使えません。
- **バケツのバイトには履歴も版もありません。**削除は即、永久です。
- Supabase 側のバックアップ設定（PITR など）は `supabase/setup.md` に
  一言も書かれていません。**入っているかどうかは未確認です。**

そして、**運営側から人のデータを見る道が今は一つもありません。**
管理画面（`www/mod.js`、`admin_counts()`）にあるのは、人数・投稿数・言語数・
通報数の四つの数字と、通報された投稿を隠す・アカウントを止める、だけです。

---

## 三、消え方の種類

**このコードで実際に起こりうるものだけ**書きます。思いつきは並べません。
どれも「読んで確認した」か「走らせて確認した」かを添えます。

### 1. 端末の壊れた一本が、サーバーの無事な一本を上書きする　【走らせて確認】

`syMerge()` は、どちらかが JSON として読めないと **端末の側を返します**。
`netLangSync1()` はそれをそのまま `netSlicePut()` に渡します。

```
端末の words = [[[not json      サーバーの words = 単語 3000 個
   → syMerge は端末の壊れた文字列を返す
   → netKeeps(mine, put) は put===mine なので通る（そもそも比較にかからない）
   → put !== サーバーの body なので netSlicePut が飛ぶ
   → サーバーの 3000 語が [[[not json になる
```

`www/sync.js` と `www/net.js` の `netKeeps` を抜き出して走らせ、この四行が
そのとおりになることを確認しました。**バックアップファイルの側は
`bkOK()` が断るので壊れません**（`bkPush()`）が、サーバーには断る所がありません。
`bkSound()` は同期の道では一度も呼ばれていません。

**これが今、一番危ない道です。**端末のストレージが一本壊れるだけで、
サーバーの唯一の版が消えます。

### 2. 保存が黙って失敗する　【読んで確認・押していない】

`www/core.js:512` の `save()` は全体が `try{ … }catch(e){}` です。
localStorage が満杯、あるいは書き込みを拒否されたとき、**単語も行も名前も
`script` も設定も索引も、一つも書かれず、画面には何も出ません。**
`bkTouch()` は先に走っているのでバックアップは書かれますが、`bkPack()` は
localStorage を読むので、**書けなかった古い中身が書き出されます。**
`langStore()` も同じ形です（`www/core.js:184`）。

`savePosts()` だけは違い、失敗すると `toast(t('post.full'))` を出します。

### 3. 言語が「誰のものでもない」になって、消えていないのに全部から外れる　【読んで確認】

`LANGS[id].uid` が落ちるか、`mine` が false になると:

- `langOwned()` が false ── 一覧に出ない
- `langMineIds()` に入らない ── **同期されない**（上がりも下りもしない）
- `bkPush()` が `langMine()` で断る ── **バックアップファイルにも書かれない**

localStorage には全部残っているのに、見えず、守られません。
`bkTake()` のコメントがこの形を書いています ── 復元した言語に印を押さなかった
ために、`lingua.<id>.*` に一語も欠けずに入っているのに、どの一覧にも
どの数にも出なかった、と。**過去に実際に起きた形です。**

### 4. 索引だけ消えて、スライスが孤児になる　【読んで確認】

`langStore()` が失敗する（上の 2）と、`LANGS` の行が無いままスライスだけが
`lingua.<id>.<slice>` に残ります。索引に無い言語は誰も名前を言えないので、
バックアップにも同期にも入りません。サーバーに一度でも上がっていれば
`netLangsDown()` が拾い直しますが、**`sid` が無い言語 ── 一度も上がって
いない言語 ── は拾えません。**

### 5. 片方だけ消える　【読んで確認】

投稿の削除（`postDel`）は、サーバーの行・バケツのバイト・端末の写し・
`Documents/Voices/` のファイルを順に片付けます。途中で通信が切れれば
片方だけ消えます。言語の削除（`wipeLangsGo`）は `netLangDrop(id)` を
**待ちません** ── 端末からは消えて、サーバーの行が残ることがあります。

### 6. 一つのアカウントの削除が、別のアカウントのものを持っていく　【起きた】

2026-09-03 に実際に起きました。`lsWipeNS()` が `lingua.` で始まるキーを
全部持っていき、`bkDropAll()` が Languages フォルダを空にしていたので、
**二つ目のアカウントを消したら一つ目のアカウントの言語が消えました。**
その言語は一度もサーバーに上がっておらず、端末が唯一の写しを自分で壊しました。

**両方の関数は削除済みです**（`lsWipeAcct(uid)` と `bkDropFor(ids)` になった）。
ここに書くのは、**これが「バグで消えた」の実例であり、そして今の仕組みでは
一つも戻せなかったから**です。

### 7. サーバーの一行を、間違った中身で上書きする　【読んで確認】

1 の特別な場合だけではありません。`slice` は上書きの表です。マージの結果が
何であれ、`netSlicePut()` が飛べばサーバーの版は置き換わり、前の版はどこにも
残りません。**バグが一度でも変な `body` を作れば、その瞬間に唯一の版が消えます。**

---

## 四、案

三つあります。**一番勧めるものが先頭です。**

期間はどれも「オーナーが決める」です。案ごとに、期間で何が変わるかを書きます。

---

### 案A（推奨）── サーバーで、スライスの前の版を残す

**なぜこれを勧めるか:** 一番失って困るもの（何か月分の言語）が、サーバーに
**一つしか版が無く、起動のたびに上書きされる**から。そこに履歴を付ければ、
上の消え方 1・7 は全部戻せます。

**何を、どこに、どれくらい**

`slice` の `body` が書き換わる直前の中身を、別の表に一行として残します。

```
slice_past(language, kind, no, body, at)
```

Postgres の BEFORE UPDATE トリガ一本。アプリ側は一行も変わりません
（`netSlicePut()` はそのままで、トリガが勝手に控えます）。

**量の見立て。**`netLangSync1()` は、マージの結果がサーバーの `body` と
同じなら送りません（`put===got.body` で止まる）。**中身が本当に変わったときだけ
一行増えます。**無料の言語まるごとで約 25 KB、5000 語で約 685 KB
（`docs/DATA_SAFETY.md` の実測）。ただし `words` を一語足すたびに、
その時点の `words` 全部が一行として残ります ── **一語ごとに 685 KB です。**
これが期間で変わるところです:

- **短い（例: 数日）** ── 直近のバグは戻せる。オーナーが気づく前に流れる
  可能性がある。置き場は小さい。
- **長い（例: 数か月）** ── 「先月から単語が減っている」に答えられる。
  大きい言語を毎日書く人が増えると置き場が効いてくる。
- どちらにしても、**古い行を消す仕組みは自動削除**なので、
  `docs/DATA_SAFETY.md` の DELETE REVIEW を先に書く必要があります。

**誰がどうやって戻すのか**

オーナーは Supabase のダッシュボードを触れます。SQL エディタで、その人の
`language` と `kind` を選び、残っている版を見て、選んだ一行を `slice` に
書き戻します。押し間違いを減らすなら、`is_admin()`（`@lingua` だけ）で守った
`security definer` の関数を一つ足して、`slice_back(言語, kind, 版)` と
呼べるようにします。アプリに画面を作るかどうかは別の決定です。

**大きさ:** 小〜中。**サーバー側の作業:** 要る（`schema.sql` に足して流す）。
`npm run rls` を回す必要があります（`schema.sql` が変わるので）。

**この案で守れないもの**

- **サーバーに一度も上がっていない言語。**`sid` が付く前 ── オンボーディングの
  途中、扉に着く前、あるいは通信が一度も通っていない端末 ── のものは、
  サーバーに無いので履歴も無い。**上の消え方 6 は、この案では戻せません。**
- **端末側が壊れている場合、サーバーを直すだけでは戻りません。**`syMerge()` は
  読めない側を捨てずに端末の側を返すので、サーバーに良い版を書き戻しても、
  次の同期でまた壊れた版が上がります。**戻すときは、その端末の該当キーを
  一本消してもらう必要があります。**これはこの案の外側で、別に要る話です。
- 投稿・下書き・写真・声（案B と C の領分）。
- 端末だけのもの ── 設定 20 欄、`wsys`、`recent`、テーマ、表示言語。

---

### 案B ── 消える直前の行を、消えた置き場に写す

**何を、どこに、どれくらい**

`post` `draft` `language` `slice` の行が DELETE されるとき、BEFORE DELETE の
トリガで、その行を丸ごと jsonb にして一つの表に写します。

```
gone(表の名前, 行の id, 誰の, 中身 jsonb, いつ)
```

**アカウント削除は写しません。**`account_delete()` は `auth.users` を消し、
そこから全部カスケードします。オーナーの線 ──「アカウント削除は本当に全部
消える」── を守るため、この道だけはトリガを免除します。

期間で変わるところ:

- **短い** ── 「今日の午後に投稿が全部消えた」に答えられる。
- **長い** ── 「先週の下書きが無い」に答えられる。ただし、**消えた投稿の
  中身を長く持ち続けること自体が、人によっては嫌がられます。**これは
  技術ではなく、オーナーの決めることです。

**誰がどうやって戻すのか**

ダッシュボードの SQL で、`gone` から選んで元の表に insert し直します。
案A と同じく、`is_admin()` で守った関数を一つ用意すれば一行で呼べます。

**大きさ:** 中。**サーバー側の作業:** 要る。`npm run rls` も。

**この案で守れないもの**

- **バケツのバイト。**写真と声は行ではないので、行を戻しても
  `post-media` から消えたバイトは戻りません。投稿の削除は
  `netDropMine()` が先にバイトを消します。**戻るのは文と器だけで、
  写真の付いていた投稿は写真の無い投稿として戻ります。**
- アカウント削除で消えたもの（意図してそうしています）。
- 端末だけで消えたもの。DELETE がサーバーに届いていない消え方 ──
  上の 2・3・4 ── には何も効きません。
- **上書きで消えたものには効きません。**これは案A の領分です。

---

### 案C ── データベース全体を、ある時点に戻せるようにする

**何を、どこに、どれくらい**

アプリもスキーマも一行も変えません。Supabase 側の設定です
（Point-in-Time Recovery、または定期的なダンプ）。**今どうなっているかは
未確認です** ── `supabase/setup.md` に一言も書かれていません。
PITR は Supabase の有料の段が要ります。**どの段を契約しているかも
確認していません。**オーナーがダッシュボードで見る所です。

期間で変わるところ: 巻き戻せる幅がそのまま期間です。短ければ「今日」だけ、
長ければ「先週」まで。費用も期間で変わります。

**誰がどうやって戻すのか**

オーナーがダッシュボードから。ただし**全体が一つの時点に戻ります。**
一人のために全員を巻き戻すことになるので、実際には別のインスタンスへ
復元して、そこから必要な行だけ取り出して書き戻す、という手順になります。
**一人を助けるには手間がかかる道です。**

**大きさ:** 小（設定だけ）。**サーバー側の作業:** 要る（ダッシュボード）。
コードの作業はゼロ。

**この案で守れないもの**

- **バケツ。**PITR はデータベースの話で、Storage は別です。写真と声は戻りません。
- **端末だけのもの。**サーバーに無いものは、どの時点にも無い。
- **細かい復旧に向きません。**「この人のこの言語だけ」を戻すのに、
  全体の復元が要ります。
- 巻き戻す幅の外で起きたことは戻せません。

---

## 五、三つの関係

重なりません。**塞ぐ穴が違います。**

| | 上書きで消えた | 削除で消えた | 端末だけで消えた | 写真・声 |
|---|---|---|---|---|
| 案A | **戻せる** | 戻せない | 戻せない | ── |
| 案B | 戻せない | **戻せる**（器だけ） | 戻せない | 戻せない |
| 案C | 戻せる（全体） | 戻せる（全体） | 戻せない | 戻せない |

**三つとも「端末だけで消えたもの」に効きません。**設定の 20 欄と `wsys` は、
サーバーに道が無いので、どの案でも戻せません。ここを塞ぐのは
「サーバーに道を作る」という別の仕事で、復旧の話ではありません。

**写真と声のバイトを守る案はここにありません。**バケツに版も履歴も無く、
削除は即・永久です。要るかどうかはオーナーの決めることなので、
穴として書き出すだけにします。

---

## 六、確かめたことと、確かめていないこと

**確かめたこと（走らせた）**

- `syMerge()` が、端末の側が JSON として読めないときに端末の側を返すこと。
  `www/sync.js` と `www/net.js` の `netKeeps` を抜き出して走らせました。
  `netSlicePut()` が飛ぶ条件も同じ場所で確認しました。
- 数えたもの ── スライス 12、サーバーの表 15、localStorage の書き込み口 29
  （うちサーバーへの道 20 / 端末のもの 9）、設定の欄 24（道 4 / 端末 20）、
  削除に見える act 33（取る 21 / 取らない 12）。すべてコードとツールから
  数えました。

**確かめたこと（読んだだけ・押していない）**

- `save()` と `langStore()` が失敗を黙って飲むこと。
- 言語の印（`uid` / `mine`）が落ちたときに一覧・同期・バックアップの三つから
  同時に外れること。
- バックアップファイルが 3 世代であること、`mine` の言語だけであること。
- `post` / `draft` / バケツに履歴が無いこと。

**確かめていないこと**

- **実機では何も押していません。**この文書はコードとドキュメントを読んで
  書いたものです。
- Supabase が今どの段で、PITR が入っているか。ダッシュボードは見ていません。
- `slice_past` に一日どれくらい行が積まれるか。**使っている人の数を
  知らないので、見立てを書きません。**
- 端末のストレージが実際にどれくらいの頻度で壊れるか。
  `bkSound()` が作られた経緯から一度は起きたと分かりますが、回数は分かりません。
