# 次のリーダーへ ── 2026-09-24

書いたのは前のリーダー（session_01JrTLuTKfAqCnASgJb1ZjQJ）。会話が約52万トークンになり、
30分ごとに起きるたびにそれを全部読み直して上限を食うので、オーナーの指示で替わる。
**あなたは会話を短く保つこと。** 読むのは要る所だけ、長い出力は `head`/`grep` で切る。

最初に読む順: `CLAUDE.md` → `docs/LEADER.md` → この文書 → `docs/STATE.md`（古い文がある、下の 7）。

---

## 1. オーナーが今求めていること

- 2026-09-23 夜「マジでルールに細かく記載してあるんだから、全部洗いざらい出して直書き禁止、
  コードは太く長く直してくれ。並列回路を無限にしたみたいなキモい治し方やめてくれ。後関数だけ
  直すとか」「バグが多いのを一部分だけ治さを繰り返すからぐちゃぐちゃなるの対症療法なの」
  「マジで全部直せ」── 穴ではなく面を覆う（CLAUDE.md § COVERED）。
- 「開いた時は通知とタイムラインだけでしょ、そのページに進むときに読み込むべき」── r71 で入った。
- 「全部終わったらゲート回してビルドまで頼む」── ただし **ビルドはオーナーが Apple の手順 1〜2 を
  「済んだ」と言ってから**（`claude/owner-todo` の枝の OWNER-TODO）。言われるまで引かない。
- 2026-09-24「30分にする」── 監査は30分（`docs/LEADER.md`）。上限を気にしている。同時に走らせる
  作業は2本までにしておくのが安全（7日枠が警告、リセット 9/29 15:00 JST）。

話し方: 日本語、短く、画面の言葉で。**決定ログ（`docs/FEATURE_RULES.md` の頭）に答えがある事を
訊き直さない**（「前に話したよね？」「説明が適当なせいで往復増える」と言われた）。訊く時は画面で
何が起きるかを具体的に。測ってから言う。

## 2. 木の状態

- 統合の枝は **`integ-0905`**。先頭 `6493c391`（r71 まで）＋この文書のコミット。
- `master` は `a451c515`（1.0.2）。ビルドの時は `git checkout master && git merge --ff-only <integ の先頭>`
  → `git push -u origin master`（push は単独のコマンド、フックが止める）→ integ に戻る →
  `ios-deploy.yml` を起こす。
- 今夜取り込んだ: r59・r61〜r64・r66・r67・r69・r70・r73（監査の報告）・r75・r76・r77・r60・r65・r74・r71。
  各々の報告は `docs/scope/<名前>.md`（integ の上）。

## 3. 走っている／待っている作業

| 名前 | セッション | 状態 | やること |
|---|---|---|---|
| r79-acct | session_01XW3ovKesRxVZdEh5Txmqqx | 5時間の上限で止まり、04:50 に再開の合図を送った | 完了したら取り込む |
| r80-block | session_01Uwdf3CsckqqT2AYf5vygf9 | **完了、取り込み待ち**（ブロックはサーバーが `block_hides()` 一つで外す、rls 498/0） | 取り込む。schema.sql が変わる |

**次に出す順**（指示書は全部 `claude/leader-briefs` の `docs/scope/brief-*.md`、書き済み）:
1. **r78-sides** ── r79 が入ってから（post.js・sns.js・me.js が重なる）。読む側が作る側に触る・PUA が
   欄の外へ出る・r76 の残り・`sheet-check` と `press` の unworn（`.edit` `.ppr` `.tfont`）の赤。
2. **r68-state** ── r78 の後（ほぼ全部のファイルに触る）。「まだ訊いていない」を無料／空と読む面。
3. **r80 の端末側** ── `netBlocked()` の読みと端末で抜く所、`tools/load-baseline.txt` の `rest/v1/block`
   の行を消す。`docs/scope/r80-block.md` に書いてある。r68 の指示書に足すのが早い（net.js を持つ）。

出し方: `create_session`（source `integ-0905`、model `claude-opus-5-5`、tags `["lingua-0905","worker"]`、
permission `auto`、outcome_branch `claude/rNN-x`）、prompt は「`git show origin/claude/leader-briefs:docs/scope/brief-rNN-x.md`
を読んでそのとおりに。終わったら『rNN-x 完了』」の数行だけ。追加の指示は `create_trigger(persistent_session_id)`
→ `fire_trigger`。上限で止まった（`status_detail` に "session limit"）ら、リセット後に同じ形で再開。

取り込み: `git merge --no-ff origin/claude/rNN-x`。`docs/CHANGELOG.md` の衝突は両側を残す（`<<<`〜`>>>`
の間を両方つなぐ）。pre-commit が速い検査を回す。push は単独。取り込んだら `archive_session`。
**取り込む前に「持ち物外の変更」を見る**（`git diff --stat integ...rNN`）。

## 4. 全部入ったら

1. `npm test` を一回（最後の行に本数が出る）。赤は取り込み前の版で回して、どの取り込みで赤になったかを
   測ってから、持ち主のファイルを持つ作業に渡す。**00:10 の回では 4 本赤**: word・gramlang（r60 由来 → r79 が直した）、
   sheet（r76 由来 → r78）、press（`.edit` `.ppr` `.tfont` → r78、キーボードの `.note` → r74 が直した）。
   r71 の報告では `keep-check` も赤（r74 由来 → r79）。
2. `npm run rls` を一回（r65・r80 で schema.sql が変わった）。
3. `docs/STATE.md` を今に（リーダーのファイル）。偽と分かっている文: 987・1660 行あたり（`postCatchUp`、
   `netLangSync()` が起動で走る）、1047・1083 行あたり（同じ）。r60・r71 の scope の「持ち物外」を読むこと。
4. 朝の報告を一つ（下の 5・6 と、見せる写真）。**ビルドの手前で止める。**

## 5. オーナーの手が要ること

- **Supabase で `supabase/schema.sql` を流してから、`verify-plan` と `push-send` を置き直す**（逆だと
  verify-plan が 500）。手順は `supabase/setup.md` の 2026-09-24。r80 が入ればその分も含む。
- **今日のお題が 9/20 で止まっている** ── サーバー側。ここからは Supabase に届かない（プロキシが止める）。
  手順は `claude/owner-todo` の枝の OWNER-TODO 11。
- ビルドの前の Apple の手順 1〜2（同じ OWNER-TODO）。

## 6. オーナーに訊くこと（朝の報告にまとめる。決定ログに無いことを確かめ済み）

1. 後から直したほうが残る、の単位 ── r60 は「一度に送る単位ごと」（言語は slice、設定は項目、プロフィールは欄、下書きは一つ）で入れた。
2. 一覧の上限 `NET_PAGE = 50`（仮）。
3. フォロー中・フォロワーの並びが @ の順（新しい順にはサーバーに時刻の列が要る）。
4. 下書きの上限（`draftsPull()` が答えに無い下書きを端末から消すので、上限を付けると消える）。
5. アプリを開いて最初に出る画面が今は自分のプロフィール。タイムラインにするか。
6. キーボード: 手で作った短い行を保存の時に 10 まで埋めるか／結合キーの「真下」をどの位置で数えるか／長押し 10px をキーの持ち運びにも使うか。
7. ブロック: ブロックされた側からもフィード・スレッド・プロフィール・通知で見えなくするか（決定は「検索は両側」だけ）。
8. アカウント削除で残る行とファイル（`publication.actor`・`report.actor`・`feedback.author` の set null、post-media のファイル）。
9. 版と消し方の決定どうしの食い違い（期限なし と「3版だけ残す」、丸ごと戻す と 部分戻し、後勝ち(端末の時刻) と「番号はサーバーが配る。iPhone の時刻はもう要らない」）。
10. 送れなかった投稿を送り直すボタンが無い／下書きの声の置き場（R3）／`netDevicePut` を「押さずに書かない」の例外にするか。
11. `docs/scope/r73-audit.md` §5 の15項目、`docs/scope/r70-marks.md` の字だけのボタン 254 と右上の字のボタン 17、
    `docs/scope/r75-shape.md` の「オーナーへ」、`docs/scope/r76-lines.md` の「オーナーへ」（ウィジェットの見本と本物）。

**リーダーの読みで決めたもの**（オーナーの原文ではない、r79 の指示書に書いた）: 持ち主の無い古い写しは
誰の物にもしない（読まない、消さない）／通知をどこまで読んだかは `profile.prefs` で上がる（2026-09-01 の
Reason と 2026-09-03）／保存した検索の表より前の ☆ の引き渡しは消す。違うと言われたら直す。

## 7. 見せる写真（見た目が変わった物、`shots/` の上）

`r60-*`（ペンと紙飛行機、顔）・`r61-*`・`r64-*`（カード）・`r66-*`・`r69-*`・`r70-*`（印）・`r71-*`（⭕ のメーター）・
`r74-*`（キーボードの寄せ・無いキー・組）・`r75-*`（行の組・テーマ）・`r76-*`（暦・検索の行）。前後の対で見せる。

## 8. 前のリーダーの監査

前のリーダーの会話に30分の起こしが一本残っている（`30分監査`、05:21 UTC）。**あなたが立ったら
`list_triggers` で `30分監査` を探して `delete_trigger` し、自分の会話に `send_later` で30分の起こしを仕掛ける。**
