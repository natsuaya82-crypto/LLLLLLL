# 2026-08-26 の引き継ぎ

master は `9d851c3`。**ビルド #92 がここから出て、成功し、オーナーの実機に入りました。**
その実機で見つかった **25 件**と、その日に出たオーナーの決定を、ここに置きます。

この日、**子セッションが一本も立たなくなりました** — 指示の中身ではなく
「Claude Code を起動中」で落ちます。リーダーのセッションだけが生きていたので、
**その会話にしか無い状態を repo に落とすために**このファイルがあります。
次に読む人は、ここから始めてください。

新しいファイルです。**他のどの `docs/` とも衝突しません。**
片付いた項目は消さずに `済` と書いてください。**歴史は書き換えない。**

---

## 0. 宙に浮いているもの

```
master          9d851c3   ビルド #92（成功・実機確認済み）
claude/admin    +14       運営の管理画面 ＋ 匿名アカウントの削除。束ねていない
claude/dl       +38       腐った規則の掃除（docs だけのはず）。束ねていない
```

- **ゲート 26 本は、この二本については一度も回っていません。**
- `claude/admin` は `supabase/schema.sql` を触っています →
  **`npm run rls` が要ります**（`npm test` に入っていません）。
- ⚠ 枝が master に**どれだけ後ろにあるか**は
  `git rev-list --count <branch>..origin/master` です。**逆を見ると「追いついている」と
  誤報告します。**この日リーダーが実際にやりました。

### Google の iOS クライアント ID ── **2026-08-27 に入りました**

```
535150348007-i8roam4vdjjlfql5ktb4mld3v9chb6gr.apps.googleusercontent.com
Bundle ID   com.tokinets.lingua
同意画面    外部 / 本番公開済み / サポートメール lingua@tokinets.com
```
秘密ではありません。repo に入る値です。

**⚠ 以下は 2026-08-26 時点の警告で、2026-08-27 に両方とも解消しました。**
値は `www/net.js` と `Info.plist` の両方に手で入り、`tools/google-id.mjs` は
コメントを見なくなり、`-` は `CFBundleURLTypes` を**ブロックごと消す**ように
なりました（プレースホルダを書き戻すのが `ITMS-90158` の原因だったので）。
残しているのは、罠の形を次の人が読めるようにするためです。

**⚠（当時）`node tools/google-id.mjs <id>` を、今のこの repo で走らせないでください。**
`ios/App/App/Info.plist` に `CFBundleURLTypes` は**ありません**（build 86 が
`ITMS-90158` で蹴られたあと、意図的に消されています）。残っているのは
**コメントの中の書き方の例**だけで、ツールの正規表現は**その一行に当たります**。
走らせると `www/net.js` には本物が入り、plist は**コメントの中が書き換わるだけ**で、
`Both written.` と表示して exit 0 — **ツールが防ぐために存在している「片方だけ」を、
ツール自身が作ります。**

やること: 手で二箇所（`www/net.js:73` の `GOOGLE_IOS_ID` と、plist に**本物の**
`CFBundleURLTypes`。scheme は前後を入れ替えた
`com.googleusercontent.apps.535150348007-i8roam4vdjjlfql5ktb4mld3v9chb6gr`）。
plist のコメントも直すこと — 「ここには無い、それは意図的だ」が嘘になります。
そして `tools/google-id.mjs` 自体の修正は**別コミット**で。
Supabase 側（Authentication → Providers → Google）は**オーナーの手**です。

---

## 1. オーナー決定 2026-08-26

決定ログ（`docs/FEATURE_RULES.md`）にまだ入っていないものが含まれます。
**入れるときは、言葉のまま引用してください。**

### サーバーの範囲
```
基本は全部サーバー管理 言語周りだけバックアップにfile使う
制作はオフラインでも可能次つながった時に更新される
```
```
Xがローカル保存してんの？nolaとかの目もアプリもそういう話してんの
```
同期の頻度とプランは「全部だって」＝ **常に同期 / プランに関係なく全員サーバに載る**。
例外: **DL した読み取り専用の言語は同期しない**（`syMerge` は両方足すので、
足された瞬間トキポナがトキポナでなくなる）。

### アカウント
```
言語はアカウントないと作れないです
ログインした人しか書けないけど
二種類になる意味も分からないけど          ← has_account/is_member の分割を廃止
SNS部分はオフラインでは動かないよそりゃそう
アカウント消したら残るわけがないあほだろ
```
**2026-08-22 の「匿名優先・扉はオンボーディングから外す」は置き換えられました。**
実装は `claude/admin`（+14）にあります。

### ログアウト中の画面
```
ログアウトしたら普通にログイン画面だけ出せばいいやろ。
それ以外は表示させるな。
ログインしてないのに謎に課金できるしバカやろ。
他の画面に行かせるな。ログアウトの時は。
```
**一箇所で決めること** — `render()` が経路を引く所。画面ごとに `if(!netSignedIn())` を
足していく形は必ず足し忘れが出ます。今まさにそれで、設定・プラン・言語の画面が
素通しになっています。

### プランのボタンは囲う（規則18 の例外）
```
11は、角丸でいいから囲わないとボタンを押してるかわからん
```
**「角丸やめろ」の、プランの画面だけの例外。** 訊き直さないこと。
⚠ **新しい CSS を書く必要はありません。** `.btn`（角丸で囲われた既存クラス、
約30画面が着ている）を着せれば、`tools/box-baseline.txt` も
`corners and borders 111 (baseline 111)` も一行も動きません。`.btn.ghost` は逆で、
`.ghost` が囲いを外すほうです。

### 値段
```
4はドル。でもさっき価格登録してきたけど日本円は800円とかになってたよ
```
月 **$4.99**、年 **$49.99**、取り消し線の元値 **$59.88**、**17% OFF**。
⚠ **ハードコードしないこと。** App Store が国ごとに値段を決めます。
`ios/App/` に StoreKit は書かれていて（`ec00697`）、**www から一度も訊いていません**。
`storeOn()` / `storeBuy()` / `storeId()` が `settings.js` にあります。

### キーボードの段の上限
```
キーボード縦8のやつなんか見たことねえよ。
```
**縦 8 → 5**（QWERTY＋数字の段が、アプリが自分で作る一番高いもの）。
⚠ **既に 5 段を超えている板は一段も動かさないこと。** 規則19 ——
上限は「足す」ときだけ効きます。`kb-check` が9段の板を入れて「何も動かない」ことを
見ています。**その検査を弱めない。**

### 通報ボタン
設定の中のものだけ消す。**投稿ごとの通報と、`claude/admin` の通報キューは残す。**

### 語順
```
俺も選ばせたくないし、文章書いてたらsvoが基本でも助詞があるかもしれないし、
好きに書かせて幅広げた方が良くねえか
```
```
自動翻訳をaiが不必要なレベルまで押し上げたい。
例えば文法とか単語とか埋めてったら勝手に翻訳されてるみたいな精度も高い！みたいなね
```
`www/grammar.js:27` の `ORDERS`（六択）は外す。
⚠ **`STG.order` は人が設定した値です。読まなくなるのと消すのは別。**
消すなら DELETE REVIEW が要り、それはオーナーのものです。

---

## 2. 実機 #92 で見つかった 25 件と、territory の切り方

一本の枝が一つの塊を持ちます。**`www/index.html` を持つのは ② だけ** ——
全画面の CSS が一本に入っている既知の地雷なので、同時に二人入れてはいけません。

`www/i18n/*.js` と `www/act-map.js` は**全部の枝が触ります。**
**足すのは、そのまとまりの末尾に足すだけ。** 既にある鍵を並べ替えない・消さない・
書き換えない。そうすれば衝突しても機械的に両方取れます。

### ① ログアウト中は扉だけ — `claude/door`
持ちもの: `www/shell.js` `www/onboard.js` `www/sns.js`

- ⑬ ログアウト後、文字をタップしたあと**戻るボタンが効かない**
- ⑭ ログアウトしているのに**言語設定の画面が出る**
- ⑮ **扉以外を出さない。課金に行かせない**（上の決定。一番大事）
- ⑯ **ログイン画面が固定のはずがスクロールする**（Apple・Google・登録リンクで背が伸びた。
  `obDoorHTML()` を測ること。CSS を足さずに収まるならそちらを先に）
- ⑰ **「パスワードをお忘れですか？」が押せない**
- ⑱ **メアド／パスワードが違う時、赤文字で出ない**
  （これは「説明」ではありません。規則がそう書いています。ただし `t()` を通し、
  文言はオーナーに直接訊く）

⚠ `claude/admin` が `net.js` `boot.js` `onboard.js` を触っています。
匿名が無くなると `netSignedIn()` と `netMember()` は**同じ意味になります。**

### ② 投稿画面 — `claude/compose`
持ちもの: `www/post.js` **`www/index.html`**

- ① **「+ から入ったのにお題になる」**
  「+ は毎回まっさらな投稿」。`openPost()` の頭のコメントが既に同じ形のバグ（返信が残る）を
  書いています。**お題（`PW.pr`）で同じことが残っています。**
- ② **中身がある時に「下書きに保存するか」を訊かない**
  `shell.js:123` のコメントは「下書きに入れて composer を空にする」と書いています。
- ③ **お題は機能として生きる。**①と矛盾しません（お題はお題の入口から。+ は白紙）
- ④ **アイコンを押したらその人へ**（タイムラインも検索も。自分ならプロフィール）
  ⚠ `postRow` は `post.js` の**線の下**。規則8 — 下から `ME` `meName` を名指ししない。
  誰のものかは**投稿に乗っている**。`sides-check` が見ています。
- ㉑ **キーボードが下ろせる／`∧ ∨ ✓` の帯がいらない**
  ⚠ **iOS の入力アクセサリかもしれません。まず測ること。**消せないなら「消せない」と
  報告する。推測で「消しました」と言わない。
- ㉒ **返信のとき自分の欄が狭すぎる**
- ㉓ **カメラ・画像・マイクの行がキーボードにくっついていない**（二度目の指摘）
- ㉔ **マイクの横に下書きのボタン**
  オーナー決定: **「キーボードにくっついた行の中」。画面の底ではない。**
  ⚠ `post.js:160` にオーナーの前の言葉が残っています ——
  「だから save a draft を底に置くのやめろって」。**矛盾しません。**決定ログに新旧を並べること。
- ㉕ **「下書き1」「下書きに保存」が無くなった** → `pwSideHTML()` は生きていて、
  **出る条件が狭いだけ**（何も書いていない＆下書き0本で消える）。㉔ と同じ一件。

### ③ キーボード — `claude/kb2`
持ちもの: `www/keyboard.js` `www/share.js` `ios/App/LinguaKeyboard/*.swift`
`tools/kb-check.mjs`

- ⑥ **フリックなのに QWERTY の大きさ**。⚠ アプリの編集画面の話か端末の拡張の話か、
  **先に確かめること。**規則19 の「横10」は QWERTY の話で、フリックは 5 列
- ⑦ **2ページ目に行くボタンはあるのに、戻るボタンがない**
  ⚠ **既に書かれている規則19 に違反しています**（「ページは行き方と戻り方の両方を
  最初から持って現れる」）。`kbDefault()` は正しく、**人が面を足したときの道が間違い**。
  `kb-check` にこの主張を足すこと
- ⑧ **縦の上限 8 → 5**（上の決定）。**2ページ目で行が足せないのはバグ**
- ⑨ **2ページ目を作っていない板なのに、端末で QWERTY の2ページ目が出る**
  `share.js` が渡すものか拡張の既定値か。⚠ **拡張は実機でしか確かめられません。**
  `docs/keyboard-extension.md` の教訓 —「まず画面に状態を出せ」。四回ビルドを溶かした話です

### ④ 設定とプラン — `claude/plan2`
持ちもの: `www/settings.js` **のみ**（`index.html` は ② のもの。`.btn` を使うので不要）

- ⑩ **設定の通報ボタンを消す**（設定の中だけ）。`dead-check` が余りを見つけるので一緒に消す
- ⑪ **プランのボタンを囲う**（上の決定）＋ **「4.99/月」を一行に**
- ⑫ **年額をお得に見せる**（取り消し線＋17% OFF）。**値段は StoreKit から**（上の決定）
  取れないとき何を出すかは**オーナーの判断**

### ⑤ 言語のページ — `claude/world2`
持ちもの: `www/home.js`

- ⑲ **言語設定から「言語のページを公開する」「文字と単語をダウンロードできるようにする」を外す**
  ```
  ここの言語ページを公開すると単語と文字dlできるようにするはいらない。wikiでできるから。
  ```
  ⚠ **外すのは「この画面から」であって「機能を消す」ではありません。**
  `claude/wiki` が同じスイッチを「この言語について」の編集画面に作りました（master に入っています）。
  **直す前に、二つが同じものを書いているか確かめること**（`wldSecHide` / `wldSecDl` /
  `world().secs`）。**違うなら、外した瞬間にオーナーの設定が消えます。**その時は外さずに報告
- ⑳ **「この言語について」は全部たたまれた状態で開く**
  ```
  この言語については初手は全部閉じてくれ。人が見て開くのよ閉じたらまた閉じるし。
  ```
  ⚠ 開いた／閉じたを**言語の側（`world()`）に保存しない。**それは「どこに立っているか」で、
  `viewReset()`（`shell.js`）のもの。**保存しないのが一番簡単で、たぶん正解**

### ⑥ 文法のページ — `claude/gram2`
持ちもの: `www/phases.js` `www/grammar.js` `www/grammar-engine/*.js`
`tools/grammar-engine-check.mjs` `tools/gramlang-check.mjs`

オーナー: 「文法のページはこれで完璧なの？作り直したやつは？どこいったん？」

**答え: 中身は新しく、画面が古いままです。**
```
www/grammar-engine/  CASE・Semantic IR・派生 —— 全部 master に入っている
www/phases.js:580    vGram()      ← 画面。作り直していない
www/phases.js:164    STAGES       ← 段
www/grammar.js:27    ORDERS       ← 語順の六択。まだ生きている
```
⚠ **いきなりコードを書かないこと。**画面が何をするかはオーナーのものです。
先に「今どうなっているか」を一枚で出して、オーナーに直接見せて決めてもらう。

⚠ **`tools/dead-check.mjs` に穴があります**（`docs/BACKLOG.md`）。宣言を拾う正規表現が
行頭アンカーで、`www/grammar-engine/` の5ファイルは IIFE に包まれて全部字下げ ——
**関数 54 個が丸ごと見られていません。**「`dead-check` が緑だから死んでいない」とは
言えません。直すのは別の日。

---

## 3. リーダーがこの日やった間違い

次の人が同じ形で踏まないように、形だけ残します。

1. **要約を見て、コミットを読まずにセッションを止めた。** `git show <sha>` を先に。
   取り消しに2コミット掛かりました
2. **データの表だけ読んで、描画を読まずに「合っている」と言った。**
   「dl可もあってないやん。俺の文章読んでる？」
3. **オーナーが既に答えたことを三回訊き直した。**
   「それもふるいわ いつまでふるいのずっとやってんだよ」
   → **止まるのは、オーナーがまだ何も言っていないときだけ。**
4. **いない人のために移行を書かせようとした。** アプリは一度も公開リリースされていません
   （TestFlight のみ）。**「今いる人のために」が要る場面は今日この repo にありません**
5. **枝が master にどれだけ後ろにあるかを、逆向きに数えた。** 二本とも「追いついている」と
   誤報告しました（実際は 35 と 46 後ろ）
6. **territory を二重に配った**結果、鍵の無い `t()` が枝に載り、
   **CI の失敗メールがオーナーに4通飛びました**
7. **文法のページの作り直しを、誰にも渡していなかった。** エンジンだけ作らせて、
   画面を忘れました

---

## 4. 数（master `9d851c3` = ビルド #92）

一本ずつ突き合わせること。**「赤が無い」では足りません** ——
`fill`/`round` が module load で死んで、上が全部緑に見えた前例があります。

```
buttons pressed              10760  (230/230 distinct names)
screens walked               425    routes reached 37/37
screens the mirror rendered  302
i18n                         en 903 鍵 × 10言語、unaccounted for 0
dead code                    1431 functions, 351 top-level vars
corners and borders          111 (baseline 111)   set from www/*.js: 0
classes worn                 588、styled and unworn 4 (baseline 4)
rows in one list             2277 lists measured
```
`buttons pressed` は #91 で 10723 (229/229)。**+37・名前+1 を動かしたのは
`claude/wiki` の `92878fa`**「記事のキーボードに名前を付け、無料の板も出す」。

**`ios/App/` は #91 から一行も動いていません** —— 木のハッシュが同一（`220ff92…`）。
125 コミットは全部 `www/ tools/ docs/ supabase/ .github/` 側でした。

---

## 5. 環境について（この日わかったこと）

- **子セッションが一本も立ちません。**「Claude Code を起動中」で
  `An error occurred while executing Claude Code.` になります。
  **指示の中身は関係ありません** —— 全部が同じ所で落ちます
- **`master` への `git push` は permission classifier に止められます。**
  サブリーダーが二人ともそこで固まって死にました。オーナーが承認すれば通ります
- **`create_trigger` をセッションから呼ぶと、許可待ちのまま固まります。**呼ばせないこと
- `.github/workflows/i18n.yml` は master と PR だけに絞りました。ただし
  **GitHub は push されたコミット側の workflow を読む**ので、それより前に切られた枝は
  `branches:['**']` を自分の中に持ったままです。**止まるのは枝ごと、追いついた順**
