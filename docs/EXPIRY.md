# 人が作ったものに期限を付けている仕組み ── 一覧

OWNER 2026-09-04「基本一生残るよな」（`docs/FEATURE_RULES.md` § 人が作ったものに
期限は無い、コミット `d7d14e6f`）と、CLAUDE.md の「Data」──
*automatic deletion, pruning and cleanup are forbidden unless a written spec
asks for them* ── に照らして、いまのアプリを全部見た結果です。

**元は調べただけの一覧で、一行も直していませんでした。**
`www/` `ios/` `supabase/` `.github/` `tools/` を読んだものです。

**2026-09-04、`claude/keep3` が 3-a・3-b・4番を引き取りました。**
直した項目には ✅ を、確かめたが直していない項目には ⚠️ を、
オーナーのものは 🔒 を見出しに付けてあります。**印の意味は一つずつの節に
書いてあります ── 「調べた」と「走らせて確かめた」と「直した」は別の文です。**

| 印 | 意味 |
|---|---|
| ✅ | 走らせて赤を見て、直して、緑を見た。**実機では押していない** |
| ⚠️ | 走らせて確かめた。**直していない**（理由は節の中） |
| 🔒 | オーナーのもの。手を出さない |
| ── | 仕様どおり。直すものではない |

読んで確かめたものと、実際に走らせて確かめたものと、確かめていないものを
一件ごとに分けて書いてあります。**実機では一つも押していません。**
`npm` のチェックは一本も回していません。

---

## 先に、無かったもの

**日付や経過時間で消える仕組みは、どこにも一件もありませんでした。**

- `www/` の中で「今」から時間を引いている所は 5 か所だけで、全部が画面の都合です
  ── 長押しの判定（`www/shell.js:1124`）、コード再送信の秒数
  （`www/onboard.js:967`）、録音の長さ（`www/rec.js:148`）、
  「何分前」の表示（`www/post.js:2497`）、オンボーディングの見本
  （`www/onboard.js:1682`）。
- `supabase/schema.sql` に、行を消すトリガも cron もありません。トリガは三本
  （`profile_first` `profile_rename` `profile_follows`）で、どれも書くだけです。
  `delete from` は一行だけ、`account_delete()`（1668行）の中の
  「自分のアカウントを消す」です。
- `supabase/functions/daily-prompt/index.ts` は書くだけで、消しません。
- `.github/workflows/` は検査だけです。何も消しません。

以下は、**数**または**上書き**で前のものが無くなる所です。

---

## 1. キーボードを一度も作っていない言語は、バックアップファイルが書かれなくなる

**何が、どういうときに消えるか。**
その言語のバックアップファイル（iPhone の中の `Documents/Languages/`）が、
**それ以降ずっと書かれなくなります。** 単語も文字も、後から足したものは
ファイルに一度も入りません。サーバーとこの iPhone の両方が駄目になった日に
残っているのは、止まった時点の古いファイルだけです。

起きるのは、キーボードを一度も組んでいない言語から**離れたとき**です ──
言語を切り替える、言語を消す、アカウントを消す。

**どこにあるか。**

| | |
|---|---|
| `www/keyboard.js:137` | `saveKb()` が `JSON.stringify(KB)` を書く。KB が空のときは文字列 `"null"` になる |
| `www/keyboard.js:44` / `:83` | キーボードを組んでいない言語では `KB` は空（`kbRead()`） |
| `www/core.js:185` `:530` | 言語を切り替えると `langSaveAll()` → `saveKb()` |
| `www/settings.js:519` `:684` | 言語を消したとき・アカウントを消したときも同じ |
| `www/keyboard.js:3634` | `kbResetGo()`（キーボードを初期状態に戻す）も同じ状態を作る |
| `www/backup.js:82` | `bkSound('kb', 'null')` は「壊れている」と答える |
| `www/backup.js:183` | だから `bkPush()` が「書かない」で止まる |

**それは人が作ったものか。** はい。言語まるごとです。

**書いた仕様はあるか。** ありません。
`docs/DATA_SAFETY.md` は逆のことを言っています ──
「A wipe, a switch and a brand new language all produce an empty slice and all
of them are written」。空の言語は書かれるはずのものです。
`tools/backup-check.mjs:434` の「空の言語」の場面は、スライスのキーを**消して**
から試すので、アプリが実際に書く `"null"` という形は試されていません。

**確かめたこと。** `www/backup.js` の `BK_SHAPE` `bkSound()` `bkOK()` を
そのまま取り出して走らせ、`bkSound('kb','null')` が `false`、
kb に `"null"` が入った荷物で `bkOK()` が `false` になることを確認しました。
残りはコードを読んだだけです。**実機では押していません。**

**直す大きさ:** 小。空のキーボードを `"null"` ではなく空の物として書くか、
`bkSound()` が `null` を「無い」として扱うかの、どちらか一行です。
**どちらにするかはこのセッションが決めることではありません。**

---

## 2. 🔒 バックアップファイルは三世代。四回前の保存は消える

**何が、どういうときに消えるか。** 言語を保存するたびに、いちばん古い
バックアップファイル（三つ前）が消えます。三回続けて壊れたものが書かれると、
最後の無事なものが押し出されます。

**どこにあるか。** `ios/App/App/LinguaShare.swift:165-173`（`keep()`）。
消えたときの話は `docs/RECOVERY.md:75-92`。

**それは人が作ったものか。** はい。言語まるごとです。

**書いた仕様はあるか。** **あります。**
`docs/DATA_SAFETY.md` §「1. A write never destroys the last good file」と、
同じ文書の DELETE REVIEW の例外の一つ目
（「`keep()` rotating a generation out ... is the price of rule 1」）。

**直す大きさ:** ── （仕様どおりです）

---

## 3. 移行が、読んだものを消している所が二つ ── ✅ 二つとも直った

CLAUDE.md は「A migration **copies** and never removes what it read」と
言っています。次の二つは消していました。**どちらも直っています。**

### 3-a. ✅⚠️ 取り込んだ発音が消え、次の起動で推測に置き換わる ── 扉が二つあり、一つはまだ開いている

**何が、どういうときに消えるか。** CSV から取り込んだ単語の発音
（`w.ph`）が消えます。次に起動したとき、綴りからの**推測**が代わりに
書き込まれます。綴りを letters で組み直した単語だけが対象です。

**どこにあるか。** `www/letters.js:1265`（`migrateSp()` の `delete w.ph;`）。
書き戻しは `www/core.js:1648`（`migratePh()`）。
取り込みで入る所は `www/import.js:818`。

**それは人が作ったものか。** はい。その人が用意した表の一列です。

**書いた仕様はあるか。** ありません。

**直す大きさ:** 小。

**✅ 直りました。**`027b0e9e`（`master` に入っています。この一覧を書いた
セッションではなく、別のセッションの仕事です）。`migrateSp()` から
`delete w.ph;` を一行削っただけで、条件は足していません。
「2発音は消えないでくい」OWNER 2026-09-04。

`claude/keep3` が 2026-09-04 に走らせて確かめました:

| | |
|---|---|
| `master` のまま | `migrate-check` 緑 |
| `delete w.ph;` を戻す | **赤三行** ── 一回目の起動で `ph` が空、二回目で `"k a n o"` |

`"k a n o"` は `phGuess()` が見出し語の綴りから作った推測です。
**無くなるのではなく、その人が入れたものに見える別の答えに置き換わります。**
`migrate-check` がこの項目を持っています。**実機では押していません。**

### 3-a-2. ⚠️ 同じことが、単語シートの保存でも起きる ── 確かめた。直していない

**`claude/keep3`、2026-09-04。`www/wordsheet.js` は `claude/pop` のものなので、
一行も触っていません。**リーダー待ちです。

**3-a は「移行が消す」という一件でしたが、同じことをする道がもう一本あります。
こちらは移行ではなく、人がボタンを押したときです。**

**何が、どういうときに消えるか。** 取り込んだ単語を単語シートで開いて
**保存を押すと、その人が表に打った発音が消えます。**次の起動で、
綴りからの推測が代わりに書き込まれます。**発音はその画面に一つも出ていません**
── 意味を直しただけでも、何も直さずに保存を押しても起きます。

**どこにあるか。**

| | |
|---|---|
| `www/wordsheet.js:1533` | `wdWrite()` の `w.hw=hw; delete w.ph;` |
| `www/core.js:1653-1659` | `migratePh()` が、`ph` が空なら `phGuess(w.hw)` を書き込む |
| `www/boot.js:31` | `migratePh()` は**毎起動走ります**（済みの印が無い） |
| `www/import.js:818` | 表の発音の列が `w.ph` に入る所 |

**確かめたこと ── 本物のアプリを走らせています。**取り込んだ単語
（`spv:1` を付けてあるので `migrateSp()` はこの単語に何もしません ──
**3-a とは別の道であることの確かめ方です**）に発音 `/t sʰ ɑ ŋ/` を入れて:

```
取り込んだまま、起動しただけ        ph = "t sʰ ɑ ŋ"
単語シートで保存を押した（landed = true）
  そのすぐあと                     ph = ""
  次の起動のあと                   ph = "k a n o"
```

`"k a n o"` は `phGuess("kano")` が見出し語の綴りから作った推測です。
**無くなるのではなく、その人が入れたものに見える別の答えに置き換わります。**
無くなれば気づけますが、これは気づけません。3-a と完全に同じ形です。

**半分ずつ、別の人のファイルにあります。**

- **消す半分** `www/wordsheet.js:1533` ── `claude/pop` のもの。
- **推測を書く半分** `www/core.js` の `migratePh()` ── `claude/keep3` のものだが、
  **こちらを変えるのは振る舞いの判断です。**コメントに書いてあるとおり
  「音の表より前の単語に並びを持たせる」という仕事があります。
  勝手に止めません。

**もう一つ、見てほしいこと。** `wPh(w)`（`www/core.js:1597`）は、`ph` が空のとき
**読む側で `phGuess()` を返します**。つまり画面に出すだけなら `migratePh()` が
書き込まなくても同じものが出ます。書き込むことで増えるのは、
**その推測が人の打った値と見分けられなくなり、バックアップとサーバーにまで
上がる**ことだけです。これをどうするかはオーナーかリーダーの判断だと思います。

**検査はまだ足していません** ── 直せないまま赤がゲートに残るためです。
上の三行を出した台本は `docs/reports/keep3-ph2-2026-09-04.md` に丸ごと置いてあるので、
直す人がそのまま `migrate-check` へ写せます。**実機では押していません。**

### 3-b. ✅ 古い形の文字が持っていた `key` が、移し替えられずに消える ── 直った

**何が、どういうときに消えるか。** 古い形で保存された文字の `role` と `key`
が消されます。`role` が `mark` で、かつ読みがまだ空のときだけ `key` は
`snd` に移されます。それ以外は移されずに消えます。

**どこにあるか。** `www/letters.js:426`（`migrateMarks()` の
`delete l.role; delete l.key;`）。

**それは人が作ったものか。** はい。文字が何を読むかです。
`key` は**その文字を打つ文字**で、`role` は `mark` か `snd` かです。

**書いた仕様はあるか。** ありません。

**直す大きさ:** 小。

**✅ 直りました。**`claude/keep3`、2026-09-04。`migrateMarks()` から
`delete l.role; delete l.key;` を一行削っただけで、移す条件は一文字も
変えていません。

**赤で見えたこと**（`migrate-check` に項目を足し、直す前に走らせた）。
古い二欄の形の文字を二つ置いて、起動を二回:

| 置いたもの | 読み | `key` | `role` |
|---|---|---|---|
| `mQ` `role:'mark'` `key:'?'` `snd:[]` | `?` になった（移せている） | **(deleted)** | **(deleted)** |
| `mB` `role:'mark'` `key:'¶'` `snd:['b']` | `b` のまま（移さない判断は正しい） | **(deleted)** | **(deleted)** |

赤五行。**保存された letters スライスの中にも `¶` は残っていませんでした。**
文字そのものは二つとも残ります（消えるのは欄だけです）。

**戻す先が無いのが、この一件の重さです。** `role` と `key` を読む所は、この
移行のほかに `www/` の中に一つもありません（`grep -n "\.role\b\|\.key\b"
www/*.js`）。だから一度消えると、iPhone からも、バックアップファイルからも、
サーバーの `slice` 行からも、同じ一手で無くなります。

残る二欄は誰も読みません。行き先の無い値を移行が置いていく、という形です。
**実機では押していません。**

---

## 4. ⚠️ 文法のスライスが無い言語に、空の `{}` が書かれる ── 確かめた。直していない

**何が、どういうときに消えるか。** 直接は何も消えません。
**「無い」が「有る」に変わる**ので、そのあと

- バックアップファイルからの穴埋め（`www/backup.js:274`、`bkTake()`）
- 他の iPhone から降りてくる穴埋め（`www/net.js:1168`、`netLangBack1()`）

が、その言語の文法を埋めなくなります。`docs/DATA_SAFETY.md` の
「A slice the app has never written is **absent**, and absent is what a restore
is for」が効かなくなる、という形です。
サーバーとの同期（`www/net.js` の `netLangSync1()` → `www/sync.js` の
`syMerge()`）は両側を足すので、**電波があれば戻ります。**
戻らないのは、電波が無い・アカウントが無い iPhone です。

**どこにあるか。** `www/phases.js:98-125`（`migrateGramLang()`）。
`raw` が無いときも `o={}` のまま `localStorage.setItem` まで進みます。
`SET.gramLang` で一度きりです。

**それは人が作ったものか。** 文法（`phases`）は人が作ったものです。
書かれる `{}` そのものは人のものではありません。

**書いた仕様はあるか。** ありません。

**直す大きさ:** 小。

### ⚠️ 走らせて確かめました。直していません ── 直す一行が territory の外です

`claude/keep3`、2026-09-04。**`www/phases.js` は `claude/keep3` に渡されて
いないので、一行も触っていません。**リーダー待ちです。

**誰も入っていません。**`origin/master`（`d4b46ab4`）を基準に、生きている
二つの枝はどちらも `www/phases.js` に一つもコミットを持っていません:

```
git rev-list --count origin/master..origin/claude/pop   -- www/phases.js   → 0
git rev-list --count origin/master..origin/claude/post3 -- www/phases.js   → 0
```

**だから渡すのに衝突の心配はありません。**下の一行を誰かに渡すか、
`claude/keep3` に `www/phases.js` を渡してくれれば、すぐ入れます。

**確かめかた。** `migrateGramLang()`（`www/phases.js:98-125`）を、
`www/grammar.js` の `ORDERS` と `GPOS_DEF` ごとそのまま Node に取り出して
走らせました。文法の質問に一度も答えていない iPhone（`SET` が空）で:

```
before  lingua.LA.phases = null
after   lingua.LA.phases = "{}"
```

**そして、それが穴埋めを止めることも走らせて確かめました。**
`bkSound()` を `www/backup.js:68-90` からそのまま取り出して:

```
その言語が phases を一度も書いていない  bkSound('phases', null) = false
migrateGramLang が {} を押したあと      bkSound('phases', '{}')  = true
```

`bkTake()`（`www/backup.js:266-277`）は **true と答えたスライスを飛ばします**
── 「その場にあるものを上書きしない」が復元の規則だからです。だから
`{}` が押されたあとは、バックアップファイルからも他の iPhone からも、
その言語の文法は二度と埋まりません。
`docs/DATA_SAFETY.md`「A slice the app has never written is **absent**, and
absent is what a restore is for」が効かなくなる、という形です。

**電波があれば戻ります**（`syMerge()` は両側を足すので）。戻らないのは、
電波が無い・アカウントが無い iPhone です。

**直す一行（提案。`www/phases.js` を持っている人へ）。**
`localStorage.setItem` の直前に一行:

```js
      /* 写すものが何も無かった言語は、前も無く、後も無い。
         「無い」と「空」は別の状態（CLAUDE.md § Data）。 */
      if(raw===null && o.order===undefined && o.gpos===undefined) continue;
```

**この一行を入れて四通り走らせました。止まるのは一つだけで、写す道は三つとも
一文字も変わりません:**

| 置いた状態 | いま | 一行を入れた後 |
|---|---|---|
| phases が無い・写すものも無い | `"{}"` | **書かれない** |
| phases が無い・語順を写す | `{"order":"SOV"}` | `{"order":"SOV"}` |
| phases が無い・位置を写す | `{"gpos":{"adj":"before"}}` | `{"gpos":{"adj":"before"}}` |
| phases がもうある | `{"done":{},"order":"SOV"}` | `{"done":{},"order":"SOV"}` |

**検査はまだ足していません。**`migrate-check` に足すと、直せないまま赤が
一本ゲートに残ります。`www/phases.js` を持つ人か、この一行の許しが出たら、
同じコミットで足します。**実機では押していません。**

---

## 5. 🔒 ★を付けた検索は、この iPhone では新しい 50 件までしか出ない

**何が、どういうときに消えるか。** ★を 50 件より多く付けている人が
起動すると、この iPhone が持っている一覧が新しい 50 件で**置き換わり**ます。
51 件目より古い★は画面から消え、続きへ行く道はありません。
**サーバーの行は消えません**（`saved_search` はそのままです）。

**どこにあるか。**
`www/net.js:2295-2296`（`netSearchSaved()` が `limit=NET_PAGE`、`NET_PAGE=50`
は `www/net.js:1490`）と `www/sns.js:1272`（`SET.saved=got;`）。

**それは人が作ったものか。** はい。その人が選んで星を付けた言葉です。

**書いた仕様はあるか。** ありません。
`docs/HANDOVER-2026-08-28.md:100` は「今は全部 50件（`NET_PAGE`）で止まり、
続きへ行く道が無い」と書いていますが、これはタイムラインと検索の
「続きを読む」の話で、★の一覧が置き換わることは書かれていません。
`docs/DATA_SAFETY.md` §「A shorter list is not a deletion」が求める三つ
（一覧の足に何件出ていないかを出す・その日に一度言う・検査）は、
どれも付いていません。

**直す大きさ:** 小。

---

## 6. 🔒 検索履歴は 5 件。6 件目を打つと一番古いのが消える

**何が、どういうときに消えるか。** 検索して 🔍 を押すたびに履歴が一つ増え、
6 件目で一番古い一件が、この iPhone からもサーバーからも消えます。

**どこにあるか。** `www/sns.js:1360`（`SNS_RECENT=5`）と
`www/sns.js:1405-1414`（`snsRecentAdd()`、押し出された分は
`netRecentDrop()` でサーバーからも消す）。
表は `supabase/schema.sql:445`、その理由は同 `:441-444`。

**それは人が作ったものか。** その人が**打った**ものです。作ったものではなく
履歴です（★は別の表で、こちらは残ります）。

**書いた仕様はあるか。** **あります。**
OWNER 2026-09-03「検索した履歴もユーザーはいらんから5個くらい検索履歴出る
ようにしたい」「1件づつ消せるでいいよ」。`docs/FEATURES.md:95`。
`supabase/schema.sql:441` にも「五件がこの機能そのもの」と書いてあります。

**直す大きさ:** ── （仕様どおりです）

---

## 7. 🔒 一筆は 160 点まで

**何が、どういうときに消えるか。** 文字を描いている最中、一本の線が 160 点を
超えると、それ以上は点が増えません。描き終わったものが後から消えるのでは
ありません。

**どこにあるか。** `www/glyph.js:95`（`GE_MAXPTS=160`）、
使う所は `www/glyph.js:1910`。

**それは人が作ったものか。** はい。線です。
ただしコードのコメントは「実際のどの線よりも高い数」と言っています。

**書いた仕様はあるか。** ありません（コードのコメントだけです）。

**直す大きさ:** 小。

---

## 8. 🔒 元に戻せる段数 ── これは人が作ったものではない

**何が、どういうときに消えるか。** 60 手（文字を描く画面）／40 手
（キーボード）より前には戻れません。描いているもの・組んでいるものは
消えません。

**どこにあるか。** `www/glyph.js:645` `:649` `:1398`（`GE_STEPS=60`）、
`www/keyboard.js:3110`（`KBU.u.length>40`）。

**それは人が作ったものか。** **いいえ。**どちらも画面を開いている間だけの
一時的な状態で、`localStorage` にもサーバーにも書かれません。

**書いた仕様はあるか。** ありません。

**直す大きさ:** ── （直すものではありません）

---

## 9. サーバー側 ── 自動で消えるものは無い

- **`on delete cascade` が 22 か所、`on delete set null` が 7 か所。**
  どれも「アカウントを消す」「言語を消す」「投稿を消す」から始まります。
  自分で押さない限り走りません。
- **他人の投稿に付いた引用の行は、引用元の言語が消されると一緒に消えます。**
  `supabase/schema.sql:501-503`（`quote.language ... on delete cascade`）。
  投稿の本文は残ります（`post.language` は `set null`）。引用の行は本文から
  作られる索引で、人が作ったものではありません。
  言語削除の DELETE REVIEW の中です。
- **話題の並びは直近 48 時間だけ** ── `supabase/schema.sql:1571`
  （`feed_hot`）。載らなくなるだけで、投稿は消えません。最新の並びと本人の
  ページには残ります。仕様は OWNER 2026-08-28。
- **`profile_rename` の 14 日**（`supabase/schema.sql:1898`）は @ を変えられる
  間隔で、消す仕組みではありません。

---

## 10. 🔒 同期で、ぶつかった値は iPhone の側が勝つ

**何が、どういうときに消えるか。** 二台の iPhone で同じ欄を別々に直すと、
後から同期した方の値だけが残り、もう一方は消えます。並び（単語や文字の
一覧）は両方足されるので、消えるのは「一つしか入らない欄」だけです。

**どこにあるか。** `www/sync.js:88`（`out[k]=mine[k];`）。

**それは人が作ったものか。** はい。

**書いた仕様はあるか。** **あります。**
`www/sync.js` の冒頭と `docs/DATA_SAFETY.md`。「両方足す。どちらも新しさで
勝たない。代わりに重複が出る」と書いてあり、値のぶつかりは iPhone の側を
返す、と明記されています。

**直す大きさ:** ── （仕様どおりです）

---

## 誰のものか ── 2026-09-04

**5・6・7・8・10・2番は、残す期間・上限の数・どちらが勝つかの話です。
オーナーのものなので `claude/keep3` は手を出していません。**
（2・6・10 は仕様どおりで、直すものではありません。5・7 は書いた仕様が
ありません。）

**1番は誰にも渡っていません。**`claude/keep3` は触っていません。

**3-a-2 は消す半分が `www/wordsheet.js`（`claude/pop`）にあります。**触っていません。
推測を書く半分は `www/core.js` で `claude/keep3` のものですが、こちらを
変えるのは振る舞いの判断なので手を出していません。

**4番は直す一行が `www/phases.js` にあり、`claude/keep3` の territory の
外です。**走らせて確かめてあり、一行と、その一行を入れた四通りの結果が
上に書いてあります。

## 確かめていないこと

- **実機では一つも押していません。**`claude/keep3` が回したのは
  `migrate-check` と `es5-check` の二本だけで、**全ゲートは回していません。**
- 3-a・3-b・4 のほかは、この一覧を書いたときのまま ── 読んだだけです。
- `docs/CHANGELOG.md`（15042 行）と `docs/FEATURE_RULES.md`（4695 行）は
  全文を読んでいません。決定ログの当該項目と、`期限`／`retention`／
  `自動削除`／`世代` の検索だけです。ここに「消してよい」と書いてある項目を
  見落としている可能性があります。
- PostgREST が返す行数の既定の上限は見ていません。5 の 50 件は
  アプリ側の `limit` を読んだものです。
- `www/i18n/`（十言語の文言）と `www/grammar-engine/` の中身は、
  保存に関わる所だけを見ました。
