# 2026-09-06 ビルド 139 ── 実機で確認する場所

書き方：**押す** → **こうなるはず**。★ は今日直した所（前は違う動きだった）。◇ は実機かサーバー越しでしか見られない所。プランは書いてある所だけ切り替える。

## 0. 先に一回だけ
- [ ] Supabase の SQL をもう一度流したか（スタッフの @ 追加が「見つかりません」を返すように変わった分）。
- [ ] 139 に上げたら、一度アプリを完全に閉じて開き直す。

## 1. 単語シート
- [ ] ★ 単語を開く → 右上の編集 → **品詞** → 一覧の行の**真ん中**を押す → 戻ったシートの品詞が変わっている → 右上の保存が金 → 保存で残る。語域・下位分類の一覧も同じ（行のどこを押しても効く）。
- [ ] ★ 編集シートで**下位分類**を選ぶ → 戻ったシートに入っている（前は入らなかった）。「なし」で外せる。
- [ ] ★ 編集シートで**つづりだけ**変える → 保存が金になる → 戻るで「保存しますか」が出る。
- [ ] ★ 新規シートで意味・例文を打って **Enter を押さずに**［追加］→ 意味と例文が入っている。編集シートの保存でも同じ。
- [ ] ★ 編集シートの左上の戻るの字が、その単語の名前（「制作」ではない）。
- [ ] ★ 読み（Plus/Pro）：音の札を押す → 画面の読みがその場で変わる → 戻ったシートも変わる → 保存しても単語の**名前は変わらない**。CSV で取り込んだ単語では音の札が出ない。
- [ ] 新しい単語の品詞は「直前に選んだ品詞」から始まる（最初は名詞）。これでいいか。
- [ ] 辞書の選択モードで 1 語だけ選んで削除 → ★「1 語を消しますか？」。
- [ ] 無料で 100 語 → 101 語目で ★「この機能を使用するにはアップグレードしてください」。
- [ ] ★ 何か変えて保存を押した瞬間に機内モード → 「保存できませんでした」→ 戻る →「保存しますか」に「いいえ」→ **画面の値が元のまま**（名前・単語・文字・キーボード・言語のページ・記事・メモ・文法の段と規則・書記体系、全部同じ）。機内モードを切って保存し直すと通る。

## 2. 文法
- [ ] ★ 一覧：上「規則で作る形」と下「章」に**同じ名前が二度出ない**（語順・複数・時制・否定・疑問・形容詞・場所は上だけ）。番号が 1 から通し。
- [ ] ★ 否定形の章の下に「not」の枠、疑問形の章の下に what/who/where/when/why/how の枠、場所の章の下に in/on/under/to/from/with の枠があり、前に入れた言葉がそのまま入っている。
- [ ] ★ 章の枠（make it）から単語シートを開いて綴りを打つ → 右上に［追加］がある → 押すと枠が埋まる（前はボタンが無くて消えた）。
- [ ] ★ 「規則を足す」→ 何も打たずに戻る → 空の規則が**残らない**。何か打って戻ると「保存しますか」。
- [ ] ★ 複数形の章の「n words」を二回押す → 二回目は増えない／ボタンが消える（前は kanok → kanokk と増え続けた）。
- [ ] 語順ボード：札をドラッグして並べ替え → 別の画面へ行って戻っても残る。3 語以外も置ける。
- [ ] 時制の章の「>>-」の規則行：追加・編集・削除。
- [ ] ◇ 名詞のページが真っ暗になる件（前回の指摘）── 実機で再現するか。

## 3. 文字・キーボード
- [ ] ★ 無料で文字の三部屋（アルファベット／記号／数字）を開く → 「n hidden」が**その部屋の数**（数字の部屋には出ない）。
- [ ] ★ Plus/Pro → キーボード ＋ → Chart → 二行目のスペースキーが**普通の幅**（前は細い棒）。
- [ ] ★ 有料で「一覧を取り込む → 文字に入れる」で q・z の名前の文字を入れてから無料に落とす → アルファベットが 26 のまま、q・z が**隠れない**。
- [ ] 描く：ペン太さ・塗り・ラウンド一回／二回で元に戻る・保存して開き直して形が残る。
- [ ] キーボード編集：何か選択中に別の行・列・キーを押すと**一回目は選択が外れるだけ**（「適当に触ったら選択解除」の決定どおり。変えたければ言う）。
- [ ] ◇ 拡張キーボードへの受け渡し → iOS のキーボードで自作文字が打てる。
- [ ] ★ ヘルプ「?」→ キーボードの手順：「設定を開く」のボタンが**3 番だけ**。押すと 設定 → Lingua。

## 4. SNS
- [ ] ★ 機内モードで投稿 → 先頭に「**未送信**」と出る → 機内モードを切って少し待つ → 「未送信」が消えて時刻になる。
- [ ] ★ 誰かをブロック → 自分の投稿のスレッドを開く → その人の返信が**出ない**。自分の返信のスレッドで親にも出ない。
- [ ] ★ 写真を付けて「文字」→ ⒜ → まだ打っていない欄が**写真の中に収まる**（前は縦の帯ではみ出た）。何も打たずに Done → 空の印が残らない。
- [ ] ★ 凍結中：プロフィールとスレッドの投稿の行に返信・ブースト・いいねが**出ない**。
- [ ] ◇ 二つ目のアカウントで：フォロー／解除、通報、通知の既読、他人のプロフィールの言語の行、他人の公開言語の DL（Plus 1 つ／Pro 3 つ）。
- [ ] ◇ 声（30 秒）が投稿に載って、別の端末で聞ける。
- [ ] 右スワイプで閉じる、タイトル押しで戻る（前回の直し）。

## 5. 設定・アカウント・プラン
- [ ] ★ 設定 → Your language → 名前を空にして保存 → 「未設定」になる。もう一度付けると戻る。
- [ ] ★ 設定 → Account の Email の行：押しても何も起きない（見た目は前と同じ）。
- [ ] ★ 「この言語を削除」を機内モードで押す → **消えない**で「言語を削除できませんでした」。電波ありなら消えて、別の端末でも消える。
- [ ] ★ 「この言語を削除」「アカウントを削除」「プランが終了しました」の文に**バックアップの話が無い**。
- [ ] ★ プランが終了しました：新しい説明文（消えていない／隠れているだけ／戻る）、［閉じる］が［アップグレード］の直下。
- [ ] ◇ 実際にプランを落とす（サブスク解除かサンドボックスの期限切れ）→ 105 語の言語で 設定 → Your language が「105 / 100」、辞書は 100 語まで、Pro に戻すと 105 全部戻る。
- [ ] ★ 上限のポップ（単語・キーボード・言語・DL、Pro で言語 3 つ目の次も）が全部「この機能を使用するにはアップグレードしてください」。
- [ ] ★ 無料で検索タブの「CSV から取り込む」→ アップグレードのポップで止まる（前は通った）。Plus で通る。
- [ ] ★ Plus で取り込み 1000 語超え → 出る文に「Free」と書いていない。
- [ ] ★ 「この言語の用途」：概要が閉じたまま右端の ＋ → 概要が開いて新しい行が見える。
- [ ] 「この言語の用途」：新しい言語は公開オン・DL の四つはオフで出る。**公開を切ると下の行が全部消える** ── これでいいかまだ返事をもらっていない。
- [ ] ◇ サインアウト → 別アカウントでサインイン → 前のアカウントの検索履歴・設定・言語が出ない。
- [ ] ◇ 購入（月／年）、復元、管理。
- [ ] ◇ Apple／Google サインイン、パスワード忘れ。
- [ ] ◇ スタッフの @ 追加（SQL を流した後）：無い @ なら「見つかりません」、ある @ なら入る。

## 6. オンボーディング
- [ ] ◇ 空の端末で最初から最後の扉まで。途中でアプリを閉じて開くと描いた字は消える（仕様）。扉を通った直後に、別の端末でその言語が見える。

## 7. 直していない・判断待ち
- 公開オフで「この言語の用途」の下の行が全部消える（返事待ち）。
- キーボード編集の「一回目は選択が外れるだけ」（決定どおり）。
- 電波が無い時に「保存しますか」→「はい」を押すと、保存はされるが画面が動かない（「通信エラーなら進まない」の決定どおり）。
- 文法一覧の下の「章」に残っている言葉の枠は前のまま（本体は「規則で作る形」に移った）。
