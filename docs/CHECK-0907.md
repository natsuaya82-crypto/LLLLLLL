# 2026-09-07 ビルド 140 ── 実機で確認する場所

書き方：**押す** → **こうなるはず**。番号は 2026-09-06 夜の指摘 22 件の番号。◇ は実機かサーバー越しでしか見られない所。

**このうち 8 か所を headless で一本に通した記録が `docs/WALK-141.md`** ── 押す順のまま、前の画面の状態を引きずって次を押した歩きです。実機の代わりにはなりません（見た目・サーバー・端末は見ていません）。

## 0. 先に一回だけ
- [ ] Supabase の SQL は**流し直さなくていい**（schema は変わっていない）。
- [ ] 140 に上げたら、一度アプリを完全に閉じて開き直す。
- [ ] **キーボードが 8 枚に増えていた分は自分で消す**。増える原因（板に id が無く、突き合わせが別物と見ていた）は止めたが、増えてしまった板は消していない（勝手に消すのは禁止）。一覧で要らない板を開いて削除 → 一覧に戻る（1）。

## 1. キーボード
- [ ] 1 キーボードを削除 → **一覧に戻る**（前は 1 枚目の板が開いた）。
- [ ] 2 目次のキーボードの行には「？」が**無い**。板を開いた画面の bar に「？」がある → 押すと手順。
- [ ] 3 列の頭（上の字）を押したまま横に引く → 引いている間も止まらずに列が光る → 離すと選択が決まる。
- [ ] 4・5 どのキーを開いても同じ画面。「押したとき」は **文字／スペース／削除／改行 の 4 つだけ**。「層」と「幅」の段は**無い**。前に層キーや幅を変えたキーがある板は、そのまま動く（ページの行き来、広いキー）。
- [ ] 6 板を変えて保存 → 「保存しました」が一度出る。単語・文字・メモ・文法・言語のページでも同じ一言。
- [ ] 7 キーボード一覧に「キーに文字を表示」のスイッチが**無い**。板を開いた画面には残っている。
- [ ] 8 アプリを閉じて開き直す・機内モードを切り替える・別の端末で開く → キーボードの**枚数が増えない**。

## 2. 単語・文法・メモ
- [ ] 9 文法 → 語順：下に札（主語・動詞・目的語…）が並び、押すと上の列の末尾に入る。上の札を押すと下に戻る。枠の数は決まっていない。右上の**保存**で残る → 別の画面へ行って戻っても同じ順。**保存する前は下の例文の行が古い順のまま**（保存すると変わる）── これでいいか。
- [ ] 10 単語シート → 意味の ＋ → **一つ目の意味が消えない**で二つ目の欄が開く。
- [ ] 11 編集シート → 下位分類 → 「＋新しく作る」の行が**他の行と同じ高さ**。
- [ ] 12 例文の ＋ → 次の例文の欄が開く（前は何も起きなかった）。
- [ ] 13 例文は 1 行目がその言語、2 行目が訳。
- [ ] 14 自作フォントをオンにして例文を見る → **四角（NO GLYPH）が出ない**。描いた文字は自作の形、描いていない文字はローマ字のまま。辞書の行・投稿・カードも同じ。
- [ ] 15 メモを開く → **閲覧**で開く（打てない）→ 右上「編集」→ 打てる → 右上が「保存」→ 保存で戻る。何か打って戻ると「保存しますか」。

## 3. SNS
- [ ] 16 制作側（描く画面・辞書・文法）で上から引いても**画面が下がらない**。フィード・検索・通知・プロフィール・スレッドでは引くと更新。
- [ ] 17 返信の返信（孫）にも上から線がある。二番目以降の返信にも。
- [ ] 18 「この言語について」を非公開にする → **自分では開けて編集できる** → 公開スイッチで戻せる。◇ 別のアカウントからは見えない。
- [ ] 19 プロフィールを開く → フォロー／フォロワーの数に**くるくるが出ない**。前に見た数が出て、答えが来たら変わる。初めて見る人は数の代わりに語だけ。※ 頼まれたのは「全部読み込んでから画面を出す」だった。今の形は「前の値を見せて後で差し替え」── **違う形なので、これでいいか一言**。
- [ ] 20 フィードの絞り込みに **#今日のお題** の行がある → その日のお題に答えた投稿だけ。前の日のお題の投稿も、本文の #今日のお題 を押すと検索に入る。
- [ ] 21 設定 → パスワード変更 → 何もせず戻る → **設定に戻る**（ログインを求められない）。「忘れた」からも同じ。

## 4. 取り込み
- [ ] 22 検索タブ → CSV から取り込む → 画面が 4 つ：①貼り付けかファイル → ②貼り付け欄（ファイルなら選んだ後） → ③列ごとに「この列は何」の行 → ④「n 語を取り込む」。
- [ ] 22 列に 例文・例文の意味・語域・タグ・語源・メモ・下位分類 がある表を入れる → ③で全部の列が選べる → 取り込んだ単語を開くと**全部入っている**。
- [ ] 22 取り込んだ後は**辞書に立つ**（終わった画面は残らない）→ 戻るを押しても取り込み画面に戻らない。
- [ ] 22 ◇ 取り込んだ単語が別の端末でも出る（サーバーに上がっている）。

## 5. 入らなかったもの・判断待ち
- 9 語順ボードの例文の行が保存前は古い順のまま（上に書いた）。
- 19 フォロー数の形が頼まれた「全部読み込んでから」と違う（上に書いた）。
- 8 増えてしまったキーボードは手で消す（0 に書いた）。
- ゲートの「左端スワイプで戻る」の検査が、4 本並列で回すと時々落ちる（アプリ側ではなく検査側の揺れ。r3-sns が検査を直し中で、ビルド 140 には入っていない）。

# ビルド 141 ── 2026-09-07 の指摘 15 件（round 4）

## 0. 先に一回だけ（141 の前に）
- [ ] **Supabase の SQL を流し直す**（`supabase/schema.sql`）。スタッフ追加の `staff_add` と、課金の `purchase` 表・`plan` の読み取り専用がこれで入る。流していないと「スタッフ追加で何も起きない」はそのまま。
- [ ] **Edge Function `verify-plan` を deploy**（手順は `supabase/setup.md` § 8b）。これが無いと 141 では段（Plus/Pro）が付かない。
- [ ] 141 に上げたら、一度アプリを完全に閉じて開き直す。

## 1. キーボード
- [ ] 目次の「キーボード」の横に「？」が**無い**。キーボード一覧（板が並ぶ画面）の bar に「？」がある。板を開いた画面には無い。

## 2. 単語・文法
- [ ] 単語の**編集シート**で例文を見る → 四角が出ない（Lingua キーボードで打った字も含めて）。
- [ ] 文法 → 語順：**最初は上が空**、札は下に箱で並ぶ → 押すと上へ、上を押すと下へ → 上半分に罫線 → 右上の保存 → 例文が罫線の下にその順で出る。
- [ ] 文法 → 過去形など：**規則の一行**、辞書の語の**表**（kano → kanota）、例文。「規則を足す」は ＋。
- [ ] 文法 → 名詞：3 行が「**私は** 山を見る」の形（主語・格の言葉が無い）。
- [ ] 文法の一覧が文法書の目次の順（語順 → 名詞 → 動詞 → 代名詞 → 数 → 挨拶…）、まだの章は薄い字。

## 3. SNS
- [ ] スレッド：孫の返信にも上から線が届く。
- [ ] 投稿以外の画面：欄の外を押すとキーボードが下りる。
- [ ] 投稿画面：意味を押しても**本体が動かない**。道具の行だけがキーボードの上に付いてくる。縦書き・横書き同じ。日本語キーボードでも。

## 4. 設定・アカウント
- [ ] ログアウト → 扉が**サインインの顔**で開く（パスワード送信の画面ではない）。「忘れた」を途中でやめた後でも同じ。
- [ ] 再ログイン → **プロフィール**に着く。
- [ ] 管理画面のスタッフ @ 追加（SQL を流した後）→ 一覧に増える。無い @ なら文が出る。

## 5. 取り込み
- [ ] 取り込みを開く → 毎回「貼り付ける／ファイルを選ぶ」の選択画面から。

## 6. 課金（サンドボックス、実機のみ）
- [ ] 買う → 段が付く（サーバーが決めている）。
- [ ] 同じアカウントで「購入を復元」→ 付く。
- [ ] **別のアカウント**でサインインして「購入を復元」→ **付かない**。
- [ ] 期限切れ → free に落ちる。

## 7. 審査
- [ ] `PrivacyInfo.xcprivacy` が本体・キーボード・Widget に入ったビルド（141）を上げて、Apple から警告のメールが来ない。
- [ ] 審査ノート：審査員用アカウントと、キーボードのフルアクセスの文（`docs/apple.md` § 5）。

## 8. 直していない・判断待ち
- 増えてしまったキーボードは手で消す（140 と同じ）。

# ビルド 142 ── 2026-09-07 昼の指摘（round 5）＋文法書の続き

- [ ] 他人のプロフィールの右下 ＋ → 投稿画面が「@そのhandle 」入りで開く。自分のプロフィールでは空。
- [ ] 通知「jj と linguaa がフォロー」を押す → 二人の一覧 → それぞれへ。
- [ ] 語順の画面：札を押しても画面がカクつかない（本体を描き直さない）。通知・プロフィールのスクロールも。
- [ ] 目次に「この言語について」の行が**無い**（隠しただけ。プロフィールの言語の行からは今までどおり開ける）。
- [ ] 文法（入った分）：名詞句の並び／複文（従属節・関係節・並列）／格に 所有・場所・道具・共同／性・名詞クラス／冠詞・指示詞／人称・数の変化 … 一覧の順は 文の骨格 → 名詞 → 動詞 → 修飾 → 付録。空の章は薄い字。
- [ ] `docs/WALK-141.md`：141 を押す順に headless で通した記録（45 項目 FAIL 0）。

文法の新章は 9 つ全部 142 に入った（名詞句の並び／複文／格 7 つ／性・名詞クラス／冠詞・指示詞／人称・数／法に可能・義務・願望／コピュラ・存在／形容詞の比較）。翻訳（意味 → 文）の検査は grammar-engine-check に章ごとにある。

# ビルド 143 ── 2026-09-07 夜の指摘（round 6）

master 7f03753b。ゲート 42 本緑。**実機未確認**（headless のみ）。

## 1. プロフィール・人
- [ ] 他人のプロフィールを開く → **開いた瞬間に**フォロー中／フォロワーの数と「非公開」の印が出ている（後から入らない）。押してから揃うまでは前の画面のまま、くるくる無し。
- [ ] 起動 → スプラッシュはプロフィールが揃うまで降りない（900ms〜4000ms の間）。
- [ ] フォロー一覧・通知の「他N人」 → 顔が「？」で出て後から変わる、が**無い**。揃ってから開く。◇ 二十人でも要求は 2 本（人と言語名）。
- [ ] タイムラインの顔・名前・本文の @handle・返信の「Replying to @〇〇」の @ → どれもその人のプロフィール（入口は `profileOpen` 一つ）。自分の顔 → 自分のプロフィール。

## 2. 投稿画面
- [ ] 英語 UI で縦書きの投稿画面 → placeholder と打った字が**一列**、ローマ字は横に倒れる（二列目に折れて切れない）。日本語は立ったまま。
- [ ] 返信の投稿画面 → 新規と**同じ固定の形**（引用の投稿だけ上に足される）。
- [ ] 縦書きでも「Replying to @〇〇」の行は**横のまま**。
- [ ] 本文を「@handle 」で始めて投稿 → その人への投稿になる（返信の形で出る）。存在しない handle・返信の中の @ はそのまま文字。

## 3. キーボード
- [ ] アプリを更新して開く・閉じて開き直す・別端末で開く → **枚数が増えない**。
- [ ] 8 枚に増えていた同じ中身の板は**一枚に畳まれる**（「消していい」OWNER 2026-09-07）。中身の違う板は残る。
- [ ] 板の id は中身から決まる（id の無い古い板が読むたびに別物にならない）。

## 4. 直していない・判断待ち
- 「@handle 」で始めた投稿がプロフィールの「返信」タブに出るか ── BACKLOG。
- post-check の下書きの主張が 4 本並列のときだけ稀に赤 ── 検査側の揺れ、BACKLOG。

# ビルド 144 ── 2026-09-08 の指摘（round 7）

master 0c459682。ゲート 43 本緑。**実機未確認**（headless のみ）。

## 0. 先に一回だけ
- [ ] **Supabase の SQL を流し直す**（`supabase/schema.sql` 丸ごと）。増えた列・変わった問い：`profile.link`・`profile.loc`、`profile_seen` に言語の列、おすすめ（`feed_hot`）から返信を除く。流すまで ◇ の項目は見えない。
- [ ] `npm run rls` の plan 3 件の赤は検査側が古かった（plan は誰も書けない、が今の仕様）。検査を書き換えた。

## 1. 投稿画面
- [ ] 他人のプロフィールの ＋ → 本文は**空**、欄の上に「Replying to @〇〇」の行。輪は満ちている（@ は数えない）。縦書きの言語でもこの行は横のまま、欄だけが縦。訳のプレビューに @ が出ない。
- [ ] 本文の頭に「@handle 」と打つ → その場で欄から抜けて同じ行になる。送った投稿はその人への投稿。無い handle・投稿への返信の中の @ はそのまま文字。
- [ ] 戻る → 「下書きに保存しますか」が**他の問いと同じ形のポップ**（画面中央）。外を押すと留まる。保存 → 下書きに入って投稿画面は空、破棄 → 空。
- [ ] 写真 4 枚の投稿 → 一枚目を開いて横に送る → 2/4、3/4、4/4。
- [ ] 英語 UI で今日のお題の投稿 → 札が **#TodaysPrompt**、日本語に戻すと #今日のお題。誰の投稿でも。投稿画面の札も表示言語。保存は一つの綴りのまま。

## 2. プロフィール・人
- [ ] ◇ 編集でリンクと場所を入れる → 自分の頁と他人の頁で bio の下に出る。空なら行が無い。
- [ ] フォロワー一覧の「Follows you」が**名前の横**（@ の横ではない）。
- [ ] 他人のプロフィール → フォロワー → **一覧が開く**（永遠くるくる無し）。0 人なら空の一覧、失敗ならポップ。
- [ ] 言語を非公開にする → ログアウト → ログイン → **非公開のまま**。スイッチは答えが戻ってから動く（端末に hide は無い、サーバーの published_at だけ）。
- [ ] 設定の見出し 7 回 → 管理画面（@lingua のみ）。ログアウト → ログインした後も。
- [ ] ログアウト → ログインで pro に**ならない**（headless で全部の道を測った。直していない）。

## 3. フィード・速さ
- [ ] ◇ おすすめに返信が出ない。フォロー中と今日のお題は今まで通り。
- [ ] 起動と人の一覧が速い（起動の直列 8 段 → 5 段、人の一覧の往復 4 → 2）。体感で。

## 4. 文法
- [ ] 一覧の最後に「この言語について」が**無い**。

## 5. 直していない・判断待ち
- 端末が二か所目の答えを持っている所の一覧：`docs/reports/SWEEP-0908.md`（8 候補、消していない ── 朝に決める）。
- iPhone の通知はリリース後（保留）。
- 写真の帯と左端の戻るスワイプが喧嘩しないかは未測定（BACKLOG）。
- 投稿画面で宛先を外す手段は無い（頼まれていない、BACKLOG）。
- お題の札を押した検索の箱に入る文字は綴りのまま（BACKLOG）。

# ビルド 145 / 146 ── 2026-09-09「端末に残すものはない」（round 8）

master b7f49f60（145）→ a223b3ba（146、DL した言語が起動で戻る）。ゲート 43 本緑、`npm run rls` 緑。**実機未確認**（headless のみ）。

## 0. 先に一回だけ
- [ ] **Supabase の SQL を流し直す**（`supabase/schema.sql` 丸ごと）。増えたもの：`language.wsys` 列、`profile.prefs` 列、`language_take` 表（+ RLS 3 本）、`language_took()` 関数と `slice_read`／`language_read`／`take_make` の書き換え（r9、非公開でも取った人は読める）、`language_seen` の作り直し。144 の分（`profile.link`・`profile.loc`・`profile_seen` の言語の列・`feed_hot`）が未実行ならそれも同じ一回で入る。**流すまで言語の一覧・記事・ダウンロードは「接続できません」**。手順は `supabase/setup.md` の 2026-09-09 の節。
- schema: https://github.com/natsuaya82-crypto/LLLLLLL/blob/master/supabase/schema.sql （raw: https://raw.githubusercontent.com/natsuaya82-crypto/LLLLLLL/master/supabase/schema.sql ）

## 1. 言語（サーバー一本になった 9 つ）
- [ ] 言語の名前 → 変えてログアウト → ログイン → 同じ名前。二台目でも同じ名前（`language.name` だけ）。古い端末の名前は列が答えるまで表示だけ（上書きしない）。
- [ ] オンボーディングを終えた端末で アカウントを削除 → 同じ端末で別アカウントにサインイン → その人の profile 行が無ければ**その人の**オンボーディング、有れば飛ばす（`SET.walked` は「この端末は歩いた」だけ）。
- [ ] 他人の言語をダウンロード → 上限（plus 1・pro 3）は**二台目でも続きから**数える（`language_take`）。自分の言語は上限に入らない。
- [ ] （146）他人の言語を ↓ で取る → アプリを**完全に閉じて開き直す** → 切り替え（プロフィール長押し）にその言語が残っていて、開くと文字・キーボードなど**中身がある**。自分の言語は一バイトも動いていない。起動は遅くなっていない（直列 4 段）。
- [ ] （r9）**元が消したら端末からも消える** ── 二台（または二アカウント）で。A の言語を B が ↓ で取る → A が「この言語を削除」→ B がアプリを閉じて開き直す → 切り替えにその行が**無い**（空の行が残らない）。B の自分の言語はそのまま。**機内モードで開き直したときは消えない**（答えが来ていないので）。
- [ ] （r9）**非公開は新規 DL を止めるだけ** ── A が言語を非公開にする → 既に取っている B は開き直しても**文字・キーボードが見える**（名前も出る）→ 取っていない C の記事からは見えず ↓ も無い。※ **SQL を流し直してから**。
- [ ] 書記体系（アルファベット／音節／abugida／表語）を変える → ログアウト → ログイン → 同じ。二つ言語を持つと別々に持てる（`language.wsys`）。
- [ ] テーマ・表示言語・自作フォント・自作文字・キーボードのローマ字面 → 二台目でサインインすると同じ設え（`profile.prefs`）。答えが戻る前は端末の色のまま、戻ったら切り替わる。

## 2. 投稿・人
- [ ] いいねの数と「自分が押したか」→ 二台目で同じ。押す → 数が動く → 二台目を引き直すと同じ数。
- [ ] フォロー → 二台目で「フォロー中」。フォロワー数・フォロー数も同じ（`follow` 表だけ）。
- [ ] 自己紹介・リンク・場所 → 二台目で同じ三つ。空なら行が無い。
- [ ] 下書き → 二台目で同じ一覧。消す → 二台目でも消えている（`draft` 表だけ、端末の写しは無い）。

## 3. 電波なし
- [ ] 機内モードで開く → **前に読み込んだ**言語・タイムライン・プロフィールは見える。名前・数・フォロー・設えは最後に見た値のまま。何も作れず、何も保存されない。
- [ ] 機内モードを戻す → 引き直すとサーバーの値に揃う。端末の写しが勝って何かが戻ることは無い。

## 4. 直していない・判断待ち
- 「まだ聞いていない」状態は**どちらとも描かない**（揃ってから開く）。名前が一瞬空・スイッチが一瞬動かないのはこれ。
- iPhone の通知はリリース後（保留）。
- `docs/BACKLOG.md` の 2026-09-09 の項：古い `lang` 鍵をいつ読まなくするか（オーナー判断）、`syMerge()` の道。

# ビルド 147 ── 2026-09-09 DL 言語の二つ（round 9）

master e82c9595。ゲート 43 本緑、`npm run rls` 緑（341 attempts）。**実機未確認**。

## 0. 先に一回だけ
- [ ] **Supabase の SQL を流し直す**（`supabase/schema.sql` 丸ごと）。変わったもの：`language_took()`、`language_read` / `slice_read` / `slice_dl` / `language_seen` に「取った人」、`take_make` に「公開中」。**流すまで、非公開になった言語を取った人は空のまま**。
- schema: https://github.com/natsuaya82-crypto/LLLLLLL/blob/master/supabase/schema.sql

## 1. 元が消えたら取った側からも消える
- [ ] B が A の言語を取る → A がその言語を削除（または A がアカウント削除）→ B がアプリを閉じて開き直す → 切り替え（プロフィール長押し）に**その言語が無い**。空の行も残らない。
- [ ] 同じ状態で B が**機内モード**で開き直す → まだ切り替えに残っている（答えが来ていないので消さない）。電波を戻して開き直すと消える。
- [ ] B の自分の言語は一語も動いていない。

## 2. 非公開は新規 DL を止めるだけ
- [ ] B が A の言語を取る → A が非公開にする → B がアプリを閉じて開き直す → **B の DL 言語はそのまま**（中身がある）。
- [ ] 同じ状態で取っていない C が A のプロフィールを見る → 記事が無い／取れない。

## 3. 確定（何も作っていない）
- 記事の ↓ はその言語に切り替えない。
- 切り替え画面のスライド削除はメモ（BACKLOG）。
