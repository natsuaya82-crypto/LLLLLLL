# What was ALREADY lost the day tools/docs-check.mjs was written, 2026-09-04.
# A NEW one fails. Taking a line OUT is progress and needs nobody -- either
# the map learned to point at the document, or the document went.
#
# `orphan <path>`               nothing reaches it from CLAUDE.md, README.md
#                               or docs/STATE.md.
# `dangling <doc> -> <path>`    <doc> sends a reader to a page that is not
#                               there.
#
# Two documents nobody is sent to. Not fixed here on purpose: which map each
# belongs on, and whether a handover from a week ago is still worth a page,
# are the owner's, not a check's.
#
# `ONE.md` was written, then deleted the same day on the owner's word
# (2026-09-04, decision log). No line here allows a pointer at it: the two
# sentences that still remember the page say its bare file name, which is what
# this check asks of a sentence ABOUT a document that is gone.

orphan docs/HANDOVER-2026-08-28-2.md
orphan docs/SCOPE-yaa.md
