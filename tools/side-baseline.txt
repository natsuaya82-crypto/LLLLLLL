# Screens that were already wider than a 402pt phone the day tools/press.mjs
# first stood in front of one. A NEW one fails. Taking a line OUT is progress
# and needs nobody -- it means the screen fits now.
#
# All three that were here were the same fault and all three are gone, 2026-08-25:
# the three tiles a new key is dragged off are the size of the key they make, and
# on a board fewer than six keys across they were wider than the sheet. A column
# is a fixed width now (kbCellW / kbSheetW in www/keyboard.js), so six of them are
# six tenths of a full board and fit by construction.
